# 需求文档：Android 后台常驻接收与开机自启

| 项目 | 内容 |
|---|---|
| 文档状态 | **修订稿 v1.5 —— 已无「设计前必须」的开放项，可以进入设计**（第五轮复核 01…03 的文本矛盾已消除）。① §16.1 的 **37 项决策已确认**（DEC-01…DEC-37，2026-09-23 由所有者逐项选定/提供）；② §20.3 的 6 处修订已**追认**；③ 原始条目原文已补录并核对（DEC-31）；④ **验收判据统一为三类**（§14.5）：**A 正向可达**（AC-01…AC-11；**AC-01/02/10 双判据**）、**B 负向不出现**（AC-12、AC-13）、**C 恢复窗口**（AC-08/10/11 的恢复部分，30 分钟，窗口内"尚未恢复"不计失败）；短时正向项零失败（DEC-32/34/35）；⑤ 关闭开关 = 只停"后台常驻"，**前台发现与连接都沿用现状**（DEC-33 + DEC-36）；⑥ **新增需求（DEC-37）**：开机恢复要求到**解锁前** —— 存储改设备加密存储、清单加 `directBootAware`；**验证路径待实测**（能自动化则纳入 L1，不得预先断言只能 L2），且**优先级低于"尊重用户停止"**（DEC-19/DEC-30）。⑦ 仍待跟踪（不阻塞设计）：A-16、A-18、A-19（**实现前须确认**）、A-20，与 Q-06、Q-11、Q-13、Q-14、Q-17；进入实现前须统一 README 与 `AGENTS.md` 的过期记录（D-07、D-08）。**设计阶段交付物**：生命周期状态机（DEC-33）、探测规程（§14.5）、追踪矩阵（§14.2.1，含 DEC-37×DEC-19 组合）、解锁前能力边界核实与存储方案（DEC-37）。 |
| 版本 | v1.5（2026-09-23）—— 第十一次修订：处理第五轮复核 01…06（按 DEC-36 统一 REQ-001/REQ-002 的前台发现表述；按 DEC-37 统一 REQ-006 的解锁前口径与 AC-10 的双判据采样；把 §14.5 的通过总则改为**服从三类判据**、§1.3 同步；补 DEC-37×DEC-19 的优先级与组合用例；更正"只能 L2"的过强断言）；v1.4（DEC-36/37）、v1.3（第四轮 01…07）、v1.2（DEC-33/34/35）、v1.1（W-01…W-08）、v1.0（DEC-32）、v0.9（原文补录）、v0.8（DEC-30）、v0.7（DEC-18…DEC-29）、v0.6（V-01…V-16）、v0.5（R-01…R-16）的记录同见 §20 |
| 适用仓库 | `localsend_x`（LocalSend 私有魔改 fork） |
| 上游代码基线 | `230fb692`（v1.18.2，2026-09-14）—— 本 fork 代码演进的起点 |
| 协作规则基线 | `fe5e5929` —— `AGENTS.md` 启动版的确认提交 |
| 本文档基线 | `b8f4900c` —— 本文件上一版（v0.4）所在提交 |
| 依据 | 仓库所有者的原始想法、后台运行策略说明，及其对 v0.1 待确认问题的逐项答复（见 §16.1）；`AGENTS.md`（协作规则，下半部分）；现状事实来自 §3 的只读复核；平台约束来自 §18 的官方文档核对 |
| 本期范围 | 仅 Android 端后台保活与开机自启；不改其他平台、不改既有功能语义 |

> 本文件是**需求**文档，不含实现方案与代码。
> §16.1 是**已确认决策**（有出处、可追溯）；§16.2 是仍需决定的开放项，其中标为「**设计前必须**」者必须先落定，其余不阻塞架构设计。
> 与 `AGENTS.md` 的规则偏差记录在 §17；本次独立复核意见 R-01…R-16 的逐条处理见 §20。

---

## 1. 目标与用户

### 1.1 目标

让 LocalSend Android 客户端成为一台**可被长期发现在线的局域网接收端**：用户开启「后台接收」后，本机在应用退到后台、返回桌面、锁屏息屏、乃至设备重启之后，仍持续保持局域网设备发现与连接监听，使同一网络内的其他设备**在任何时间**都能发现本机并发起文件传输，无需用户提前打开应用。

### 1.2 用户

| 角色 | 描述 | 与本需求的关系 |
|---|---|---|
| 主用户（仓库所有者） | 把本机作为常驻接收端，希望随时从自己的其他设备向手机发送文件 | **L2 真机最终验收**的执行人（发布前，用自己的 Android 16 手机）；日常 L1 验收由 AI 在本机自动化完成（DEC-16，§14.1） |
| 发送方（同网络的其他人/自己的其他设备） | 使用任意 LocalSend 客户端或本 fork 的其他实例 | 只感知"对方在线"，不感知本需求的存在 |
| 维护者 | 后续在本 fork 上继续演进的人/模型 | 需要本文件与 `AGENTS.md` 保持一致 |

### 1.3 成功定义（一句话）

用户在**默认开启**的两个开关下（§16.1 DEC-10）完成一次性引导后，**在所有验收场景中任意时刻**，另一台同网段设备都能**发现并连接**到本机。**判据分三类**（详见 §14.5）：**正向场景 AC-01…AC-11** 要求"发现并连接"（其中 **AC-01 / AC-02 / AC-10 为双判据**："可被发现 **且** 可连接"，其余判"可连接"）；**负向场景 AC-12 / AC-13** 要求**不出现**相反结果；**恢复类**在 30 分钟窗口内出现恢复即算成立（窗口内"尚未恢复"不计失败）。（DEC-32、DEC-35、DEC-37、§14.5 三类判据）

---

## 2. 原始需求逐条落位

> **可审计性说明（v0.5 修订、v0.6 更正、v0.9 补录完成）**：三组原始条目在仓库内都**没有**独立载体（它们是在对话中提出的），因此本节必须**自己承载原文**：
> - 第 1 组（5 条原始想法）：见 §2.1，已由所有者在 2026-09-23 确认（**DEC-29**）。
> - 第 2 组（10 条实现要求）与第 3 组（原始 **11** 项验收标准）：**原文已于 2026-09-23 由所有者提供并逐字补录**（§2.2 / §2.3），逐条映射已核对完毕（**DEC-31**）。Q-28 关闭。
>
> **一处更正**：v0.5 曾称第 1 组"在 `README.md` 的 fork 段与 §3.3 中有载体"，该说法**有误**（`README.md` 未记录这些条目）；另 v0.4 把验收标准记作"10 项"，实为 **11 项**（§2.3）。二者均已更正。
> **自本版起**："无遗漏、无扩写"对三组均**成立**，依据是原文与逐条核对表，而不是推断。

### 2.1 原始想法（5 条，**已由仓库所有者确认，2026-09-23｜DEC-29**）

| 原始条目 | 落位 |
|---|---|
| 1. 软件可以设置开机自启 | REQ-006（**独立「开机自启」开关**，另见 §16.1 DEC-10） |
| 2. 软件可以在锁屏状态下后台运行 | REQ-004 |
| 3. 软件可以在任务栏中常驻来保活 | REQ-002（常驻主体）+ REQ-003（通知可见性） |
| 4. 不改变软件其他功能 | REQ-011、NFR-006 |
| 5. 只需要修改安卓端，其他平台不进行修改 | §5.2 明确不做、NFR-006 |

> 上表五行是**本文件对所有者原始表述的转录**，仓库内**无独立载体可比对**（v0.6 更正）；**已由仓库所有者于 2026-09-23 确认其准确性**（DEC-29）—— 即：本文件本节是这五条的唯一载体，检索本仓库不会找到第二处，引用时请以本节为准。

### 2.2 后台运行策略（原始 10 条实现要求；**原文已补录，2026-09-23**）

**原文·目标（所有者提供，逐字）**：

> 后台运行策略：高可靠长期常驻。
>
> 目标：当用户开启"后台接收"后，即使应用处于后台、设备息屏、当前没有文件传输任务，也应持续保持局域网设备发现和连接监听，使其他设备能够随时发现本设备并发起文件传输。

**原文·实现要求（所有者提供，逐字，10 条）**：

| 序号 | 原文 | 落位 | 核对结果 |
|---|---|---|---|
| 1 | 使用长期 Foreground Service 作为后台常驻主体。 | REQ-002 | ✅ 一致 |
| 2 | 后台接收开启期间允许通知栏显示永久通知。 | REQ-003 | ✅ 一致 |
| 3 | 前台服务优先按 connectedDevice 场景设计，不使用 dataSync 作为全天候保活服务。 | REQ-002.2、§3.4、DEC-11 | ✅ 一致。原文的"**优先**"所留的弹性由 REQ-002 边界保留（真机不接受 `connectedDevice` 前置条件时回退到"其他允许的类型或路径"） |
| 4 | App 进入后台、返回桌面、锁屏后，服务继续运行。 | REQ-004 | ✅ 一致 |
| 5 | 用户从最近任务列表划掉 App 后，应尽可能保持后台接收服务。 | REQ-005（尽力而为） | ✅ 一致 |
| 6 | 引导用户：关闭应用电池优化；设置后台运行"不受限制"；国产 ROM 开启自启动/后台运行权限。 | REQ-009 | ✅ 一致（三项逐一对应） |
| 7 | 设备重启后，在 Android 规则允许范围内自动恢复后台接收。 | REQ-006 | ✅ 一致 |
| 8 | 服务因异常或系统资源压力退出后，使用 Android 允许的机制进行恢复。 | REQ-007 | ✅ 一致 |
| 9 | 不采用 1 像素 Activity、无限循环 Alarm、高频 WorkManager 等明显滥用系统机制的方案。 | §5.2、NFR-001 | ✅ 一致。本文件另列"多进程互相守护""伪造前台通知的空壳服务"两项，属原文"**等**明显滥用系统机制"的展开，已在 §5.2 注明理由，不是隐藏新增 |
| 10 | 用户主动关闭"后台接收"时，必须彻底停止常驻服务。 | REQ-008 | ✅ 一致 |

> **核对结论**：10 条**逐条对应、无一对多、无一对零、无重复、无未覆盖项**；v0.4 的合并映射（"实现要求 N→REQ-xxx"）经原文核对**全部正确**。§2 的"无遗漏、无扩写"自本版起**对该组成立**。

### 2.3 原始验收标准（原始 **11** 项；**原文已补录，2026-09-23**）

**原文（所有者提供，逐字）**：

| 序号 | 原文 | 落位 | 核对结果 |
|---|---|---|---|
| 1 | App 在后台 1 小时后仍可被发现； | AC-01 | ✅ 一致 |
| 2 | 息屏 1 小时后仍可被发现； | AC-02 | ✅ 一致 |
| 3 | 息屏 8 小时后仍可连接； | AC-03 | ✅ 一致（**DEC-32** 已把全部 AC 的判据改为"可连接"，与本项原文的措辞对齐） |
| 4 | 空闲 24 小时后仍可连接； | AC-04 | ✅ 一致（同 DEC-32） |
| 5 | 充电状态下连续运行 72 小时； | AC-05 | ✅ 一致（原文未写判定口径，本文件按 DEC-26 的参数补足） |
| 6 | App 从最近任务划掉后验证； | AC-06 | ✅ 一致 |
| 7 | Doze 状态验证； | AC-07 | ✅ 一致 |
| 8 | Wi-Fi 断开再连接后验证自动恢复监听； | AC-08 | ✅ 一致 |
| 9 | 路由器重启后验证自动恢复； | AC-09 | ✅ 一致 |
| 10 | 设备重启后验证自动恢复； | AC-10 | ✅ 一致 |
| 11 | 服务异常退出后验证恢复能力。 | AC-11 | ✅ 一致 |

> **记数更正（v0.9）**：原始验收标准实为 **11 项**，与 AC-01…AC-11 **逐条同序一一对应**。v0.4 曾记为"10 项"并由此推出"原始 10 项 → 现有 11 条 AC，数量不符"——该疑问**系本文件自身的记数错误，现予更正**（第一轮复核 R-01、第二轮 V-02 的疑问到此关闭）。
> `AC-12`（由 REQ-008 推导）与 `AC-13`（由 REQ-006 推导）仍为**推导项**，已在 §14.4 标注，**不属**原始 11 项 —— 因此本组同样**不存在未标注的扩写**。
> 第 3、4 项原文写"仍可**连接**"，而当时判据是"可**被发现**" —— 这一强弱差异**已按 DEC-32 统一为"可连接"**，现已完全一致；不是漏项或重复。
> 任务级自动化验收（§14.2）**不属**原始验收标准，是本期新增的工程要求，已单独说明来源。

---

## 3. 现状（只读复核事实，2026-09-23）

以下为**已核实的事实**，是本需求要改变或复用的基线。未在此列出的行为不视为已知。

### 3.1 Android 端现状

| 事实 | 位置 | 对本需求的意义 |
|---|---|---|
| 已存在一个前台服务 `com.pravera.flutter_foreground_task.service.ForegroundService`，`android:foregroundServiceType="dataSync"` | `app/android/app/src/main/AndroidManifest.xml:93-96` | 已有载体，但类型为 `dataSync`，与"全天候常驻"冲突（实现要求 3 明确要求改按 `connectedDevice` 设计；平台依据见 §3.4） |
| 已声明 `FOREGROUND_SERVICE_DATA_SYNC` | 同上 `:23` | 改为 `connectedDevice` 需相应调整权限声明 |
| **无** `RECEIVE_BOOT_COMPLETED` 权限、**无**任何开机广播接收者 | 全仓 `grep BOOT_COMPLETED / RECEIVE_BOOT` 无命中 | REQ-006 从零开始 |
| **无**电池优化白名单相关权限（如 `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`）与引导 | 同上 | REQ-009 从零开始；可用性依据 §16.1 DEC-06 |
| `compileSdkVersion 36`、`targetSdkVersion 37`、`minSdkVersion flutter.minSdkVersion`（**未显式声明**） | `app/android/app/build.gradle:27,47,48` | targetSdk 已高于 Android 16 ⇒ 新的前台服务与 Doze 约束全部生效（§3.4）；minSdk 的处理见 §16.1 DEC-04 |
| `applicationId "org.localsend.localsend_app"`；已有 `QuickTileService`（快捷设置磁贴，仅用于启动 App，跑在 `:quick_tile_service` 独立进程）；`MainActivity` 为 `singleTask`，已有 `ACCESS_LOCAL_NETWORK` 运行时权限（API 37 起必需） | 同上 `:44`；`AndroidManifest.xml:78-89`；`MainActivity.kt` | 磁贴**不**保活，本需求不复用它；`ACCESS_LOCAL_NETWORK` 是"可被发现"的前置权限 |
| 既有多媒体/存储类权限（`READ_MEDIA_*`、`QUERY_ALL_PACKAGES`、`REQUEST_INSTALL_PACKAGES` 等） | `AndroidManifest.xml:5-19` | 本期不动（REQ-011） |

### 3.2 Dart / 服务层现状

| 事实 | 位置 | 对本需求的意义 |
|---|---|---|
| 前台服务对 Dart 侧的封装 `ForegroundService` 是**传输期专用**：由传输启动、无传输时停止；注释明确"必须在 App 位于前台时启动，Android 12+ 拒绝从后台启动前台服务" | `packages/localsend_isolates/lib/util/foreground_service.dart:37-87` | REQ-002 需要把"空闲期也常驻"这一新语义叠加进来，改动落点在此文件（已获授权，§16.1 DEC-01） |
| 该封装使用固定 `_serviceId = 1`、固定通知渠道 `localsend_foreground_service`，`autoRunOnBoot: false`、`autoRunOnMyPackageReplaced: false`、`allowWakeLock: true`、`allowWifiLock: true` | 同上 `:8-9,164-171` | 唤醒锁与 WiFi 锁**已开启**（对 REQ-004/REQ-010 有利）；开机自启被显式关闭；常驻服务将**另建渠道/服务**（§16.1 DEC-02） |
| 通知由 `TransferNotification` **引用计数**驱动：一个通知服务多路并发传输，首个传输启动服务、最后一个传输结束服务，通知展示各传输进度之和 | `packages/localsend_isolates/lib/util/transfer_notification.dart:1-45` | 与 REQ-003 的"永久通知"的冲突已按下述决策解决：**另建渠道/服务**，两者互不干扰（§16.1 DEC-02） |
| HTTP 服务端在 App 启动引导时启动一次（`startServerFromSettings()`），其生命周期与 App 进程一致 | `app/lib/config/init.dart:208`；`app/lib/provider/network/server/server_provider.dart:102` | 进程死则服务端死 ⇒ REQ-002 的常驻对象正是"承载服务端的进程" |
| 生命周期回调只在 `resumed` 时重建 socket（`ensureRunning()`），`detached` 时释放 isolates；**没有**针对 `paused`/后台的网络恢复路径 | `app/lib/main.dart:57-80` | 后台/息屏场景下的网络中断恢复（REQ-010）需要新的触发路径 |
| App 依赖 `wakelock_plus`（用户可保持屏幕常亮）；**不**依赖 `permission_handler` 等权限引导库 | `app/pubspec.yaml:68` | REQ-009 的引导跳转落地方式见 §16.2 Q-13；新增依赖已获授权（DEC-01） |
| 设置持久化已在用 `shared_preferences` | `app/pubspec.yaml:59` | REQ-001/REQ-006 的开关持久化无需新增依赖 |

### 3.3 复核结论

- 现状**已有一半零件**（前台服务封装、唤醒锁、WiFi 锁、快捷磁贴），但全部服务于"传输进行中"这一短时场景。
- 用户 5 条原始想法中，**第 1 条（开机自启）与第 3 条（常驻保活）在现状中完全不存在**；第 2 条（锁屏后台运行）仅在传输进行中被满足。
- 本需求不是从零新建，而是**给现有传输期前台服务旁新增一条空闲期常驻链路**（另建服务/渠道，DEC-02）并新增开机自启链路。这决定了 REQ-011（不回归）是本期最大风险面。

### 3.4 平台约束核对（2026-09-23，来源见 §18）

以下为**官方文档核对结论**，是 REQ-002 选择 `connectedDevice` 的直接依据；**设计阶段仍须在真机上复核**。

| 编号 | 结论 | 依据 |
|---|---|---|
| P-01 | `targetSdk ≥ 35` 时，**禁止**从 `BOOT_COMPLETED` 广播启动 `dataSync` / `camera` / `mediaPlayback` / `phoneCall` / `microphone` 类型的前台服务（否则抛 `ForegroundServiceStartNotAllowedException`）。**`connectedDevice` 不在受限名单内。** | FGS 变更文档 |
| P-02 | `dataSync` 自 Android 15 起有累计运行时长上限（约 6 小时 / 24 小时），`mediaProcessing` 同有 6 小时限制；**`connectedDevice` 未见运行时长上限**。 | FGS 类型文档、Android 15 FGS 类型变更 |
| P-03 | `connectedDevice` 的前提条件 = 声明 `FOREGROUND_SERVICE_CONNECTED_DEVICE`（+ 基础 `FOREGROUND_SERVICE`），**且至少满足其一**：声明 `CHANGE_NETWORK_STATE` / `CHANGE_WIFI_STATE` / `CHANGE_WIFI_MULTICAST_STATE` / `NFC` / `TRANSMIT_IR` 之一；或取得 `BLUETOOTH_CONNECT`/`BLUETOOTH_SCAN`/`BLUETOOTH_ADVERTISE`/`UWB_RANGING` 运行时权限；或取得 USB 设备访问。 | FGS 类型文档 |
| P-04 | P-03 的第一组权限（含 `CHANGE_WIFI_MULTICAST_STATE`）均为**安装时授予的普通权限，不产生运行时弹窗**。 | 同上 |
| P-05 | API 34 起必须为每个前台服务声明类型；在 Play Console 申报类型的要求仅适用于上架分发 —— 本需求为**私有侧载**，无需申报（§16.1 DEC-06）。 | 同上 |
| P-06 | Android 16（API 36）起，**由前台服务发起的后台作业**（`JobScheduler` / WorkManager / `DownloadManager`）须遵守运行时配额限制；`user-initiated data transfer` 作业不受该配额限制。**与 `targetSdk` 无关，仅取决于运行版本。** | FGS 变更文档（§18 第 1 条） |

**结论**：用户"优先按 `connectedDevice` 设计，不使用 `dataSync` 作为全天候保活服务"的选择（实现要求 3）在平台上**成立且必要** —— 它同时解开了 REQ-006（开机自启在 `targetSdk 37` 下唯一可行的一组类型）与 REQ-002（无运行时长上限）两个阻塞。

---

## 4. 核心场景

| 编号 | 场景 | 触发 | 期望结果 |
|---|---|---|---|
| S-01 | 首次安装即为常驻态 | 用户首次安装/首次启动（两个开关默认开启，DEC-10） | 引导用户完成设置；常驻服务启动、常驻通知可见；关闭应用后本机仍可被发现 |
| S-02 | 息屏后随时接收 | 设备息屏数小时，另一台设备发起传输 | 对方在任意时刻都能发现本机；用户无需先点亮屏幕或打开应用 |
| S-03 | 划掉最近任务后仍在线 | 用户从最近任务列表划掉 LocalSend | 本机仍可被发现（尽力而为，见 REQ-005） |
| S-04 | 设备重启后自动恢复 | 设备重启完成，用户未打开应用 | 本机自动恢复可被发现（需「开机自启」开启，REQ-006） |
| S-05 | 网络环境变化后自愈 | Wi-Fi 断开重连 / 路由器重启 / 切换网络 | 无需用户操作，本机重新可被发现 |
| S-06 | 服务被系统回收后自愈 | 系统内存压力杀掉进程 / 服务异常退出 | 在 Android 允许的机制下恢复常驻 |
| S-07 | 用户主动关闭后台接收 | 用户在设置页或常驻通知中关闭 | 常驻服务与通知停止、功耗回归基线；**不在前台时**本机不再可被发现/连接（**前台打开着时接收沿用现状**，DEC-33） |
| S-08 | 常驻状态下正常传输 | 常驻状态下收到传输 | 传输流程与现状一致（REQ-011）；常驻通知与传输进度通知各自独立、互不破坏（DEC-02） |

---

## 5. 本期范围

### 5.1 做什么

1. 「后台接收」开关，**默认开启**（REQ-001，DEC-10）。
2. 长期前台服务承载空闲期的设备发现与连接监听，**按 `connectedDevice` 设计**（REQ-002，§3.4）。
3. 后台接收开启期间的常驻通知，**使用独立渠道/服务**（REQ-003，DEC-02）。
4. 后台/返回桌面/锁屏后持续运行（REQ-004）。
5. 从最近任务划掉后的尽力保持（REQ-005）。
6. **独立的「开机自启」开关**与设备重启后自动恢复（REQ-006，DEC-10）。
7. 异常退出后的恢复（REQ-007）。
8. 关闭开关时彻底停止（REQ-008）。
9. 一次性引导流程（电池优化、后台不受限、国产 ROM 自启）（REQ-009）。
10. 网络变化后的自动恢复监听（REQ-010）。
11. 既有功能不回归的约束（REQ-011）。
12. §13 的非功能要求。

### 5.2 明确不做

| 不做的事 | 理由 |
|---|---|
| 修改 iOS / macOS / Windows / Linux / Web 的任何行为 | 原始需求第 5 条；`ForegroundService` 现有实现已按平台隔离（`_isSupported`） |
| 修改 `server/`（WebRTC 信令服务）与 `cli/` | 与本期目标无关；改动会扩大与上游差异 |
| 修改 `packages/core`（Rust 协议层） | 保活是宿主进程问题，不涉及协议、加密或 HTTP 语义（已确认的授权范围仅为 `packages/localsend_isolates`，DEC-01） |
| 1 像素 Activity 保活 | 实现要求 9；NFR-001 明确禁止 |
| 无限循环 Alarm / 高频 WorkManager | 实现要求 9；NFR-001 明确禁止 |
| 多进程互相守护、循环唤醒等对抗式保活 | 实现要求 9 的精神；与"可靠"目标相反（耗电与不可预期性） |
| 修改 `applicationId` / 包名 / 图标 / 应用名 | `AGENTS.md` §8 要求单独授权；本期无此需要 |
| 修改签名配置、发布签名流程 | 同上 |
| **改变 minSdk 或任何 SDK 级别** | 见 §16.1 DEC-04：本期保持现状；如需变更须另行授权 |
| 新增任何外部上报、统计、第三方服务 | NFR-008 |
| 移除任何现有功能（含 FOSS 剥离标记相关代码） | `AGENTS.md` §10 |
| 常驻通知上的"暂停接收"按钮 | §16.2 Q-14 只把它列为候选。**未确认的新需求不进入本期**（`AGENTS.md` §10）；Q-14 因此默认限定为"本期只有停止按钮"。若你要新增"暂停"，需先确认这是新需求而非既有条目的实现细节，再回到本节撤销本行 |
| AI 相关功能 | 本期无 AI 功能，故"质量评估 / 成本延迟 / 失败兜底"要求在本文件中不适用（§12） |

---

## 6. 优先级

| 优先级 | 条目 | 说明 |
|---|---|---|
| P0（本期必须） | REQ-001、REQ-002、REQ-003、REQ-004、REQ-008、REQ-011、NFR-001、NFR-006 | 没有它们需求不成立：开关、常驻主体、可见性、后台持续、可控关闭、不回归 |
| P0（本期必须，但依赖系统行为） | REQ-009 | 引导是国产 ROM 与 Doze 场景下唯一可行的用户侧手段 |
| P1（本期应有） | REQ-005、REQ-006、REQ-007、REQ-010 | "高可靠长期常驻"的核心自愈能力；受系统规则限制，验收按 §14 的判定口径 |
| P2（可观测即可，可后续迭代） | NFR-002、NFR-003、NFR-007 的量化部分 | 已确认**不设阈值**（DEC-07），本期只保证可观测 |

> 优先级不代表"可省略"：P0 任一条不满足即视为本期未完成（`AGENTS.md` §9：无证据的需求视为未完成）。

### 6.1 三套门槛（v0.5 新增，依据复核 R-05）

原文只明确了"P0 任一条不满足即本期未完成"，但没有区分**任务完成 / 本期完成 / 发布放行**这三件不同的事，也没有说明 P1 失败与"已知限制"（BR-04）各自阻塞什么。三者定义如下：

| 门槛 | 定义 | 规则（**已落定**） |
|---|---|---|
| ① 任务完成 | 单个开发任务的 L1 自动化验收（§14.2）通过 | L1 未通过即任务未完成（§14.1、DEC-16） |
| ② 本期完成 | 本期范围（§5.1）内的条目全部达成 | **P0 任一不满足即本期未完成；P1 失败不阻塞本期**，但每条 P1 必须带证据如实记录其达成状态（**DEC-20**） |
| ③ 发布放行 | L2 真机验收通过（§14.4），且无未处理缺陷 | 发布前一次性人工验收（DEC-16）；**ROM / 系统导致的"已知限制"不阻塞发布**，但须按 BR-04 记录机型与 ROM 并如实标注"未达成"；**其余缺陷由发布前人工判定**（**DEC-20**） |

> 三者相互独立：一个任务可以在 P1 上失败而仍算"任务完成"；本期可以带着 P1 的失败与已知限制结束；**发布**则要求"已知限制以外"没有未处理缺陷。
> 无论用哪一套门槛，结论都必须写清门槛编号，并且**未达成就写未达成** —— 不得用"系统行为"或"本期不带此项"把未达成表述为通过（§14.5）。

---

## 7. 功能需求

格式：**触发/输入 → 处理规则 → 可观察结果**，并给出正常、失败、边界三类场景。
文中 `DEC-xx` 指 §16.1 已确认决策，`A-xx` 指 §15 剩余假设，`Q-xx` 指 §16.2 开放项。

### REQ-001 「后台接收」开关

- **触发/输入**：用户在设置页操作「后台接收」开关；或应用首次安装/首次启动读取默认值。
- **处理规则**：**默认值为开启**（DEC-10；**覆盖升级时缺失该键同样按"开启"处理**，DEC-23）。开关状态**持久化**，在 App 重启、设备重启后保持 —— **持久化是需求；具体存储方式是设计选择**（v0.6 修订，依据复核 V-10：正文不再指定 `shared_preferences`，见 §10.1；键的授权见 DEC-27）；开启 → 进入 REQ-002 启动流程并按 REQ-003 展示通知；关闭 → 执行 REQ-008 的彻底停止流程。开关操作幂等（重复开启/关闭不产生副作用）。
- **可观察结果（v1.3 按 DEC-33 改写，复核 01）**：设置页存在一个可切换的「后台接收」项及其当前状态；开启后下拉通知栏可见常驻通知，且另一台设备在任意时刻可发现并连接到本机；**关闭后常驻通知消失** —— 其中**不在前台时**本机不可被连接/发现，**应用在前台打开着时接收能力沿用现状仍可用**（**DEC-33** + **DEC-36**：前台关闭时**发现与连接都沿用现状**）。
- **正常**：开启 → 通知出现 → 另一台设备可发现并连接；关闭 → 通知消失（关闭的两种情形见上）。
- **失败**：常驻服务启动被系统拒绝（权限未授予、Android 12+ 后台启动限制等）→ **开关回滚为"关闭"并提示原因**（DEC-03），提示中给出引导入口（REQ-009）。不允许出现"开关为开但服务未运行"的状态。
- **边界**：
  - **首次启动即为开启**：需要在该次启动中请求通知权限并展示引导（S-01）；通知权限被拒的表现见 REQ-003 失败分支。
  - 引导未完成时开启：允许开启，并提示"可能无法长期生效"。
  - 在传输进行中关闭：**不在前台时**进行中的传输会被中断（**DEC-18**，已按 DEC-33 收窄）；**在前台**关闭不会中断。中断后的行为沿用现状的中断语义（见 REQ-008、E-12、BR-10）。
    > **v1.1 更正（复核 W-04）**：本行原写"仅关闭常驻语义，进行中的传输按现状语义继续（DEC-02 使两者互不依赖）"，与 DEC-18 的"立即停止、传输中断"**正好相反**，实现者按不同章节会做出相反行为 —— 已按已确认的 DEC-18 统一；**v1.2 再按 DEC-33 收窄为"不在前台时"**。两次都是修正矛盾，未重新取舍。
  - 在应用**前台**时关闭：开关只停"后台常驻"（前台服务、常驻通知、唤醒锁、恢复注册），**HTTP 服务端与前台接收沿用现状继续可用**（**DEC-33**）；退到后台时该监听随之停止。

### REQ-002 常驻前台服务（后台接收主体）

- **触发/输入**：REQ-001 开启；或设备重启后的恢复（REQ-006）；或服务退出后的恢复（REQ-007）。
- **处理规则**：
  1. 以**长期前台服务**作为后台常驻主体，承载**空闲期**的局域网设备发现与连接监听（即 HTTP 服务端 + 发现机制的宿主进程）。
     > **注意（v1.2，DEC-33）**：HTTP 服务端的**实际**生命周期**不单独由本服务决定** —— 应用在**前台**时服务端不依赖本开关（沿用现状），停止监听只发生在"不在前台 且 开关关闭"时。见 REQ-008。
  2. 前台服务类型按 **`connectedDevice`** 设计，**不**将 `dataSync` 用作全天候保活服务（实现要求 3）。平台依据与前提条件见 §3.4 P-01…P-04：需声明 `FOREGROUND_SERVICE_CONNECTED_DEVICE`，并以 `CHANGE_WIFI_MULTICAST_STATE` 等**普通权限**满足前置条件（不产生额外运行时弹窗）。
  3. 与传输期的前台服务**完全分离**：常驻服务是**独立的服务实例**、**独立的通知渠道**，与传输期服务互不影响（DEC-02）。
  4. **传输进行期间，常驻服务不停止、不重启、不降级**：传输会话由现状的传输期服务承载，常驻服务不承载传输，因此在传输开始/结束时都不发生启停切换，监听不中断（S-08、E-16）。原文"空闲期与传输期共用同一常驻主体"表达的就是这一点，**不**表示传输期服务被并入常驻服务（v0.5 按复核 R-02 改写，消除歧义）。
  5. 保持 WiFi 锁与唤醒锁（现状已开启）。
- **资源归属（v0.5 新增，依据复核 R-02；事实来源 §3.2 + DEC-02）**：

| 资源 | 归属 | 传输进行时 | 关闭「后台接收」时（REQ-008） |
|---|---|---|---|
| 常驻前台服务实例（`connectedDevice`） | **本需求新增** | 继续运行 | 停止 |
| 常驻通知及其渠道 | **本需求新增** | 继续存在 | 移除 |
| 传输期前台服务实例（现有 `dataSync`） | 现状，不改 | 由传输驱动 | **不**受影响 |
| 传输通知及其渠道 | 现状，不改 | 由传输驱动 | **不**受影响 |
| HTTP 服务端 + 发现机制（组播） | 现状为**进程级单实例**（`startServerFromSettings()` 随进程，§3.2） | 被传输使用 | **不在前台时停止；在前台时沿用现状继续可用**（**DEC-33**） |
| 唤醒锁 / WiFi 锁 | 现状已开启 | 不变 | 释放 |

- **原冲突的结论（v0.5 提出、v1.3 按 DEC-18 + DEC-33 改写）**：原冲突是"关闭要求停监听"与"进行中的传输不受影响"在**进程级单实例**前提下不可兼得。**已定**：关闭**只停"后台常驻"** —— **应用不在前台**时停止监听并中断进行中的传输（REQ-011 的显式例外，见 BR-10、E-12）；**应用在前台**时关闭**不停监听、不中断传输**（**DEC-33**）。因此上表"关闭时"一列**按"是否在前台"分两种读法**，详见 REQ-008。
- **前台关闭时的发现与连接（v1.5 已定，**DEC-36**）**：应用在**前台**时，关闭开关**只停"后台常驻"** —— **发现（组播公告）与连接都沿用现状保持可用**（与 DEC-33 一致）。因此"前台打开着仍可被扫描发现与连接"**是预期行为**；只有"**不在前台时**仍在监听"才算残留（BR-06）。
- **可观察结果**：无任何传输任务时，进程保持存活、常驻通知存在、HTTP 服务端与发现机制在监听，另一台设备在任意时刻可发现本机。
- **正常**：开启后空闲 30 分钟，另一台设备仍能发现本机。
- **失败**：系统拒绝启动前台服务 → 按 REQ-001 的失败路径回滚并记录日志（NFR-007）。
- **边界**：同一时刻只有一次常驻服务实例（不得重复常驻）；`dataSync` 的运行时长上限**不**适用于本服务（§3.4 P-02）；若 `connectedDevice` 前置条件在目标真机上未被接受（§3.4 的复核项），须在设计阶段回退到"其他允许的类型或路径"。

### REQ-003 后台接收期间的常驻通知

- **触发/输入**：REQ-002 的服务在运行。
- **处理规则**：后台接收开启期间**允许**通知栏显示永久通知（实现要求 2）；使用**独立于传输通知的渠道与服务**（DEC-02）；通知内容表明"后台接收中"这一状态；通知提供**停止**入口（点按即执行 REQ-008）。
- **用户可用的停止途径（v0.5 修订，依据复核 R-06 与官方文档核对）**：共**三**条 —— 原先"唯一途径是开关或通知内的停止操作"的表述与平台事实不符，已删除：
  1. 设置页关闭「后台接收」开关（REQ-001 → REQ-008）；
  2. 常驻通知内的停止操作（REQ-008）；
  3. **Android 13+ 系统"活动应用 / Task Manager"中的 Stop**：系统会**终止整个应用进程、移除常驻通知，且不向应用发送任何回调**；应用只能在下次启动时通过 `ApplicationExitInfo.REASON_USER_REQUESTED` 得知这是用户主动停止。**已定（DEC-19）：尊重用户停止，不自动恢复** —— 在用户下次**主动打开应用**之前保持停止，期间不通过任何后台机制恢复。**已定（DEC-30）：用户下次主动打开应用时即恢复** —— 应用启动时按开关的持久化状态启动常驻服务与监听。即：停止在"应用未运行"期间被尊重；打开应用即为用户重新表达意图，开关显示为"开"且确为运行中。
- **通知可划掉性**：前台服务通知通常不可被用户单独划掉，但**本文件未在目标 API 级别实测确认**，因此不再把"不可划掉"作为需求前提。替代约束（与平台行为无关）：**无论该通知是否可见或被用户清除，常驻服务与设置页状态都不得因此改变**（不得因通知消失而停止服务或把开关显示为关，BR-05、NFR-005）。
- **可观察结果**：通知栏存在一条**独立于传输通知**的常驻通知；点其"停止"后服务停止且通知消失；传输进行时两条通知同时存在且各自正确。
- **正常**：开启 → 常驻通知出现且长期存在；点停止 → 通知消失。
- **失败**：Android 13+ 通知权限被拒 → 前台服务仍可运行但常驻通知不可见；此时设置页状态仍须准确（不得因为"看不到通知"而误判为未开启），并提示授予通知权限（NFR-005）。
- **边界**：与传输通知的渠道命名/ID 需在设计中避免冲突；用户单独关闭常驻通知的渠道时的表现（应提示"通知已关闭，后台接收仍运行"，文案待定，Q-14）；通知文案的语言与翻译来源（Q-06）。

### REQ-004 后台、返回桌面、锁屏后持续运行

- **触发/输入**：应用进入后台 / 用户按 Home 返回桌面 / 设备锁屏或息屏。
- **处理规则**：服务继续运行；设备发现与连接监听**不因**上述状态变化而停止或降级（实现要求 4）。息屏后网络链路的保持依赖 WiFi 锁（现状已开启）；若系统仍断链，由 REQ-010 的恢复路径接管。
- **例外（DEC-19 / DEC-30）**：用户在系统"活动应用"中**主动停止**应用时，本条"持续运行"的承诺中止（见 REQ-003 途径 3）；**该次停止一直有效，直到用户再次主动打开应用**（打开即恢复，DEC-30）。除此之外，本条的"持续运行"不设其他例外。
- **可观察结果**：应用处于后台、用户返回桌面、设备锁屏后（含息屏），另一台设备在任意时刻均可发现本机（对应 AC-01/AC-02/AC-03/AC-04）。
- **正常**：息屏 1 小时后另一台设备可发现本机。
- **失败**：系统进入深度 Doze 导致链路不可达 → 属系统行为，按 §14 的判定口径记录；不得为规避而使用 NFR-001 禁止的手段。
- **边界**：Doze 维护窗口期间的可达性（AC-07）；用户启用系统的"省电模式/超级省电"（可能强制停用后台，需引导说明）；多任务界面停留但不交互。

### REQ-005 从最近任务列表划掉后保持

- **触发/输入**：用户在最近任务列表划掉 LocalSend（实现要求 5）。
- **处理规则**：应用**不主动**在任务移除时停止常驻服务（即不随任务移除而停止服务）；若系统/ROM 强制终止进程，则交由 REQ-007 的恢复机制处理，不采用对抗手段（§5.2）。
- **可观察结果**：划掉后本机**尽量**仍可被发现；判定为"尽力而为"，允许在部分 ROM 上失败并记为已知限制。
- **正常**：划掉后 1 分钟内另一台设备仍可发现本机。
- **失败**：划掉后服务被系统终止且恢复机制未能恢复 → 记录机型与 ROM，计入已知限制（按 §14.5 的口径记录，不作为通过）。
- **边界**：连续多次划掉；在传输进行中划掉（进行中的传输是否中断由系统行为决定，本需求不承诺不影响传输结果）。

### REQ-006 独立的「开机自启」开关与设备重启后自动恢复

- **触发/输入**：用户在设置页操作「开机自启」开关（**独立于 REQ-001 的开关**，DEC-10）；或设备重启完成。
- **处理规则**：
  1. 「开机自启」是一个**独立的用户开关**，状态持久化（存储方式是设计选择，见 §10.1），**默认值为开启**（DEC-14；覆盖升级时缺失该键同样按"开启"，DEC-23）。
  2. 「开机自启」开启**且**「后台接收」开启时：设备重启后，在 **Android 规则允许的范围内**自动恢复后台接收，使本机在用户**未打开应用**的情况下即恢复可被发现。**例外（DEC-19 / DEC-30）**：若用户在重启前曾在系统"活动应用"中主动停止过应用，则**不**自动恢复；**该"被用户停止"状态跨设备重启保持**，直到用户主动打开应用（打开即恢复，DEC-30）。
  3. 「后台接收」关闭时，「开机自启」不产生任何行为（常驻能力本身不存在，开机恢复无对象）——UI 上须说明该依赖关系，避免开关呈现"开着但无用"的误导（BR-05）。
  4. 恢复路径需使用系统允许的机制；平台依据见 §3.4 P-01（`connectedDevice` 允许从 `BOOT_COMPLETED` 启动）。
  5. **恢复时刻（v1.4 重写：DEC-37 选项 ② —— 要求解锁前也能恢复）**：设备重启后，**在用户解锁前**即应恢复"**可被发现 + 可连接**"（在 Android 规则允许的范围内）。为此需要（原假设 A-15 已被本决策取代）：
     - **存储**：两个开关的状态须在**解锁前可读** ⇒ 改为 / 镜像到**设备加密存储**（`createDeviceProtectedStorageContext()`）—— 见 §10.1（**候选方案与 A-14 随之改变**；DEC-27 的键授权覆盖之）。
     - **清单**：新增 `android:directBootAware="true"` 的接收器监听 `LOCKED_BOOT_COMPLETED`；`ACTION_USER_UNLOCKED` 用于解锁后的收尾（例如把设备加密存储中的状态与凭据加密存储对齐）。
     - **解锁前的能力边界（须由设计阶段核实，本文件不预设结论）**：解锁前能否启动前台服务、HTTP 服务端与 TLS 证书是否可用、下载目录是否可写、局域网权限是否可用。**若核实结果是不允许，须回到本文件修订，不得静默降级为"解锁后恢复"。**
     - **验收限制**：该场景**无法在 L1 验证**（API 36 模拟器不支持 Direct Boot 模拟），须记为 **L2 真机**或"未验证 + 原因"（§14.3）。
     > **来源说明**：这是对原始需求第 7 条的**加强（新增需求）**。原文只写"在 Android 规则允许范围内自动恢复"，本决策把"允许范围"明确要求到**解锁前**。
  6. **覆盖安装（应用升级）后的行为（v0.5 新增，依据复核 R-16）**：现状 `autoRunOnMyPackageReplaced: false`（§3.2），系统不会在覆盖安装后重建前台服务；本期是否要求此时也恢复（由 REQ-007 的恢复路径接管，或明确不做）**未确认**，记为**假设 A-16**，暂列后续。
- **可观察结果（v1.5 按 DEC-37 改写，复核 02）**：设备重启后、**在用户解锁前**，无需用户打开应用，另一台设备即可**发现并连接**到本机（AC-10；采样起点与观察窗口见 §14.5）。**解锁前的能力边界以设计阶段的核实结果为准**（第 5 条）。
- **正常**：两开关均开启时，重启后自动恢复。
- **失败**：ROM 拦截自启（国产 ROM 常见）→ 引导用户开启自启动权限（REQ-009）；失败时记录日志（NFR-007）。
- **边界**：用户在开机过程中关闭「后台接收」（以用户意图为准，不恢复，BR-01）；仅在「开机自启」关闭时重启（不恢复，但用户下次打开应用后常驻能力仍正常）；Android 不允许从该广播启动相应类型前台服务的情形（§3.4 P-01）；快速连续重启；**重启后尚未解锁在承诺范围内**（第 5 条、**DEC-37** —— 要求解锁前即可被发现且可连接；其能力边界与验收限制见该条）；**覆盖安装应用**后的恢复（第 6 条、A-16）。

### REQ-007 服务异常退出后的恢复

- **触发/输入**：服务因异常或系统资源压力退出（实现要求 8）。
- **处理规则**：使用 **Android 允许的机制**进行恢复（§5.2 排除项之外）；恢复尝试需可观测、有界，不产生无限唤醒（NFR-001）。
- **可观察结果**：人为模拟服务退出后，本机重新可被发现（AC-11）。模拟手段见 §14.3：**不得**使用 `am force-stop`（它会让应用进入 stopped state，必然产生"无法恢复"的假失败）。
- **判定闭合（v0.5 新增，依据复核 R-04）**：DEC-07 确认的是"**不设性能阈值**"，但验收必须能终止 —— 若只写"不设时限"，则"一直未恢复"也永远不必判失败。因此本文件继续不设性能阈值，但**设有观察窗口**：**30 分钟**（**DEC-26**）。窗口内恢复即通过，窗口内未恢复即不通过；实际耗时如实记录。
- **正常**：服务被终止后自动恢复。
- **失败**：恢复机制被 ROM 完全禁止 → 记录为已知限制并引导用户（REQ-009）。
- **边界**：短时间内反复被杀（应避免恢复风暴）；设备处于深度 Doze（恢复可能被推迟，验收记录实际耗时即可，不作门槛）。

### REQ-008 关闭「后台接收」时彻底停止

- **触发/输入**：用户在设置页关闭开关，或在常驻通知中点"停止"（实现要求 10）。
- **处理规则（v1.2 按 DEC-33 重写）**：关闭分两种情形，**必须按"应用是否在前台"区分**：
  1. **应用不在前台**：停止常驻前台服务、移除常驻通知、**停止发现与连接监听**（HTTP 服务端）、释放唤醒锁/WiFi 锁、取消为保活注册的恢复机制；不残留任何保活组件。
  2. **应用在前台**：停止常驻前台服务与常驻通知、释放锁与恢复注册，但**保留 HTTP 服务端与接收能力** —— 前台接收沿用现状，**不因本开关而改变**（DEC-33）。
  两种情形都保留开关偏好（下次开启可直接使用）。
- **"彻底停止"的准确含义（AC-12 的判据）**：指**保活相关组件全部停止**（前台服务、常驻通知、唤醒锁/WiFi 锁、恢复注册），且**"不在前台时"监听停止**；**不**表示"应用在前台打开着也无法接收"。
- **进行中的传输（v1.2 收窄 DEC-18）**：**只在第 1 种情形（不在前台）**下，关闭会立即停止监听并**中断进行中的传输** —— 这是对 REQ-011 的**显式例外**（见 BR-10）。在**前台**关闭**不会**中断传输（监听未停）。两种情形都**不引入**"等传输结束后再停"或"有传输时不可关"的逻辑。
- **可观察结果**：常驻通知消失；**不在前台时**本机不可被连接；无残留保活组件；进程可被系统正常回收（对应 AC-12）。重新开启后功能恢复正常。
- **正常**：关闭后（不在前台时）本机不再可被连接；重新开启后功能恢复正常（可重复开关）。
- **失败**：通知移除失败或服务停止失败 → 记录日志；不允许出现"开关已关但通知仍在"的不一致（如出现须视为缺陷，BR-06）。
- **边界**：在传输进行中关闭 —— 不在前台时会中断（见上），**设计阶段须先核实现状的传输中断语义**（§3 未复核该路径：残留文件如何处理、对端如何提示），实现必须沿用该语义；**前后台切换**（前台关闭 → 退到后台时应停止监听；后台关闭 → 回到前台时应恢复监听）是 **DEC-33 要求的状态机**的一部分，须在设计阶段定义；快速连续开关（E-11、E-18）。

### REQ-009 一次性引导流程

- **触发/输入**：首次启动/首次开启（默认开启，DEC-10）；或检测到引导项未满足时（实现要求 6）。
- **处理规则**：引导用户完成以下三项，并对每项给出**当前状态**与**跳转到系统对应设置页的入口**：
  1. 关闭应用电池优化；
  2. 将应用后台运行设置为"不受限制"；
  3. 国产 ROM：开启自启动权限与后台运行权限。
  **允许跳过（DEC-21）**：引导不阻塞用户；跳过时明确告知"后台接收可能无法长期生效"。
- **可检测性边界（v0.5 新增，依据复核 R-09）**：第 3 项（厂商自启动）在 Android 上没有统一的查询接口，部分 ROM 上的"后台运行不受限"同样**无法可靠查询**。因此：
  - 每项状态的取值扩展为三态：**已满足 / 未满足 / 无法检测（需人工确认）**；"无法检测"是合法且预期的取值，**不得**为凑齐两态而伪造检测结果；
  - **不得**把"用户跳转过设置页"当作"已满足" —— 跳转成功只表示打开过页面；
  - **区分两类状态**：系统实时状态（能查到才查）与"用户已阅读 / 已跳过"（持久化数据）。后者只用于避免重复打扰，**不得**用于判断能力是否具备（对应 §10.1 的引导状态数据）；
  - 对"无法检测"项，允许用户手动标记"我已设置"，界面须标注为**用户自述**而非系统确认（BR-05：不得呈现假象）。默认呈现已定（**DEC-24**）：显示"无法检测（需你自行确认）"+ 用户自述入口；
  - 某项在目标设备上不适用时显示"该设备无需此项"（见下方边界）。
- **可观察结果**：引导页/引导区列出三项及其状态（**已满足 / 未满足 / 无法检测**）；点按可跳转到对应系统设置；用户完成后状态更新为已满足（能查到实时状态时）或标注为用户自述（查不到时）。
- **正常**：三项均可跳转并可在返回后反映状态。
- **失败**：某 ROM 无对应设置页或跳转失败 → 给出该 ROM 的手动操作说明（文案待定）；不崩溃。
- **边界**：用户完成后又手动在系统设置里改回（下次进入引导时应重新显示为未满足）；部分设备不支持某项（应显示"该设备无需此项"而非"未满足"）。

### REQ-010 网络变化后自动恢复监听

- **触发/输入**：Wi-Fi 断开后重连、路由器重启后网络恢复、IP 地址变化、网络切换。
- **处理规则**：无需用户操作，自动恢复设备发现与连接监听；恢复不依赖 App 处于前台（现状的 `ensureRunning()` 仅在 `resumed` 触发，见 §3.2，因此需要新的后台触发路径）。
- **可观察结果**：断开并重连 Wi-Fi（或重启路由器）后，另一台设备在本机应用处于后台的情况下即可重新发现本机（AC-08/AC-09）。
- **正常**：重连后自动恢复。
- **失败**：恢复失败 → 记录日志并按 Android 允许的机制重试（退避，不得超过 NFR-001 的强度约束）。
- **判定闭合与平台约束（v0.5 新增、v0.7 落定）**：判定闭合与 REQ-007 同 —— **30 分钟观察窗口**（DEC-26）。**平台约束补充**：Android 16 起，从常驻前台服务发起的后台作业（`JobScheduler` / WorkManager / `DownloadManager`）须遵守运行时配额（§3.4 P-06）—— 若恢复/重试路径选用其中任一机制，设计阶段须先核对配额。
- **边界**：短时间多次网络抖动（避免恢复风暴，E-11）；从 Wi-Fi 切到移动网络（移动网络下无法被局域网发现，应保持服务运行并在恢复 Wi-Fi 后可达）；多网卡/多接口并存。

### REQ-011 既有功能不回归

- **触发/输入**：本需求的所有改动生效后使用应用（原始需求第 4 条）。
- **处理规则**：不改变既有功能的语义与用户可见行为，包括但不限于：发送/接收全流程、网页分享（web send）、扫描发现、分享入口（share_handler）、接收历史、设置项语义、i18n 语言集与翻译键、FOSS 剥离标记所保护的结构、快捷磁贴行为、传输期前台服务与进度通知（DEC-02 下必须保持原样可用）。
- **可观察结果**：既有的自动化检查（`AGENTS.md` 上半部分列出的 analyze/format/test）、既有测试套件全部通过；手工回归清单全部通过。**这些命令取决于工具链是否就绪**（§14.6；执行已获授权，**DEC-28**）。
- **正常**：全部检查通过。
- **失败**：任一检查失败或既有行为改变 → 本需求未完成。
- **边界**：传输进行中开启/关闭后台接收（两个服务独立，需各自正确）；后台接收开启状态下使用网页分享（两者都要监听端口，端口/证书行为需在设计阶段核实）。

---

## 8. 关键页面、状态与用户操作流程

### 8.1 关键页面

| 页面/区域 | 位置 | 本期变化 | 关键状态 |
|---|---|---|---|
| 设置页 | 现有设置 Tab（`app/lib/pages/tabs/settings_tab.dart`） | 新增**两个**开关（「后台接收」「开机自启」）+ 引导状态入口 | 见 §8.3 |
| 引导页/引导区 | 新建（形态待定，Q-13） | 新增 | 全部满足 / 部分满足（逐项显示） / 已跳过 |
| 常驻通知 | 系统通知栏（独立渠道，DEC-02） | 新增 | 后台接收中 / 不可见（通知权限被拒） |
| 传输进度通知 | 系统通知栏（现有渠道） | **行为不变**（REQ-011、DEC-02） | 与常驻通知共存，不互相覆盖 |

### 8.2 用户操作流程（主流程）

1. 用户安装并首次启动（两个开关默认开启，DEC-10、DEC-14）→ 应用请求通知权限（Android 13+）→ 启动常驻服务 → 展示常驻通知。
2. 应用检查引导三项的状态：
   - 全部满足 → 就地提示"后台接收已生效"。
   - 存在未满足项 → 展示引导（可跳过），提示"后台接收可能无法长期生效"。
3. 用户完成引导（或跳过）→ 设置页两个开关显示为开。
4. 用户离开应用 / 息屏 / 重启设备 → 无需其他操作，本机持续可被发现。
5. 用户停止：在设置页关闭「后台接收」，或在常驻通知中点"停止" → 常驻服务与通知消失。**若此刻应用在前台**，接收能力仍沿用现状可用（只是不再"常驻"），退到后台即停止监听（**DEC-33**）。
6. 用户只想关掉开机自启而保留后台接收：关闭「开机自启」→ 重启后不再自动恢复，本次运行不受影响。

### 8.3 用户可见状态汇总

| 状态 | 用户可见表现 | 出现条件 |
|---|---|---|
| 后台接收·开·生效中 | 开关为开，常驻通知存在 | 服务运行 **且** 监听可用，且引导无"未满足"项（口径见下方注） |
| 后台接收·开·部分未满足 | 开关为开，常驻通知存在，引导区显示未满足项 | 引导有"未满足"项（"无法检测"不计入） |
| 后台接收·关（含启动失败回滚） | 开关为关，无常驻通知；**提示了失败原因与引导入口**（启动失败回滚时） | 用户关闭 / 服务启动被系统拒绝（DEC-03） |
| 后台接收·关·应用在前台 | 开关为关，无常驻通知；**接收能力沿用现状仍可用**（**DEC-33**）——界面上不应表述为"已无法接收" | 用户在前台关闭开关 |
| 后台接收·开·通知不可见 | 开关为开，通知栏无通知 | 通知权限被拒或用户关闭了该渠道 |
| 后台接收·开·无局域网 | 开关为开，常驻通知存在，设置页提示"当前无局域网，暂不可被发现" | 无网络 / 仅移动网络（E-06、REQ-010 边界） |
| 后台接收·开·恢复中 | 开关为开，常驻通知存在；**是否提示"恢复中"待确认** | 网络变化或服务被回收后的恢复期（REQ-007、REQ-010） |
| 后台接收·开·监听不可用 | 开关保持"开"，并显示"**异常 / 暂不可被发现**"（**DEC-22**）；不得显示为"生效中" | 服务已启动，但 HTTP 服务端/组播初始化失败，或局域网权限被拒（§10.3） |
| 后台接收·开·被系统用户停止 | 应用未运行期间**不自动恢复**（DEC-19）；**用户下次打开应用即恢复**，开关显示为"开"且确为运行中（**DEC-30**） | 用户在系统"活动应用"中 Stop（REQ-003 途径 3） |
| 后台接收·启动中 / 停止中 | 过渡态：**呈现方式待设计确认**（v0.6 新增，依据复核 V-14）：不得把"启动中"显示为"已生效"，也不得把"停止中"显示为已停止 | 用户快速切换，或启动/停止尚未完成（E-11、E-18；假设 A-19） |
| 开机自启·开（依赖后台接收） | 开关为开；若后台接收为关，显示依赖说明 | REQ-006.3 |
| 开机自启·关 | 开关为关 | 用户关闭，重启后不恢复 |

> **"生效中"的判定口径（v0.5 新增，依据复核 R-08）**：上表首行的"生效中"**不得**只以"前台服务在运行"为准 —— 必须同时满足"服务运行 **且** 监听可用（HTTP 服务端与发现机制已就绪）"。
> 原文只覆盖了"前台服务启动被拒"这一种失败（DEC-03），**没有**覆盖：服务已启动但监听初始化失败、局域网权限被拒、无局域网、恢复重试中、被系统用户停止。上表已把这些状态补入：**监听不可用**按 DEC-22 呈现（保持"开"+ 显示"异常"）、**被系统用户停止**按 DEC-19 不自动恢复（用户下次打开应用即恢复，DEC-30）；任何状态都**不得**统一显示为"生效中"。

---

## 9. 业务规则

| 编号 | 规则 |
|---|---|
| BR-01 | 用户对**每个开关**的显式选择是最高优先级的意图：用户关闭「后台接收」后，任何自动机制（重启恢复、异常恢复）都不得重新开启；用户关闭「开机自启」后，重启不得自动恢复。 |
| BR-02 | 两个开关的**默认值均为开启**（DEC-10、DEC-14）。这是有意的默认行为改变：首次启动即出现常驻通知并保持局域网在线，属于对用户可见默认行为的变更，因此 REQ-009 的引导必须在首次启动即出现。 |
| BR-03 | 保活手段的合法性优先于保活强度：任何与系统机制正常用法冲突的手段一律不采用（§5.2、NFR-001）。 |
| BR-04 | 系统限制导致的能力上限是**已知限制**而非缺陷，但必须实测记录机型与 ROM（§14.5）。 |
| BR-05 | 开关状态不得呈现假象：以**服务真实运行**为准；启动被拒时回滚开关并说明原因（DEC-03）；「开机自启」在「后台接收」关闭时不产生行为，须在 UI 说明依赖关系。 |
| BR-06 | 关闭必须彻底：不接受"关了开关但通知还在""关了开关但**常驻**服务 / 唤醒锁 / 恢复注册仍在"等残留状态（REQ-008）。**v1.3 更正（复核 01）**：原写"关了开关但仍在监听" —— 按 **DEC-33**，应用在**前台**时服务端与接收**本应**继续可用，那种情形下的"仍在监听"**是预期行为、不算残留**；残留的判据是"**不在前台时**仍在监听"。 |
| BR-07 | 本期不改变任何既有功能的语义与默认值（REQ-011）；常驻能力与传输能力是两条独立链路（DEC-02）。 |
| BR-08 | 不为保活而新增任何外部数据流动（NFR-008）。 |
| BR-09 | 两个开关相互独立，但「开机自启」的效果以「后台接收」开启为前提（REQ-006.3）。 |
| BR-10 | 关闭「后台接收」是**立即生效**的用户动作：**在应用不在前台时**，它会停止监听并**中断进行中的传输**（DEC-18）—— 这是 REQ-011「既有功能不回归」的**唯一例外**，且只作用于"用户主动关闭 **+ 不在前台**"这一条路径（**v1.2 按 DEC-33 收窄**：在**前台**关闭**不构成例外** —— 监听不停、传输不受影响）。且只作用于"用户主动关闭"这一条路径。**v1.1 更正（复核 W-06）**：本条原写"其余**任何情形**都不得中断进行中的传输"，那是**应用无法保证的绝对承诺** —— 系统杀进程、ROM 限制、断网都会中断进行中的传输，REQ-005 / REQ-007 / REQ-010 也已承认这一点。正确表述是：**本次新增的逻辑不得主动中断既有传输**；由外部原因造成的中断，按**现状的中断语义**如实记录（既不宣称"未中断"，也不计入本条的例外）。 |

---

## 10. 数据与权限

### 10.1 新增数据（本地）

| 数据 | 存储 | 用途 | 敏感性 |
|---|---|---|---|
| 「后台接收」开关状态（布尔，默认开） | `shared_preferences` | REQ-001 | 低 |
| 「开机自启」开关状态（布尔，默认开，DEC-14） | `shared_preferences` | REQ-006 | 低 |
| 引导完成状态（逐项，布尔集合或时间戳） | `shared_preferences` | REQ-009（避免重复打扰；须允许用户被系统设置改回后重新提示，见 REQ-009 边界） | 低 |
| 保活诊断信息（最近一次启动/被拒/恢复尝试的结果） | 现有日志体系 | NFR-007 | 低（不得含文件内容、密钥、指纹） |

**不新增**：不上传、不同步、不写外部存储、不采集设备标识用于保活之外的用途。

**已确认约束 vs 候选方案（v0.5 新增、v0.6 更正，依据复核 R-13 与 V-10）**：

- **已确认的约束**：不上传、不同步、不写外部存储、不采集设备标识用于保活之外的用途（NFR-008）；**持久化本身是需求**（REQ-001 / REQ-006 要求开关状态在重启后保持）。
- **候选方案（不是需求，由设计确定）**：上表"存储"一列的 `shared_preferences`、"引导完成状态"的取值形态（"布尔集合或时间戳"）、具体键名与默认值迁移规则。
- **依赖授权（v0.6 更正）**：v0.5 曾写"已确认的只有不新增依赖"，**与 DEC-01 冲突** —— DEC-01 明确**授权新增依赖**。准确表述是：**允许**新增依赖（DEC-01），但**不因保活而无必要地新增**（沿用现状能力属设计取向，不是硬约束）。
- **耦合提醒（V-10 提出、v1.4 反转）**：v0.6 时写的是"REQ-006.5 的'首次解锁后恢复'取决于存储位于凭据加密存储"；**DEC-37 之后方向相反** —— 本期**要求解锁前恢复**，因此存储**必须**解锁前可读（设备加密存储），见下方"解锁前可读性"。设计若回到凭据加密存储，须回到 REQ-006.5 重评（即等于否定 DEC-37）。

具体键名、数据结构与默认值迁移规则由**设计阶段确定**，届时须回到本节核对；`AGENTS.md` §4 的"配置改了同步文档"在此适用。

**三种初始化情形（DEC-23）**：**首次安装 / 覆盖升级 / 清除数据后**，缺少两个开关的键时一律按"**开启**"处理；其中**覆盖升级后的首次启动须给出说明**，解释"为何出现常驻通知与开机自启"（依据 BR-02）。

**授权边界（v0.5 新增、v0.7 落定）**：新增持久化键属于 `AGENTS.md` §8 的"持久化格式变更"范畴，**已获仓库所有者授权**（**DEC-27**，2026-09-23）：允许新增两个开关与引导状态的键及其迁移规则；**不新增依赖**（沿用现状存储能力，与 A-14 的方向一致）。

**解锁前可读性（v1.4 新增，**DEC-37**）**：由于本期**要求解锁前也能恢复**（REQ-006.5），两个开关的状态**必须**在解锁前可读 ⇒ 需存于 / **镜像到设备加密存储**（`createDeviceProtectedStorageContext()`）。这**改变了上面"候选方案"中的存储选择**（原候选为"沿用 `shared_preferences` 现有键空间"），并影响 **A-14**；具体做法（直接改存位置，还是在设备加密存储中镜像一份供直接启动阶段读取，并在解锁后与凭据加密存储对齐）**由设计阶段确定**。引导状态与保活诊断信息**不**要求解锁前可读。

### 10.2 权限

| 权限 | 现状 | 本期 | 用途 / 依据 |
|---|---|---|---|
| `FOREGROUND_SERVICE` | 由 `flutter_foreground_task` 带来 | 复用 | 前台服务基础（§3.4 P-03） |
| `FOREGROUND_SERVICE_CONNECTED_DEVICE` | **无** | **新增** | `connectedDevice` 类型的必需权限（§3.4 P-03） |
| `CHANGE_WIFI_MULTICAST_STATE`（或以 `CHANGE_NETWORK_STATE` / `CHANGE_WIFI_STATE` 替代） | **无** | **候选新增**（其一即可，由设计确定） | 满足 `connectedDevice` 前置条件；均为普通权限、无运行时弹窗（§3.4 P-03/P-04）。`CHANGE_WIFI_MULTICAST_STATE` 与本应用已在做的组播发现语义天然契合 |
| `RECEIVE_BOOT_COMPLETED` + 开机广播接收者 | **无** | **新增** | REQ-006（§3.4 P-01） |
| `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` | **无** | **候选新增**（设计二选一） | REQ-009 引导第 1 项；**仅私有侧载**下可用（DEC-06）。若只跳转设置页而不声明该权限，则不需要它 —— 二选一由设计阶段确定 |
| `POST_NOTIFICATIONS` | 由 `flutter_foreground_task` 带来，代码已请求 | 复用 | Android 13+ 通知可见性 |
| `FOREGROUND_SERVICE_DATA_SYNC` | 已声明 | **保留** | 传输期前台服务仍使用 `dataSync`（DEC-02：传输链路不改）；常驻服务不使用它 |
| `INTERNET`、`ACCESS_LOCAL_NETWORK` | 已有（后者已声明并已运行时请求，API 37+） | 复用 | 局域网通信与"可被发现"的前置 |
| 厂商自启动权限 | 无（不可声明，只能跳转系统设置） | **需要跳转能力** | REQ-009 第 3 项 |

> **权限表的读法（v0.6 新增，依据复核 V-15）**：标 **候选新增**的两行**不是已确定的权限清单** —— 是否声明取决于设计阶段的选择（例如 `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` 到底是声明它，还是只跳转设置页）。设计要求：**给出最小权限集合**，并逐项说明**不声明 / 被拒绝时的行为**（降级表现），不得只列"需要什么"而不写"没有会怎样"。

### 10.3 权限失败的用户可见后果

| 情形 | 后果 | 要求 |
|---|---|---|
| 常驻服务启动被系统拒绝 | 后台接收不生效 | **回滚开关为关 + 提示原因 + 引导入口**（DEC-03） |
| 通知权限被拒 | 常驻通知不可见，服务仍运行 | 不得误报为未开启（§8.3、NFR-005） |
| 电池优化未关闭 | 后台可能被冻结，可达性下降 | 引导中显示为未满足（REQ-009） |
| 自启动未开启 | 重启后不恢复 | 引导中显示为未满足；REQ-006 失败分支 |
| `ACCESS_LOCAL_NETWORK` 被拒（API 37+） | 局域网通信不可用 | 复用现状的权限请求逻辑；失败时明确提示 |
| 服务已启动、但监听不可用（初始化失败 / 局域网权限被拒） | 常驻服务在运行，但本机不可被发现 | **保持开关为"开"并显示"异常 / 暂不可被发现"**（**DEC-22**）；**不得**显示为"生效中"，也**不得**误报为未开启 |

---

## 11. 异常与边界情况

| 编号 | 情况 | 期望行为 |
|---|---|---|
| E-01 | 常驻服务启动被系统拒绝（前台启动限制、权限缺失） | **回滚开关为关 + 提示原因 + 引导入口**（DEC-03、BR-05） |
| E-02 | 进程被系统内存压力回收 | 由 REQ-007 恢复；不影响用户已保存的数据 |
| E-03 | ROM 强制禁止后台/自启 | 尽力而为；记录机型与 ROM 为已知限制（BR-04）；引导用户手动设置 |
| E-04 | 深度 Doze | 可达性可能被延迟；按 §14 判定口径记录实际耗时；不得为规避而使用滥用手段（NFR-001） |
| E-05 | Wi-Fi 断链 / 路由器重启 / IP 变化 | REQ-010 自动恢复；不得需要用户打开应用 |
| E-06 | 移动网络 / 无网络 | 服务保持运行；恢复局域网后自动可达（REQ-010 边界） |
| E-07 | 用户关闭常驻通知的渠道 | 服务仍运行；状态提示与引导（REQ-003 边界） |
| E-08 | 用户开启系统"超级省电"/"省电模式" | 可能强制停用后台；在引导中说明（文案待定） |
| E-09 | 设备时间被修改 / 时区变化 | 不影响保活判断；不引入依赖本机时钟的保活逻辑 |
| E-10 | 用户在多任务界面划掉应用 | REQ-005；尽力而为 |
| E-11 | 快速连续开关 / 反复重启设备 | 幂等、无恢复风暴、无重复服务实例（REQ-001/REQ-006/REQ-007 边界） |
| E-12 | 传输进行中关闭后台接收 | 分两种情形（**DEC-33**）：**不在前台** → **进行中的传输会被中断**（DEC-18，REQ-011 的显式例外，见 BR-10），常驻服务与监听立即停止；**在前台** → 开关只停常驻能力，**监听与进行中的传输都不受影响**。两种情形下的残留文件与对端提示都**沿用现状的中断语义**（设计阶段须先核实，不得新增或改变中断处理路径） |
| E-13 | 磁盘空间不足（接收时） | 属现状行为，本期不改变（REQ-011） |
| E-14 | 应用被卸载后残留计划任务 | 卸载应清理；不得留下系统层面的孤儿任务（NFR-001） |
| E-15 | 多台本 fork 设备同网段同时常驻 | 互不影响，均可被发现（现状的指纹去重机制保持不变） |
| E-16 | 首次启动时用户正在传输 | 默认开启的常驻服务不得干扰传输链路（DEC-02 独立性） |
| E-17 | 设备无 Wi-Fi / 仅以太网（平板、TV） | 行为按现状；常驻不因无 Wi-Fi 而崩溃或反复报错 |
| E-18 | 设置页关闭与通知"停止"同时发生；或在启动尚未完成时又关闭 | **按假设 A-19（未确认）建模**：最后一次明确的用户意图优先 + 操作幂等 —— 晚到的启动结果**不得**覆盖用户最新的关闭意图，不得留下重复服务，也不得写入与用户意图相反的持久化状态。**v1.3 更正（复核 06）**：本行原写成命令式规则，与 A-19 仍是"未确认假设"矛盾；现明确标为"**按假设建模**"，**实现前须由所有者确认 A-19**，否则该状态机需另设计 |

---

## 12. 适用性说明（AI 相关章节）

本期**不涉及任何 AI 功能**，因此"AI 质量评估、成本/延迟边界、失败兜底"三项要求不适用于本文件。若后续引入，需在本文件新增对应章节。

---

## 13. 非功能要求（NFR）

> 仓库所有者已确认**不设量化阈值**（DEC-07）：性能与资源类要求本期只保证**可观测**，不承诺数值。

### 13.1 NFR 分类与判定口径（v0.5 新增，依据复核 R-15）

原文把全部 NFR 并列，但其中一部分**没有通过/失败语义**（不设阈值），因此"全部 NFR 通过"不是有效结论。分类如下：

| 类别 | 条目 | 是否参与质量放行 |
|---|---|---|
| **通过/失败型** | NFR-001、NFR-004、NFR-005、NFR-006、NFR-008、NFR-009、NFR-010 | 是 —— 有明确的检查项与判据 |
| **证据采集型（只观测）** | NFR-002、NFR-003 | **否**（DEC-07：不设阈值）。只要求"能采到数据"，**不得**表述为"通过"或"未通过" |
| **混合** | NFR-007 | 检查项（日志是否写出、是否可导出）通过/失败；耗时类只记录 |

> 由此，完成报告里每个 NFR 的结论必须写成"通过 / 未通过 / 已采集证据（不判定）/ 未验证 + 原因"四者之一。
> 下文每条 NFR 的"可验证方式 / 测量条件"即其**可枚举检查项**；仅含主观形容词（"行为不变""改动集中""能回答失败在哪一步"）的表述已在下文改为可枚举形式。

### NFR-001 保活手段的合法性（禁止滥用系统机制）

- **要求**：实现中不得出现以下任一：1 像素 Activity、无限循环 Alarm、高频（分钟级或更密）WorkManager 轮询、多进程互相守护、伪造前台通知内容的空壳服务。
- **可验证方式**：代码审查 + 静态检索（对新增代码检索上述模式的等价写法），并在设计文档中逐条声明所用机制及其必要性。
- **测量条件**：审查范围为本次改动涉及的全部文件。

### NFR-002 保活期间耗电（只观测）

- **要求**：**不设阈值**（DEC-07）。要求"可观测"：能回答"后台接收开启后，静置 N 小时的耗电是多少"。
- **观测条件**：满电 100%；关闭电池优化；后台接收开启；无任何传输；屏幕关闭；同一路由环境且 Wi-Fi 保持连接；记录开始与结束电量（系统电量统计）与 8 小时 / 24 小时的下降百分比。
- **判定**：本期只记录数据，不作通过/不通过判定；后续若需要阈值，再写入本节。

### NFR-003 常驻期间资源占用（只观测）

- **要求**：**不设阈值**（DEC-07）。要求"可观测"。
- **观测条件**：常驻静置 1 小时后，记录应用进程 PSS 与累计 CPU 时间；与"后台接收关闭"状态下的同一指标对比。
- **判定**：本期只记录数据，不作通过/不通过判定。

### NFR-004 兼容性与配置不变式

- **要求**：
  1. 不改变 `applicationId`、包名、签名配置。
  2. **不改变 minSdk，也不改变任何其余 SDK 级别**（DEC-04）。`minSdkVersion` 保持为 `flutter.minSdkVersion`（现状即未显式声明，本期继续不显式声明）。理由：本需求不需要更高的下限；提高 minSdk 会不可逆地丢弃可安装设备，属于超出需求的改动（`AGENTS.md` §3）。若你日后要求改变 minSdk，需另行授权（`AGENTS.md` §8）。
  3. **验收基线为 Android 16（API 36）**（DEC-04）。在低于 Android 12 的设备上，保活能力按平台可用性自然降级，且不得呈现假象状态（BR-05）：设计阶段需确定"低于某个 API 级别时的呈现方式"（设计输入，非需求变更）。
  4. 前台服务类型与权限声明需与目标 API 级别的平台要求一致（§3.4）。
- **可验证方式**：`build.gradle` 与清单 diff 审查（仅允许出现与保活直接相关的改动）；构建产物可安装且可升级覆盖旧版本。

### NFR-005 通知合规与降级

- **要求**：Android 13+ 通知权限被拒时服务仍可运行；被拒不得导致功能静默失效或状态误报（§8.3）。
- **测量条件**：拒绝通知权限（模拟器可直接 `adb shell pm revoke <pkg> android.permission.POST_NOTIFICATIONS`）后执行首次启动/开启流程，观察设置页状态与可达性。

### NFR-006 平台与功能隔离

- **要求**：仅 Android 行为改变；非 Android 平台构建与行为不变；既有功能语义、默认值与传输期链路不变（REQ-011、BR-07）。
- **可验证方式（v0.5 改为可枚举、v0.6 修正范围，依据复核 R-11/R-15 与 V-11/V-13）**：①既有自动化检查全通过（`fvm dart format --set-exit-if-changed lib test`、`fvm flutter analyze`、`fvm flutter test`）；②改动面静态检查（按下方**允许清单**）；③非 Android 平台构建检查；④手工回归清单（设计阶段产出）全部通过。
- **允许的改动范围（v0.6 新增、v1.3 补全，依据复核 V-11 与 02）**：`app/android/**`、**`app/lib/**`（Dart 侧全部代码，含 `app/lib/gen/**`）**、**`app/test/**`（测试与 mock）**、`packages/localsend_isolates/**`，以及 **NFR-009 强制要求的同步产物** —— `docs/**`、`app/assets/i18n/**`。
  **补全理由（v1.3，复核 02）**：v0.6 的清单**漏了 `app/lib/**`**，而本文件自己把开关状态、服务端生命周期与通知的落点写在 `app/lib/`（§3.2：`app/lib/config/init.dart`、`app/lib/provider/network/server/server_provider.dart`、`app/lib/pages/tabs/settings_tab.dart`）—— 按旧清单，**正常实现会被判越界**。
  **仍然排除**：非 Android 平台的行为代码（`ios/`、`linux/`、`macos/`、`windows/`、`web/` 下的平台实现）、`packages/core`、`server/`、`cli/`；且**非 Android 平台的行为必须保持不变**（这才是本 NFR 的实质要求）。
- **③ 的执行层级（v0.7 已定，DEC-25）**：非 Android 平台构建检查**不是每任务门槛，而是发布门槛** —— 每个任务的 L1 只做静态隔离检查（①②④），非 Android 构建在发布前执行一次（本机缺 Visual Studio C++ 工作负载，见 §14.6）。**不得**用 ② 的静态检查表述为"构建通过"：进行该检查时若无法执行，写"未验证 + 原因"，并在发布放行门槛（§6.1 ③）处补齐。

### NFR-007 可观测性与可诊断

- **要求**：保活关键事件（服务启动成功/被拒、服务停止、恢复尝试与结果、引导状态变化、回滚原因）写入现有日志体系，且用户可在应用内导出或查看（沿用现状的诊断入口）。日志不得含文件内容、密钥或证书指纹。
- **测量条件（v0.5 改为可枚举，依据复核 R-15）**：完整执行 §14 各验收项后，能从日志用证据回答以下**每一条**（缺一条即本 NFR 不通过）：
  1. 常驻服务是否启动成功、发生在何时；
  2. 若失败，失败在**启动被拒 / 权限缺失 / 监听初始化 / 网络不可用**中的哪一步；
  3. 恢复机制共尝试了几次、每次的结果；
  4. 引导状态何时由何值变为何值；
  5. 用户触发的停止/关闭动作（开关与通知按钮）是否被记录。

### NFR-008 隐私与数据边界

- **要求**：不新增任何外部上报、第三方 SDK 或云服务；新增权限仅用于本地保活与局域网发现（清单见 §10.2）。
- **可验证方式**：依赖清单 diff + 新增权限清单与用途说明对照。

### NFR-009 文档与本地化同步

- **要求**：行为变更同步更新本文件与相关说明；新增用户可见文案走现有 i18n 机制（slang，`app/assets/i18n/`）并同步 `app/lib/gen`；若触及 `[FOSS_REMOVE]` 保护的文件，保留全部标记。
- **可验证方式**：`AGENTS.md` §4 的同步要求在完成报告中逐项对照（翻译文案的写入范围见 Q-06）。

### NFR-010 可回滚性

- **要求**：改动集中、可独立回滚；与**上游代码基线 `230fb692`** 的 diff 便于逐行对比（`AGENTS.md` §7 的取向）。
  > **基线名不要混用（v0.5 修订，依据复核 R-14）**：`230fb692` = 上游代码基线（v1.18.2）／`fe5e5929` = 协作规则基线／`b8f4900c` = 本文档 v0.4 所在提交。三者用途不同：比对**代码**改动用 `230fb692`，比对**规则**改动用 `fe5e5929`。
- **可验证方式（v0.5 改为可枚举、v0.6 修正比较基准，依据复核 R-15 与 V-11）**：①**本任务改动**（`git diff --stat <任务起点>..HEAD`，任务起点 = 该任务分支的父提交）的文件清单全部落在 NFR-006 的**允许清单**内（含 NFR-009 要求的文档 / i18n / 生成文件）；②非 Android 平台目录下无行为代码改动；③改动可整体回滚而不影响既有功能（由 REQ-011 的回归检查验证）。
- **与 `230fb692` 的对比不是判据（v0.6 更正）**：`git diff --stat 230fb692` 必然包含本 fork 已有的 README / AGENTS / docs 提交，故它只能用于观察"与上游差异总量"，**不能**用来判定单个任务是否越界。

---

## 14. 验收标准

### 14.1 验收分层（2026-09-23 调整，DEC-16）

| 层级 | 由谁执行 | 环境 | 何时执行 | 是否阻塞任务 |
|---|---|---|---|---|
| **L1 自动化验收** | **AI，在本机自动完成** | Android Emulator（**Android 16 / API 36**）+ 命令行 + 日志 | **每个开发任务完成前必须执行** | **是** —— L1 未通过即该任务未完成 |
| **L2 真机最终验收** | 仓库所有者人工 | 真实 Android 设备 + 真实局域网 + 第二台设备 | **仅发布前一次性执行** | **否** —— 不作为任何单个开发任务的完成前置 |

原则：

- 凡能通过**自动化测试、模拟器、命令行或日志**验证的内容，**不再要求人工真机验收**（DEC-16）。
- 模拟器不可靠覆盖的能力，明确标记为 **L2 真机最终验收项**（§14.4），但**不得因此阻塞普通开发任务**。
- 依赖真实网络环境的功能（局域网发现、设备互传、后台接收）在**开发阶段**允许通过模拟器、Mock、自动化集成测试或本机多实例验证（DEC-17，§14.3）；真机之间的端到端传输属 L2。
- **缺少真机不构成任何任务的阻塞条件。**
- **非 Android 平台构建检查不是每任务门槛，而是发布门槛**（**DEC-25**）：每任务只做静态隔离检查（NFR-006 ①②④）。

### 14.2 L1 自动化验收项（每个开发任务必须执行）

命令遵循本仓库的 `fvm` 约定（见 `AGENTS.md`）。

| 编号 | 检查 | 命令 | 通过判据 |
|---|---|---|---|
| M-01 | 格式化 | `fvm dart format --set-exit-if-changed lib test`（CI 会先删除 `lib/gen`，生成代码不参与格式检查） | 退出码 0 |
| M-02 | 静态检查 | `fvm flutter analyze` | 无 error |
| M-03 | 单元 / widget 测试 | `fvm flutter test`；改动涉及 UI 状态时可补必要的 widget / integration test；涉及保活逻辑时须补对应单元测试 | 全部通过 |
| M-04 | Android Debug APK 构建 | `fvm flutter build apk --debug` | 构建成功并产出 APK |
| M-05 | 模拟器安装与冒烟 | `adb install -r <apk>` → 启动 → 按该任务验收标准操作关键路径 | 启动无崩溃，关键路径可完成 |
| M-06 | 运行期错误检查 | `adb logcat`（按包名与 `AndroidRuntime` / `FATAL` 过滤）；必要时 `adb shell dumpsys dropbox` | 无未处理异常、无崩溃 |
| M-07 | 变更专项检查 | **任务开始前**在该任务的验收标准里预先声明的专项断言（例：`dumpsys activity services` 中 `foregroundServiceType` 的期望值、`dumpsys power` 中唤醒锁的期望状态） | 任务**预先声明**的每一条断言都通过（v0.5 修订，依据复核 R-11：原判据"与该任务验收标准一致"是循环定义） |

> 完成报告中每个需求 ID 必须附「执行的命令 + 真实输出」（`AGENTS.md` §5/§9）。
> **未实际执行的检查不得声称通过**；无法执行时写"未验证 + 原因"。

#### 14.2.1 L1 的最小覆盖集与追踪矩阵（v0.5 新增，依据复核 R-11）

仅有 M-01…M-07 全绿**不足以**证明需求已被覆盖 —— 这些检查多数与具体需求无关。以下场景**每个都必须有对应的 L1 用例**：

| 必须覆盖的场景 | 对应条目 |
|---|---|
| 四种开关组合（后台接收开/关 × 开机自启开/关）的行为 | REQ-001、REQ-006、BR-09 |
| 首次启动的默认值与引导出现（含"无法检测"态） | REQ-009、BR-02、A-17 |
| 常驻服务启动被拒 → 开关回滚 + 提示原因 | DEC-03、E-01 |
| 服务已启动但监听初始化失败 → 状态呈现 | REQ-002、DEC-22 |
| 关闭常驻通知渠道后，服务与设置页状态不受影响 | REQ-003、NFR-005 |
| 常驻通知与传输通知并存、互不破坏 | DEC-02、S-08、E-16 |
| 关闭「后台接收」后无残留服务/唤醒锁/恢复注册 | REQ-008、AC-12 |
| 用户从系统"活动应用"停止应用之后的行为 | REQ-003、DEC-19、DEC-30 |
| **组合：用户从系统"活动应用"停止应用 → 重启 → 解锁前**（v1.5 新增，复核 04） | REQ-006.5、DEC-19、DEC-30、**DEC-37**（优先级：用户停止优先，**解锁前也不得恢复**） |
| 既有自动化检查全通过 + 改动面静态检查 | REQ-011、NFR-006、NFR-010 |

**追踪矩阵（设计阶段交付物）**：每个 REQ / NFR → 用例 ID → 执行环境（模拟器 / 静态检查）→ 判据。矩阵未产出前，**不得**声称"所有需求已被验收覆盖"；矩阵中无法在 L1 环境执行的项，必须显式标注为"L2 或静态检查"并说明理由（依据复核 R-11）。

**设计阶段另需产出的两项交付物（v1.2 新增）**：

1. **「可连接」探测规程**（§14.5，复核 W-02）：须满足可重复 / 可清理 / 不干扰真实传输 / 成功判定明确四条强制性质；未产出前 L1 无法执行。
2. **服务端生命周期状态机**（**DEC-33**）：前台 / 后台 × 开关开 / 关 的四种组合下，HTTP 服务端、常驻前台服务、通知、锁与恢复注册各自的启停规则，以及前后台切换时的迁移。

### 14.3 模拟器的能力边界与开发期替代验证

**可由模拟器 + 命令行自动验证**（可纳入 L1；各项需在环境就绪后实测确认）：

| 能力 | 自动化手段 |
|---|---|
| 服务与进程状态 | `adb shell dumpsys activity services`、`pidof`、`dumpsys notification` |
| 前台服务与唤醒锁 | `adb shell dumpsys power`（WakeLock）、`dumpsys activity services`（`foregroundServiceType`） |
| 开机自启路径（REQ-006） | `adb shell am broadcast -a android.intent.action.BOOT_COMPLETED <pkg>`；用 `adb shell am compat enable FGS_BOOT_COMPLETED_RESTRICTIONS <pkg>` 模拟 Android 15+ 的启动限制（§3.4 P-01） |
| Doze（AC-07） | `adb shell dumpsys deviceidle force-idle` / `unidle` / `step` |
| 设备重启（AC-10） | `adb reboot` + 等待 `sys.boot_completed` |
| 服务异常退出与恢复（AC-11） | `adb shell am crash <pkg>`、`adb shell kill -9 <pid>` —— **不含 `am force-stop`**（原因见下方"停止语义三分"） |
| 用户在系统"活动应用"中停止应用 | `adb shell cmd activity stop-app <pkg>`（官方文档为"用户停止应用"推荐的等价命令；**本文件未实测**，需在环境就绪后确认） |
| 强制停止（`FLAG_STOPPED`）后的行为 | `adb shell am force-stop <pkg>` —— **单列，不得用于"自动恢复"类验收** |
| 关闭后彻底停止（AC-12） | `dumpsys` 断言无残留服务/唤醒锁 |
| 通知权限（NFR-005） | `adb shell pm revoke <pkg> android.permission.POST_NOTIFICATIONS` |
| 网络栈层面切换（AC-08 的近似） | `adb shell svc wifi disable/enable`、`svc data`（**视镜像而定，需实测确认**） |
| 崩溃与异常 | `logcat`、`dumpsys dropbox` |
| 短时静置（AC-01/AC-02 的近似） | 直接等待 1 小时，或按任务需要选择代表性时长 |
| **「可连接」的判定（v1.0 新增，依据 DEC-32）** | `adb forward` / `adb reverse` 建立端口通道后，直接对目标 HTTP 端口发起真实请求并走到"准备上传"应答（即本表下方"开发期替代验证方式"第 3 条）；或同主机多实例互测。**这是本组 AC 的判据手段，不是可选项** |

**停止语义三分（v0.5 新增，依据复核 R-06 与官方文档核对）** —— 三种"应用被停"不可混用：

| 情形 | 手段 | 系统行为 | 能否用于"自动恢复"验收 |
|---|---|---|---|
| 进程被异常终止 | `am crash <pkg>`、`kill -9 <pid>` | 进程死亡，应用**不**进入 stopped state | ✅ 可以（AC-11 的主用例） |
| **用户主动停止**（Android 13+ 系统"活动应用 / Task Manager"的 Stop） | `cmd activity stop-app <pkg>` | 终止整个应用、移除前台服务通知、**不发送任何回调**；应用只能在下次启动时通过 `ApplicationExitInfo.REASON_USER_REQUESTED` 得知 | ❌ 不可以 —— **已定（DEC-19 / DEC-30）：尊重用户停止、不自动恢复；用户下次打开应用时即恢复** |
| 强制停止 | `am force-stop <pkg>` | 应用进入 **stopped state**；Android 15 起系统**取消其所有待处理 intent**，且只有**用户直接或间接与应用交互**才能解除该状态 | ❌ **不可以** —— 必然产生"无法恢复"的假失败 |

> 平台依据（2026-09-23 核对）：① 用户停止前台服务应用的行为与官方测试命令：<https://developer.android.com/develop/background-work/services/fgs/handle-user-stopping>；② stopped state 的解除条件与 intent 取消：<https://developer.android.com/about/versions/15/behavior-changes-all>。
> 本节同时是 REQ-003 删除"用户停止的唯一途径是开关或通知"这一表述的依据，并落实 REQ-007 的判定闭合（观察窗口 **30 分钟**，DEC-26）。

**模拟器不可靠覆盖 → 标记为 L2 真机最终验收项**（§14.4，不阻塞任务）：

| 能力 | 为何不可靠 |
|---|---|
| 真实局域网设备发现（跨主机组播、AP 隔离、VLAN） | 模拟器处于 NAT 虚拟网络，跨主机组播/广播通常不可靠（需实测确认） |
| 后台长期保活（AC-03/04/05 的 8 / 24 / 72 小时） | 模拟器没有真实的内存压力、省电策略与厂商回收行为；且长跑不适合每个任务执行 |
| 不同厂商 Android 行为（MIUI / EMUI / ColorOS 的自启与后台限制） | 仅有 AOSP 镜像 |
| 真实通知 / 权限交互差异 | 系统 UI、通知渠道策略与真机不同 |
| **解锁前（Direct Boot）的可发现/可连接（AC-10）** | **验证路径待实测核实（v1.5 更正，复核 05）**：v1.4 曾直接断言"只能 L2"，**该断言过强** —— 官方文档只把 `sm set-emulate-fbe` 的**模拟模式**限定在 Android 13 及以下，同时给出了在**采用文件级加密的设备**上测试 Direct Boot 的方法（§18 第 6 条）。**本机 API 36 模拟器的加密状态与"锁屏重启后能否被探测"尚未实测**（可用 `adb shell getprop ro.crypto.type` 起步）。⇒ **设计阶段先实测**：能验证的部分**纳入 L1**，确实不能的再记 **L2 真机**或"未验证 + 原因"。**不得预先断言"L1 必然不可能"**。 |
| 从最近任务列表划掉应用（AC-06） | `am force-stop` 的语义与"划掉任务"完全不同（会进入 stopped state，§14.3），模拟器也不具备真实 ROM 的任务移除行为 ⇒ **只能 L2 真机人工验证 + 静态检查**（v0.6 新增，依据复核 V-12） |
| 真实 Wi-Fi / 移动网络切换、路由器重启（AC-09） | 模拟器无真实射频与路由器 |
| 真机文件系统与系统分享行为（SAF、相册保存、share intent 来源） | 模拟器存储与分享来源有限 |
| 电池优化白名单 / "后台运行不受限" / 厂商自启动设置页（REQ-009、AC-03 起效前提） | 这些系统设置页在模拟器上不存在或不同 |

**开发期对网络相关要求的替代验证方式**（DEC-17；用于开发阶段，**不替代** L2）：

1. 同一主机运行**多个模拟器实例**互测；
2. **本机多实例**：模拟器 + 本机其他构建（桌面版 / `cli/`）互为对端；
3. `adb forward` / `adb reverse` 建立端口通道，直接对目标 HTTP 端口发起真实请求，验证服务端与注册流程；
4. Mock / 自动化集成测试覆盖发现、注册与上传的消息流（不依赖真实组播可达性）。

### 14.4 L2 真机最终验收项（发布前一次性人工验收）

环境：仓库所有者本人的 Android 16 真机（机型待补充，Q-11）+ 真实局域网 + 第二台可运行 LocalSend 的设备。
通用前置条件：

1. 两个开关均为**默认开启**状态（若被关闭则先开启）；引导三项**均无"未满足"** —— 允许"无法检测 / 用户自述已设置"（REQ-009、A-17；v0.5 修订，依据复核 R-09）。
2. 本机与探测设备处于**同一 Wi-Fi 子网**，无客户端隔离（AP isolation）、无跨 VLAN。
3. 本机在验收期间不被手动打开应用（除 AC-06 要求划掉任务）。
4. 通信与传输所需的既有权限（含 `ACCESS_LOCAL_NETWORK`、通知权限）均已授予。

> **"可被发现"的判定口径（DEC-05）**：另一台设备在**任何时间**发起扫描都能发现本机。
> **通过判据（v1.2 立、v1.5 补 AC-10，依据 DEC-32 / DEC-34 / DEC-35 / DEC-37）**：
> - **AC-01 / AC-02 / AC-10：双判据** —— 必须**同时**满足"**可被发现**"（经局域网发现链路验证，**不得**用端口转发/直连绕过，**DEC-35**）与"**可连接**"（定义见下）。AC-10 之所以同样要求"被发现"，是因为开机恢复的目标就是"同网段设备能重新找到本机"（DEC-37）。
> - **AC-03…AC-09、AC-11：单判据** —— 判"**可连接**"；**AC-12 / AC-13 是负向判据**（判"**不**出现"，不适用正向探测规则，见 §14.5 的"三类判据"）。
> - **"可连接"的定义**：对端能对本机**完成一次 prepare-upload 握手**（"发起传输 → 本机正常应答到'准备上传'"），**不要求传完文件** —— 原文第 3、4 项即写作"仍可连接"（§2.3）。
> - **容差（DEC-34）**：短时点测项（AC-01、AC-02、AC-06…AC-13）**零失败**；统计容差（每 24 小时 ≤2 次、且无连续 2 次失败）**只用于长跑项 AC-03 / AC-04 / AC-05**。
> **与 DEC-05 的关系**：DEC-05 的"只判可被发现"**由 DEC-32 取代**；DEC-05 的另一半（**不要求完成传输**）**继续有效** —— 传输能否完成仍属既有功能（REQ-011 覆盖），每个观测点**另行记录**作为观测数据，但**不作为通过门槛**。
> **强度提示与两处更正（v1.1 / v1.2）**："可连接"确实**多**要求了 HTTP 服务端与握手可用，因此覆盖"服务已启动但监听不可用"这一类假达标（§8.3、DEC-22）；但**它不蕴含"可被发现"** —— 直连探测完全绕过组播发现链路，发现坏了照样"可连接"。v1.0 曾据"更强"推断"同时覆盖发现"，**该推断错误**（v1.1 删除）；**v1.2 起 AC-01/AC-02 保留发现判据**（DEC-35）。
> AC-06/07/10/11/12/13 已在 §14.3 列出对应的 L1 自动化近似手段；本表是它们的**最终确认**，也包含模拟器不可覆盖的 AC-01…AC-05、AC-08、AC-09。

> **"任何时间"的可测化参数（v0.7 落定，DEC-26）**：
> - **探测间隔 5 分钟**；**单次"可连接"确认 ≤60 秒**；**每 24 小时允许 ≤2 次失败**；**连续 2 次失败即判不通过**。
> - **服务恢复类**（AC-11，以及 AC-08 / AC-10 的恢复部分）：**观察窗口 30 分钟** —— 窗口内恢复即通过，窗口内未恢复即不通过；实际耗时如实记录（**不设性能阈值**，DEC-07 不变）。
> - 说明：DEC-05 的"任何时间"是连续性要求，而验收只能离散观测，上述参数即二者的操作化。判定一律按这组参数，**不得**再出现"记录但不判定"的第二套口径。
> - **AC-07 的 Doze 延迟**：延迟本身**只记录、不设阈值**（由系统决定），但"是否**可连接**"仍按上述参数判定。

| 编号 | 场景 | 验收条件 | 判定 | L1 是否有近似手段 |
|---|---|---|---|---|
| AC-01 | 后台 1 小时 | 应用退到后台放置 1 小时后 | **可被发现 且 可连接**（双判据，**零失败**；原文"仍可被发现"，DEC-35） | 有（短时静置 + `dumpsys`；**发现链路需实测确认**） |
| AC-02 | 息屏 1 小时 | 锁屏息屏 1 小时后 | **可被发现 且 可连接**（双判据，**零失败**；原文"仍可被发现"，DEC-35） | 有（短时静置 + `dumpsys`；**发现链路需实测确认**） |
| AC-03 | 息屏 8 小时 | 锁屏息屏 8 小时后 | 在观测点**均可连接**（原文"仍可连接"，§2.3）；**长跑项，容差见 §14.5** | **无**（长跑，仅真机） |
| AC-04 | 空闲 24 小时 | 无任何交互静置 24 小时后 | 在观测点**均可连接**（原文"仍可连接"，§2.3）；**长跑项，容差见 §14.5** | **无**（长跑，仅真机） |
| AC-05 | 充电连续 72 小时 | 设备保持充电、后台接收开启、期间无任何交互，连续运行 72 小时 | 按 DEC-26 判定（每 5 分钟探测、单次**可连接**确认 ≤60 秒、每 24 小时 ≤2 次失败、连续 2 次失败即不通过）；无服务崩溃、无异常重启 | **无**（长跑，仅真机） |
| AC-06 | 划掉最近任务 | 从最近任务列表划掉应用后 | 1 分钟内仍**可连接**（**零失败**；按 REQ-005，允许部分 ROM 失败并记入已知限制） | **无可靠近似**（v0.6 更正，依据复核 V-12）：`am force-stop` 与"划掉任务"系统语义不同 ⇒ **不得**使用；只能做静态检查（清单/服务声明）+ L2 真机 |
| AC-07 | Doze 状态 | 设备静置进入 Doze（熄屏、未插电、无移动）后 | 按 DEC-26 判定"是否**可连接**"（**零失败**）；**Doze 下的实际延迟只记录、不设阈值** | 有（`dumpsys deviceidle force-idle`） |
| AC-08 | Wi-Fi 断开重连 | 断开 Wi-Fi 再重连同一网络，全程不打开应用 | 自动恢复监听，**可连接**；无需用户操作（REQ-010） | 部分（`svc wifi` + 端口探测） |
| AC-09 | 路由器重启 | 重启路由器至网络恢复，全程不打开应用 | 自动恢复监听，**可连接**；无需用户操作（REQ-010） | **无**（无真实路由器） |
| AC-10 | 设备重启 | 重启设备，全程不打开应用 | **在用户解锁前**即恢复：**可被发现 且 可连接**（REQ-006 第 5 条、**DEC-37**）；采样从"系统启动完成（锁屏出现）"起算、窗口 30 分钟（§14.5）。**组合情形**：若用户在重启前曾从系统"活动应用"停止应用，则**解锁前也不得恢复**（**DEC-19 / DEC-30 优先于 DEC-37** —— 见 DEC-37 的优先级说明）。解锁前的能力边界以设计阶段的核实结果为准 | **验证路径待实测核实**（见 §14.3）：先查镜像加密状态与锁屏重启行为，能自动化则纳入 L1，确实不能才记 **L2 真机**或"未验证 + 原因" |
| AC-11 | 服务异常退出 | 人为终止常驻服务（手段限 `am crash` / `kill -9`，**不得**用 `am force-stop`，§14.3） | **观察窗口 30 分钟**（DEC-26）：窗口内恢复即通过，未恢复即不通过；实际耗时如实记录 | 有（`am crash` / `kill -9`） |
| AC-12 | 关闭后彻底停止 | 由 REQ-008 推导：关闭「后台接收」后 | 常驻通知消失；**不在前台时**本机不再**可连接**（应用在前台打开着时仍可连接 —— **DEC-33**）；无残留保活组件（唤醒锁/定时任务）；重新开启后 AC-01 仍可通过 | 有（`dumpsys` 断言） |
| AC-13 | 「开机自启」独立生效 | 由 REQ-006 推导：保持「后台接收」开启、关闭「开机自启」后重启 | 重启后**不**自动恢复（符合用户意图；**负向判据的观察窗口见 §14.5 —— 30 分钟，A-20**）；再开启后重启可恢复 | 有（`am broadcast` + `dumpsys`） |

### 14.5 判定与证据要求（所有层级）

- **判据定义：可连接（v1.0，依据 DEC-32）**：观测点上，对端能对本机**完成一次 prepare-upload 握手**即算"可连接" —— 即"发起传输 → 本机应答到'准备上传'"成立；**不要求传完文件**。原文第 3、4 项即写作"仍可连接"（§2.3）。**注意：本判据不蕴含"可被发现"**（见 §14.4 的更正）；**AC-01 / AC-02 另需满足发现判据**（**DEC-35**）。
- **探测规程（v1.1 新增，复核 W-02；**设计阶段必须先产出，未产出则 L1 无法执行**）**：每次"可连接"判定都是一次**真实的接收请求**，而现状的服务端**同一时刻只允许一个上传会话**（§3.2）。因此探测规程必须满足以下**强制性质**：
  1. **可重复**：请求与响应可脚本化重复，含长跑（72 小时 ≈ 864 次探测）；
  2. **可清理**：探测结束（成功、被拒或超时）后**不得留下会话**；须能用 `dumpsys` / 日志断言无残留会话与半开连接；
  3. **不干扰真实传输**：探测与真实传输互斥时必须**让位于真实传输** —— 该次探测记为"**跳过（不可判定）**"，**不判失败**，且**不得**因此中断真实传输；
  4. **成功判定**：本机对 prepare-upload 请求给出**协议合法的正常应答**（超时、连接失败、5xx 均算失败）即算"可连接"成立；是否被**受理**（接收）**另行记录**为观测数据 —— 与 DEC-05"不要求完成传输"一致；
  5. **与 AC-05"无任何交互"的关系**：AC-05 的"无交互"指**无用户操作**；探测是允许的，但受第 3 条约束。
  > 若设计阶段确认上述性质**无法同时满足**（例如探测必然打断真实传输），**必须回到本文件重新决定判据**，**不得**静默降级为"探测可选"。
- **负向判据的观察窗口（v1.1 新增，复核 W-07；**测试假设 A-20**）**：要求"**不**恢复 / **不**出现"的项（如 AC-13）同样使用 **30 分钟**观察窗口 —— 窗口内未出现自动恢复即判通过；否则"迟到的恢复"无法排除。该窗口与 DEC-26 的正向恢复窗口对齐。**这是测试假设，待设计验收用例确认**。
- **三类判据与采样起点（v1.3 新增，复核 04）**：不同性质的 AC 不能用同一把尺子，**按下列三类分别判定，同类内不得混用**：

  | 类别 | 项目 | 判据 | **采样起点** | 容差 |
  |---|---|---|---|---|
  | **A. 正向可达** | AC-01…AC-11 | 每个周期"发现（**AC-01 / AC-02 / AC-10**）+ 连接"均成立 | 触发动作完成时（退到后台 / 息屏 / 划掉任务 / 进入 Doze / 断网 / **重启完成（锁屏出现、即解锁前，AC-10）** / 服务被杀 / 路由器恢复） | 短时项**零失败**（**AC-01、AC-02、AC-06…AC-11 的正向部分**，见 DEC-34）；长跑项 AC-03 / AC-04 / AC-05 按 DEC-26 的统计容差 |
  | **B. 负向不出现** | **AC-12**（关闭后**不再**可连接）、**AC-13**（重启后**不**自动恢复） | 观测窗口内**不得出现**任何一次正向结果；出现一次即失败 | 触发动作完成时（关闭开关完成 / 系统启动完成） | **零容差**：一次正向结果即失败；窗口结束仍未出现即通过 |
  | **C. 恢复窗口** | AC-08 / AC-10 / AC-11 的**恢复部分** | 在 **30 分钟**窗口内**出现**恢复 | 故障发生时刻（断链 / 重启完成 / 进程被杀） | 窗口内恢复即通过；超窗未恢复即失败；实际耗时如实记录 |

  > 由此明确两件此前含糊的事（复核 04 的实质）：① **AC-12 / AC-13 不适用"每次探测都必须成功"** —— 它们本来就是负向判据；② **恢复类项在窗口内的"尚未恢复"时段不计失败**，否则同一份记录会被两类规则判成相反结果。窗口值取 **DEC-26 / DEC-34 / A-20**（30 分钟）。
  > **A 与 C 的关系（v1.5 补充，复核 02）**：AC-08 / AC-10 / AC-11 同时受**两类**约束 —— **C 类**管"从故障到恢复"（窗口内未恢复不计失败），**A 类**管"恢复之后"的正向探测。判定顺序：**先按 C 类确认窗口内出现恢复，再按 A 类做正向探测**。其中 **AC-10 的正向探测从"系统启动完成（锁屏出现）"起、到 30 分钟窗口结束**，因此它**在解锁前就开始采样**（DEC-37）。
- **通过 / 失败（参数按 DEC-26，判据按 DEC-32 / DEC-34 / DEC-35）**：
  - **通过（v1.5 重写：总则服从三类判据，不得用一条规则覆盖全部 AC —— 复核 03）**：
    - **A 类 · 正向可达（AC-01…AC-11）**：按 **每 5 分钟至少一次**的探测节奏，**每次"可连接"确认耗时 ≤60 秒**；其中 **短时点测项（AC-01、AC-02、AC-06、AC-07、AC-08、AC-09、AC-10、AC-11 的正向部分）探测全部成功（零失败）**（**DEC-34**），**长跑项 AC-03 / AC-04 / AC-05** 允许**每 24 小时 ≤2 次失败**且**无连续 2 次失败**；
    - **A 类中带发现判据者（AC-01、AC-02、AC-10）**：还须满足"**可被发现**"（经局域网发现链路验证，不得绕过，**DEC-35**；采样见上方"发现判据的手段与采样"）；
    - **B 类 · 负向不出现（AC-12、AC-13）**：**不适用** A 类的"每次探测都必须成功" —— 观测窗口内出现**一次**正向结果即失败，窗口结束仍未出现即通过（见下方三类判据表）；
    - **C 类 · 恢复窗口（AC-08 / AC-10 / AC-11 的恢复部分）**：在 **30 分钟**窗口内**出现**恢复即通过；**窗口内的"尚未恢复"不计失败**（否则会与 A 类的正向规则互相打架）。Doze 下的实际延迟只记录，不作为判据。
  - **失败** = **对应类别**的条件被突破：A 类 —— 短时项任一失败 / 长跑项连续 2 次失败或每 24 小时超过 2 次 / 单次连接确认超 60 秒 / 发现判据不成立；B 类 —— 窗口内出现一次正向结果；C 类 —— 超过 30 分钟窗口仍未恢复。
  - **发现判据的手段与采样（AC-01 / AC-02；v1.3 补全采样，复核 03）**：须经**局域网发现链路**验证 —— L1 用模拟器 / 本机多实例的组播发现（§14.3 中标注"需实测确认"），**不得**用 `adb forward` 直连替代；若 L1 无法可靠验证发现，该项须标为 **L2 或"未验证 + 原因"**，**不得**用"连接成功"反向推断"发现成立"。
    **采样方式与容差（v1.5 扩到 AC-10，复核 02）**：发现与连接**共用同一节奏、同一超时** —— 每个探测周期（每 5 分钟）**先做一次发现扫描（≤60 秒内发现本机）、再做一次连接握手（≤60 秒）**；两者的失败判据一致（短时项**零失败**，DEC-34）。**观察窗口**：**AC-01 / AC-02** 与该项本身时长相同（各 1 小时）；**AC-10** 从"**系统启动完成（锁屏出现）**"起算，到 **30 分钟恢复窗口**结束为止（**在解锁前即开始采样**，DEC-37 + A-20）。窗口起点类别见下方"三类判据与采样起点"。
  - 无论通过或失败，都须记录：机型/镜像与系统版本、当时的电量与省电状态、是否处于 Doze、日志片段。
  - **不得**表述为"已证明全程任意时刻在线"（连续性由上述参数的采样密度近似，而非连续证明）；也**不得**同时出现"通过"与"未被证明"两套口径。
  - **Doze / 息屏等场景下"可连接"可能被系统延迟**：只记录实际耗时，**不因此改判为通过** —— 判据仍是"可连接"是否在参数内成立。
- 对 ROM 导致、且已按 REQ-009 完成引导后仍失败的项，记入"已知限制"（BR-04），**不通过也如实记录**，不得以"系统行为"为由判为通过。
- **L1 证据** = 实际命令 + 退出码 / 测试输出 + `logcat` / `dumpsys` 摘录，写进完成报告。
- **L2 证据** = 执行时间、起始状态、观测时刻、结果、失败时的日志与系统设置截图。
- **不得声称未实际执行的测试已经通过**；未执行即写"未验证 + 原因"（`AGENTS.md` §4/§9）。

### 14.6 环境现状与剩余步骤（**v0.5 重测，2026-09-23**）

> 本节原记录（"以下全部缺失"）来自更早一次限定范围检索，**已被本次实测取代**（依据复核 R-12/R-14 的事实部分）。原安装步骤清单已大部分完成，故本节改写为"现状 + 剩余步骤"。

**实测方式**：`command -v <tool>`、`fvm flutter --version`、`fvm flutter doctor`、`ls` 直接检查 SDK 目录、`emulator -list-avds`。

**已就位（本次实测通过）**：

| 项 | 实测证据 |
|---|---|
| Flutter（经 fvm） | `fvm flutter --version` → `Flutter 3.41.9 • channel stable`、`Dart 3.11.5`，与 `.fvmrc` 一致；SDK 位于 `~/fvm/versions/3.41.9` |
| Rust 工具链 | `cargo 1.97.1`、`rustc 1.97.1`（`~/.cargo/bin`） |
| JDK | `JAVA_HOME=C:\Users\Zen\AppData\Local\Programs\Temurin\jdk-17.0.20.1+1`（注意 `java` **不在 `PATH`**） |
| Android SDK | `ANDROID_HOME=…\Android\Sdk`：`platforms/android-36`、`build-tools/36.0.0`、`ndk/28.2.13676358`、`cmake/3.22.1`、`cmdline-tools/latest/bin/sdkmanager.bat`、`platform-tools/adb`、`emulator/emulator.exe` |
| 系统镜像 | `system-images/android-36/google_apis/x86_64` |
| AVD | `emulator -list-avds` → `localsend-api36` |
| 仓库内配置 | `app/android/local.properties` 已指向上述 Flutter 与 Android SDK；该文件**未被版本控制**（符合 `AGENTS.md` §8） |

**仍缺 / 仍有问题（本次实测）**：

| 项 | 实测证据 | 影响 |
|---|---|---|
| Android SDK 许可未接受 | `fvm flutter doctor` → `[!] Android toolchain … ✗ Android license status unknown` | Android 构建可能被拒 |
| 模拟器实例 offline | `fvm flutter doctor` → `! Device emulator-5554 is offline` | M-05 之前不可用，需先排查 |
| Visual Studio 缺 C++ 工作负载 | `fvm flutter doctor` → 缺 `MSVC v142`、`C++ CMake tools for Windows`、`Windows 10 SDK` | **非 Android 桌面构建不可用** ⇒ NFR-006 的"非 Android 平台构建检查"当前**无法执行**，只能写"未验证 + 原因"（复核 R-11/R-15） |
| `flutter_rust_bridge_codegen` | 不在 `PATH`（`~/.cargo/bin` 中无） | 仅当改动 FRB 接口时需要；本期未计划 |
| Chrome | 未安装 | 不影响本期（Web 目标不在范围） |

**剩余步骤**：

| 步骤 | 命令 | 验收方式 |
|---|---|---|
| 1 | 接受 Android SDK 许可：`fvm flutter doctor --android-licenses` | 退出码 0，再跑 `fvm flutter doctor` 不再出现 `[!] Android toolchain` |
| 2 | 排查 `emulator-5554` offline：`adb devices`、`adb kill-server && adb start-server`，必要时重开模拟器 | `adb devices` 显示 `device` |
| 3（**不需要**，DEC-25） | 安装 Visual Studio 的"使用 C++ 的桌面开发"工作负载 | 仅当日后把非 Android 构建检查改回每任务门槛时才需要；当前它属发布门槛，可由具备该环境的机器或 CI 执行 |
| 4（按需） | `cargo install flutter_rust_bridge_codegen` | 仅当改动 FRB 接口时 |

**授权状态（v0.7）**：步骤 1、2 会改变本机外部状态，执行构建/测试会写入 `target/`、`.dart_tool/` 等被忽略目录 —— 按 `AGENTS.md` §6/§8 需显式授权，**已获授权**（**DEC-28**，2026-09-23）。步骤 3（Visual Studio 工作负载）**不需要**（DEC-25 已把非 Android 构建降为发布门槛）；步骤 4 仅当改动 FRB 接口时需要。
**仍必须遵守**：任何 L1 检查在**未实际执行**前一律写"未验证 + 原因"，该任务不得判定为完成（§14.1）；授权不等于"可以声称跑过"。

> **与 `AGENTS.md` §11 的偏差**：§11 的"环境现状"仍记录工具链全部缺失，与本表不符。`AGENTS.md` 属协作规则文档，本次**未修改**，需单独授权（见 §17 D-08）。

**环境搭建参考（在新机器上重建环境时使用；本次实测前已基本完成，与上表"仍缺"项对照使用）**：

| 步骤 | 内容 | 验收方式 |
|---|---|---|
| 1 | 取得 Flutter SDK，版本须与 `.fvmrc`（`3.41.9`）一致。两条路径：① 初始化仓库内的 `support/submodules/flutter` 子模块（与 pin 完全一致）；② 安装 `fvm`（Windows 独立安装器）后在本仓根目录执行 `fvm install` | `fvm flutter --version` 显示 3.41.9 |
| 2 | 安装 JDK（Android 构建所需，建议 Temurin 17 或 Android Studio 自带的 JBR） | `java -version` |
| 3 | 安装 Android SDK 命令行工具（`commandlinetools-win-*.zip` → `%LOCALAPPDATA%\Android\Sdk\cmdline-tools\latest`），设置 `ANDROID_HOME` / `ANDROID_SDK_ROOT`（**本次实测：二者均已设置**，见上表） | `sdkmanager --version` |
| 4 | `sdkmanager --licenses` 接受许可 | 退出码 0 |
| 5 | `sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0" "emulator"` | `adb version`、`emulator -version` |
| 6 | 安装 API 36 系统镜像：`sdkmanager "system-images;android-36;google_apis;x86_64"` | `sdkmanager --list_installed` 含该项 |
| 7 | 创建 AVD：`avdmanager create avd -n localsend-api36 -k "system-images;android-36;google_apis;x86_64"` | `emulator -list-avds` 含 `localsend-api36` |
| 8 | 启用硬件加速（Windows 需 WHPX 或 AEHD；须在 BIOS 中开启 CPU 虚拟化） | 模拟器正常启动 |
| 9 | `fvm flutter doctor -v`，逐项解决 | 无未解决项 |
| 10 | 首次构建与冒烟：`fvm flutter pub get` → `build_runner` / `slang` codegen → `fvm flutter build apk --debug` → 装入模拟器 | §14.2 的 M-01…M-07 全部可执行 |

**已知需在环境就绪后核实的项（v0.5 更新）**：

1. 现状 `compileSdkVersion 36` 而 `targetSdkVersion 37`（§3.1）；构建期是否因此报错或仅告警，需实测（不属本需求改动，仅记录）。
2. §14.3 表中标注"需实测确认"的各项：模拟器组播可达性、`svc wifi` 行为、以及 v0.5 新增的 `cmd activity stop-app` 行为。
3. `fvm flutter test` 与 `fvm flutter build apk --debug` 在本机的实际可用性（**本次未实测**：构建/测试属 `AGENTS.md` §6 需单独授权的操作，且 Android 许可尚未接受）。

---

## 15. 假设清单（剩余，**未确认**）

§16.1 的决策已不再列入本表。以下为随本次更新产生的**剩余假设**。

| 编号 | 假设 | 影响面 | 若不成立的后果 | 状态 |
|---|---|---|---|---|
| ~~A-01~~ | **已于 2026-09-23 确认**：「开机自启」默认**开启** → 升为 DEC-14 | REQ-006、BR-02 | — | 已解决 |
| ~~A-09~~ | **已解决（2026-09-23）→ 升为 DEC-21**：引导允许跳过，不强制 | REQ-009 | — | 已解决 |
| A-11 | 常驻服务的通知渠道名与传输通知的现用渠道（`localsend_foreground_service`）不冲突，由设计阶段命名 | REQ-003 | 若冲突会互相覆盖（DEC-02 已排除复用，但命名仍需避让） | 设计阶段处理 |
| A-12 | AC-11 的"人为终止常驻服务"通过 `adb` 完成（L1 手段） | §14.3、AC-11 | v0.7 更新：`adb` 与模拟器**已就位**（§14.6 实测），执行也已授权（DEC-28）；仍待实测确认的是 `cmd activity stop-app` 的具体行为 | 已不阻塞 |
| A-13 | 接收历史、扫描去重等既有数据不因保活改动而改变 | REQ-011 | 兜底由回归验收覆盖 | 低风险 |
| A-14 | 两个开关的持久化沿用**现有存储能力**（候选：`shared_preferences` 现有键空间），不引入新的持久化机制。**v1.4 变更**：因 **DEC-37** 要求解锁前可读，存储须改为 / 镜像到**设备加密存储** ⇒ 本假设的"沿用现有键空间"部分**已失效**，见 §10.1 | §10.1、NFR-004、REQ-006.5 | 新增键已获授权（**DEC-27**）；"是否构成持久化格式变更"按"是"处理 | **已变更（DEC-37）** |
| ~~A-15~~ | **已解决（2026-09-23）→ 升为 DEC-37（选项 ②）**：本期**要求解锁前也能恢复**（新增需求），存储改为设备加密存储、清单新增 `directBootAware` 接收器 —— 详见 REQ-006.5 | REQ-006.5、AC-10、§10.1 | — | 已解决 |
| A-16 | **覆盖安装（应用升级）后不自动恢复**常驻服务，暂列后续（复核 R-16；现状 `autoRunOnMyPackageReplaced: false`） | REQ-006.6、REQ-007 | 若要求恢复，需新增触发路径与对应用例 | **待确认** |
| ~~A-17~~ | **已解决（2026-09-23）→ 升为 DEC-24**：引导"无法检测"项显示"无法检测（需你自行确认）"+ 用户自述入口 | REQ-009、§8.3 | — | 已解决 |
| A-18 | **复核建议的临时取向**（仅为让设计可推进，未确认）：Q-13 引导形态取"设置页内联状态区 + 首次启动提示"；Q-14 本期只有"停止"按钮；Q-17 低版本不承诺保活、只要求不显示"已生效" | REQ-009、REQ-003、NFR-004.3 | 三者任一被否决时，对应设计需返工 | **待确认** |
| A-19 | 并发与过渡态：**最后一次明确的用户意图优先 + 操作幂等**，晚到的启动结果不得覆盖最新关闭意图（复核 V-14 建议） | REQ-001、REQ-008、E-11、E-18 | 若要求"先到先得"或"一律以服务真实状态为准"，状态机需另设计 | **待确认**（须在设计状态机中显式验证，**不得**当作已确认需求） |
| A-20 | 负向判据（AC-13 等"不恢复 / 不出现"项）也使用 **30 分钟**观察窗口（复核 W-07 建议；与 DEC-26 的正向窗口对齐） | AC-13、§14.5 | 若设计要求更长或更短的窗口，需在验收用例中标注并说明理由 | **待确认（测试假设）** |

---

## 16. 决策与开放项

### 16.1 已确认决策（2026-09-23，确认人：仓库所有者）

> 第一批 **DEC-01…DEC-17**（需求确认阶段）；第二批 **DEC-18…DEC-29**（第二轮复核 V-01…V-16 引出的取舍，2026-09-23 由仓库所有者逐项选定）。

| 编号 | 问题 | **决策** | 影响的条目 |
|---|---|---|---|
| DEC-01 | 改动范围与依赖授权（原 Q-02） | **授权**修改 `packages/localsend_isolates/**`；**授权**新增依赖 | REQ-002、§3.2、NFR-010 |
| DEC-02 | 常驻通知与传输通知的关系（原 Q-05） | **另建渠道/服务**：常驻服务与传输期服务完全分离，互不影响 | REQ-002.3、REQ-003、S-08、E-12、BR-07、REQ-011 |
| DEC-03 | 服务启动被拒时的开关呈现（原 Q-03） | **回滚为"关闭" + 提示原因**（不允许"开而未生效"的假象） | REQ-001 失败分支、E-01、BR-05、§8.3、§10.3 |
| DEC-04 | minSdk / 目标 Android 版本（原 Q-07） | **由我决定 → 保持现状**：不改变 minSdk 与任何 SDK 级别（继续不显式声明，沿用 `flutter.minSdkVersion`）；**验收基线 = Android 16（API 36）**。理由见 NFR-004.2/3 | NFR-004、§5.2、§14.1 |
| DEC-05 | "可被发现"的判定（原 Q-08） | **第二台设备在任何时间发起扫描都能发现本机**；通过判据仅为"可被发现"，不要求完成传输（传输能力作为观测记录）。**v1.0 修订**：本条的前半（**通过判据**）**由 DEC-32 取代** —— 判据改为"**可连接**"；后半（**不要求完成传输**）**继续有效** | §14.4、§14.5、REQ-004/005 判定、DEC-32 |
| DEC-06 | 发行渠道（原 Q-10） | **仅私有侧载** → 可直接使用 `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`；无需 Play Console 类型申报 | REQ-009、§10.2、NFR-004.4 |
| DEC-07 | 耗电/资源阈值与恢复时限（原 Q-09/Q-04） | **不设**：耗电与资源本期只观测不设阈值；REQ-007 的恢复**不设时限**，只记录实际耗时 | NFR-002、NFR-003、REQ-007、AC-07、AC-11 |
| DEC-08 | 验收设备（**已被 DEC-16 取代**） | 原为"仓库所有者本人的手机即日常验收设备" | §14.1 → 现降级为 L2 真机最终验收用机 |
| DEC-09 | 需求 ID 格式（原 Q-12） | **修订 `AGENTS.md` §5**：改为 `REQ-<三位序号>` / `NFR-<三位序号>`。**已执行** | §17 D-01、`AGENTS.md` §5 |
| DEC-10 | 开关默认值与独立性（原 Q-01） | 「后台接收」**默认开启**；「开机自启」是**独立开关**（默认值见 DEC-14） | REQ-001、REQ-006、BR-02、BR-09、§8、S-01 |
| DEC-11 | 目标平台类型选择（实现要求 3 的落实） | 常驻服务按 **`connectedDevice`** 设计，不使用 `dataSync` 作为全天候保活（平台核对见 §3.4，结论：该选择同时解开 REQ-002 与 REQ-006 的阻塞） | REQ-002.2、§10.2、§3.4 |
| DEC-12 | 常驻通知是否允许长期显示 | **允许**（实现要求 2） | REQ-003 |
| DEC-13 | 滥用系统的方案 | **不采用** 1 像素 Activity / 无限 Alarm / 高频 WorkManager / 多进程守护（实现要求 9） | §5.2、NFR-001 |
| DEC-14 | 「开机自启」开关的默认值（原 Q-15，2026-09-23 确认） | **默认开启**（与「后台接收」一致） | REQ-006.1、BR-02、§8.2、§10.1 |
| DEC-15 | `AGENTS.md` §1 / §2 的路径引用（原 Q-18，2026-09-23 确认） | **同步修订**为 `docs/requirements.md`。**已执行** | §17 D-02、`AGENTS.md` §1、§2 |
| DEC-16 | 验收策略（2026-09-23 调整） | **分层**：L1 自动化验收（AI 在本机用 Android Emulator + 命令行完成，**每个任务必须执行**，是任务完成判据）；L2 真机最终验收（**仅发布前一次性人工执行**，不作为任务前置）。凡可自动化验证的，不再要求人工真机验收。**缺少真机不构成阻塞** | §14.1、§14.2、§14.4、§16.2 Q-11、§15 A-12、§17 D-04、§1.2；取代 DEC-08 |
| DEC-17 | 网络类能力的开发期验证方式 | 局域网发现、设备互传、后台接收等依赖真实网络环境的能力，**开发阶段**允许用模拟器 / Mock / 自动化集成测试 / 本机多实例验证；真机之间端到端传输属 L2 | §14.3、REQ-010 |
| DEC-18 | 关闭「后台接收」时对共享 HTTP 服务端 / 监听的处置（原 Q-20） | **立即停止**：关闭即停止监听并释放资源；**进行中的传输会被中断**。这是对 REQ-011（既有功能不回归）的**显式例外**，且**不新增**"优雅等待传输完成"逻辑。**v1.2 收窄（DEC-33）**：本条**只在"应用不在前台 且 开关关闭"时成立** —— 应用在**前台**时关闭开关**不**停止监听（前台接收沿用现状），因此也**不**中断传输 | REQ-008、E-12、BR-10、REQ-011、DEC-33 |
| DEC-19 | 用户在 Android 13+ 系统"活动应用"中 Stop 之后（原 Q-21） | **尊重用户停止，不自动恢复**：在用户下次主动打开应用之前保持停止（与 BR-01 一致）；期间**不得通过任何后台机制**（开机自启、恢复重试、网络变化）重启常驻服务。**v0.8 修订**：本条原有一句"应用下次启动时不得静默重启常驻服务"**由 DEC-30 取代**（用户打开应用即恢复） | REQ-003、REQ-004、REQ-006、AC-11、§8.3、BR-01、DEC-30 |
| DEC-20 | P1 失败与"已知限制"阻塞到哪一层（原 Q-23） | **P1 失败不阻塞本期完成**（须带证据如实记录）；**ROM/系统导致的已知限制不阻塞发布**（按 BR-04 记录机型与 ROM，如实标注未达成） | §6.1、§14.4、§14.5、BR-04 |
| DEC-21 | 引导是否允许跳过（原 Q-16、A-09） | **允许跳过**（保持 A-09 的方向）：跳过时明确告知"后台接收可能无法长期生效" | REQ-009、§6.1 |
| DEC-22 | 服务已启动但监听不可用时的开关呈现（原 Q-25） | **保持"开"并显示"异常 / 暂不可被发现"**（不置灰、不回滚为关）；不得显示为"生效中"（BR-05） | REQ-002、§8.3、§10.3 |
| DEC-23 | 覆盖升级后缺少两个新键时的默认值（原 Q-26） | **缺失键按"开启"** —— 首次安装、覆盖升级、清除数据三种情形一致；覆盖升级后的首次启动**须给出说明**（说明"为何出现常驻通知与开机自启"） | REQ-001、REQ-006、§8.2、§10.1、BR-02 |
| DEC-24 | 引导第 3 项不可检测时的呈现（原 Q-27、A-17） | **显示"无法检测（需你自行确认）"**，并允许用户手动标记"我已设置"（界面标注为**用户自述**，不冒充系统确认） | REQ-009、§8.3 |
| DEC-25 | NFR-006 的非 Android 构建检查属于哪一层（原 Q-29） | **降为发布门槛**：每个任务的 L1 只做静态隔离检查（NFR-006 ①②④）；非 Android 构建在发布前执行一次；**不得**用静态检查表述为"构建通过" | NFR-006、§14.1、§14.2 |
| DEC-26 | AC-01…AC-11 的判定参数（原 Q-22） | **探测间隔 5 分钟；单次确认 ≤60 秒；每 24 小时允许 ≤2 次失败；连续 2 次失败即判不通过；服务恢复观察窗口 30 分钟**（恢复仍不设性能阈值，仅设窗口）。**v1.0 口径说明**：其中"单次发现 ≤60 秒"按 **DEC-32** 读作"单次**可连接**确认 ≤60 秒"。**v1.2 收窄（DEC-34）**：其中"每 24 小时允许 ≤2 次失败"**只适用于长跑项 AC-03 / AC-04 / AC-05**；短时点测项**零失败**。**v1.5 澄清**：该"零失败"适用于**正向**探测项（AC-01、AC-02、AC-06…AC-11 的正向部分）；**AC-12 / AC-13 是负向判据**，按 §14.5 的 B 类判定，**不适用"零失败"** | §14.4、§14.5、AC-01…AC-11、REQ-007、REQ-010、DEC-32、DEC-34 |
| DEC-27 | 新增持久化键是否授权（原 Q-24） | **授权**新增持久化键（两个开关 + 引导状态及其迁移规则），**不新增依赖**（沿用现状存储能力） | §10.1、REQ-001、REQ-006、A-14 |
| DEC-28 | 授权执行构建 / 测试 / 许可接受（原 Q-19） | **授权**：接受 Android SDK 许可、排查 offline 模拟器、执行 `pub get` / codegen / `analyze` / `test` / 构建（写入 `.dart_tool`、`target` 等被忽略目录）。安装 Visual Studio 工作负载**不需要**（DEC-25 已把非 Android 构建降为发布门槛） | §14.2、§14.6、NFR-006 |
| DEC-29 | §2.1 五条原始想法的准确性（原 V-01 / Q-28 第 ① 组） | **经仓库所有者确认无误**（2026-09-23）；§2.1 据此标注"已由所有者确认"。§2.2 的 10 条实现要求与 §2.3 的验收标准原文**其后已补录完毕**（见 **DEC-31**，含"实为 11 项"的记数更正） | §2.1、§16.2 Q-28 |
| DEC-30 | 被系统停止后"下次打开应用"的语义与呈现（原 Q-30；**选项 ③**） | **用户主动打开应用即恢复**：应用启动时按开关的持久化状态（用户从未关闭 → 仍为"开"）启动常驻服务与监听，视为用户**重新表达意图**。**对 DEC-19 的收窄**：DEC-19 的"不自动恢复"只约束**后台机制**（开机自启、恢复重试、网络变化），不约束"用户打开应用"这条路径；DEC-19 中"下次启动不得静默重启"一句由本条取代。**该"被用户停止"状态跨设备重启保持**，直到用户打开应用为止。"打开应用即恢复"之后开关显示为"开"且确为运行中，符合 BR-05（不呈现假象） | REQ-003、REQ-004、REQ-006、AC-11、§8.3、BR-01、BR-05 |
| DEC-31 | 原始条目原文的补录与核对（原 Q-28；按 §20.9 的第 ① 种处理） | **已补录并核对完毕**（2026-09-23，原文由仓库所有者提供）：§2.2 的 10 条实现要求**逐条一一对应**，v0.4 的合并映射经原文核对**全部正确**；§2.3 的原始验收标准**实为 11 项**（此前记为"10 项"系本文件记数错误），与 AC-01…AC-11 **逐条同序对应**。唯一实质差异是第 3、4 项原文写"仍可**连接**"而当时判据为"可**被发现**" → 已由 **DEC-32** 处理 | §2.2、§2.3、§16.2 Q-28 / Q-31 |
| DEC-32 | AC-03/AC-04 及其余 AC 的判据强弱（原 Q-31；**选项 ③**） | **全部 AC 的通过判据由"可被发现"改为"可连接"**："可连接"= 对端能对本机完成一次 **prepare-upload 握手**（"发起传输 → 本机应答到'准备上传'"），**不要求传完文件**。**取代 DEC-05 的通过判据部分**（其"不要求完成传输"继续有效）。**v1.2 说明（重要）**：本条只覆盖"**连接**"一侧（含 HTTP 服务端与监听是否可用 ⇒ 覆盖"服务已启动但监听不可用"的假达标，§8.3、DEC-22）；它**不蕴含"可被发现"**，发现判据见 **DEC-35** | §1.3、§14.4、§14.5、AC-01…AC-12、DEC-05、DEC-35、§19 术语 |
| DEC-33 | 关闭「后台接收」是否连"前台接收"一并禁用（原 Q-33；**选项 ②**） | **开关只控制"后台常驻"**：应用在**前台**时，HTTP 服务端与接收能力**不依赖该开关**，沿用现状（服务端随进程可用）；**停止监听只发生在"应用不在前台 且 开关关闭"时**。由此：① 关闭开关**不改变**前台接收 ⇒ 前台部分**不构成** REQ-011 的新增例外；② 本需求新增的停止行为 = 常驻服务、常驻通知、唤醒锁、恢复注册的停止，**加上"不在前台时"的监听停止**；③ 服务端生命周期由"应用是否在前台"与"开关"**共同决定** —— 这**改变了现状的"随进程启动一次"**，设计阶段须给出状态机（含前后台切换的边界）。**关联：DEC-18 据此收窄** | REQ-001、REQ-002、REQ-008、BR-10、E-12、AC-12、DEC-18、§8.2、§8.3 |
| DEC-34 | 短时 AC 是否允许失败（原 Q-34；**选项 ①**） | **短时点测项零失败**：AC-01、AC-02、AC-06…AC-13 的探测**必须全部成功**，不允许偶发失败；**统计容差（每 24 小时 ≤2 次失败、且无连续 2 次失败）只用于长跑项 AC-03 / AC-04 / AC-05**。**收窄 DEC-26 的容差条款** | §14.4、§14.5、DEC-26、AC-01…AC-13 |
| DEC-35 | "可发现"是否仍单独验收（原 Q-32；**选项 ①**） | **AC-01 / AC-02 保留双判据**：必须**同时**满足"**可被发现**"（经局域网发现链路，**不得**用端口转发/直连绕过）与"可**连接**"；其余 AC 仍只判"可连接"。同时确认：v1.1 删除"可连接蕴含可发现"的推断**是必要的更正** | §14.4、§14.5、AC-01、AC-02、§19 术语 |
| DEC-36 | 前台关闭开关时发现机制是否沿用现状（原 Q-35；**选项 ①**） | **沿用现状**：应用在**前台**时，**发现（组播公告）与连接都保持可用** —— 关闭开关只停"后台常驻"（DEC-33）。由此 BR-06 的残留判据为"不在前台时仍在监听"，§8.3 的前台行同时表示"仍可被发现与连接" | REQ-001、REQ-002、BR-06、§8.3、DEC-33 |
| DEC-37 | 本期是否只要求"首次解锁后"自动恢复（原 Q-36；**选项 ②**） | **要求解锁前也能恢复**：设备重启后、**在用户解锁前**即应恢复"可被发现 + 可连接"（在 Android 规则允许的范围内）。**这是对原始需求第 7 条的加强（新增需求，不是对既有条目的解释）**，并带来明确的架构代价：① 两个开关状态须在**解锁前可读** ⇒ 存储须改为 / 镜像到**设备加密存储**（`createDeviceProtectedStorageContext()`），§10.1 的候选方案与 A-14 随之改变，DEC-27 的键授权覆盖之；② 清单须新增 `directBootAware` 接收器并监听 `LOCKED_BOOT_COMPLETED`（`ACTION_USER_UNLOCKED` 用于解锁后的收尾）；③ **解锁前的能力边界须由设计阶段核实**（解锁前能否启动前台服务、HTTP 服务端与 TLS 证书是否可用、下载目录是否可写、局域网权限是否可用）—— **本文件不预设核实结果**；若平台不允许，须回到本文件修订，**不得静默降级**；④ **验证路径待实测核实**（v1.5 更正：先查镜像加密状态与锁屏重启行为，能自动化则纳入 L1，确实不能才记 **L2 真机**或"未验证 + 原因"）。**优先级（v1.5 补充，复核 04）**：**DEC-19 / DEC-30"尊重用户停止"优先于本条** —— 若用户在重启前从系统"活动应用"停止过应用，则**解锁前也不得恢复**；BR-01（用户显式意图优先）在此同样适用 | REQ-006.5、AC-10、§10.1、§10.2、§14.3、§14.2.1、A-14、A-15、DEC-19、DEC-27、DEC-30、§18 |

> **对 DEC-07 的范围说明（v0.5 新增，依据复核 R-03/R-04）**：DEC-07 的"**不设时限**"被本文件读作"**不设性能阈值**"，而**不是**"连判定窗口也不设" —— 否则 AC-07/AC-11 将永远无法判失败。判定所需的观察窗口已定为 **30 分钟**（**DEC-26**），即：不设性能阈值、但窗口内未恢复即判不通过。

### 16.2 开放项

**标记图例（v0.6 统一，依据复核 V-04）** —— 标记**只在本表出现**，§20 不再维护第二份清单（v0.5 的两份清单已产生标记漂移）：

| 标记 | 含义 |
|---|---|
| **「设计前必须」** | 必须先落定；否则设计会把未确认的假设固化成需求 |
| **「实现前必须」** | 设计中可以提出方案，但**进入实现前**必须落定 |
| （无标记） | 不阻塞设计与实现，可随设计一并确定 |

**本节的任何一条都不是已确认需求**：在落定前一律按 `AGENTS.md` §10 记录、不实现。

| 编号 | 问题 | 备选 / 备注 |
|---|---|---|
| ~~Q-15~~ | 已决定：「开机自启」默认**开启** → 见 DEC-14 | 已解决（2026-09-23） |
| Q-11 | L2 真机最终验收用机的具体机型与系统版本（**不阻塞任何任务**）。 | 需你提供 |
| Q-13 | 引导页的形态：独立页面 / 设置页内联区 / 首次启动时的对话框。 | 影响 §8.1；建议内联区 + 首次对话框 |
| Q-14 | 常驻通知的文案与是否需要更多操作按钮（如"暂停接收"）。 | 影响 REQ-003 |
| Q-06 | 新增用户可见文案是否允许写入 `app/assets/i18n/*.json`（会与上游 Weblate 流程产生差异）。 | 影响 NFR-009 |
| ~~Q-16~~ | 已解决（2026-09-23）→ **DEC-21**：**允许跳过**（保持 A-09 的方向）。 | 已解决 |
| Q-17 | 低于 Android 12 的设备上，保活能力不可用时如何呈现（NFR-004.3 的设计输入）。 | ①开关置灰并说明；②允许开启但标注"本设备不受支持" |
| ~~Q-18~~ | 已决定：同步修订 → 见 DEC-15 | 已解决（2026-09-23） |
| ~~Q-19~~ | 已解决（2026-09-23）→ **DEC-28**：**授权**接受 Android SDK 许可、排查 offline 模拟器、执行 `pub get` / codegen / `analyze` / `test` / 构建。安装 Visual Studio 工作负载**不需要**（DEC-25 已把非 Android 构建降为发布门槛）。 | 已解决 |
| ~~Q-29~~ | 已解决（2026-09-23）→ **DEC-25**：**降为发布门槛**（每任务只做静态隔离检查）。 | 已解决 |
| ~~Q-20~~ | 已解决（2026-09-23）→ **DEC-18**：**立即停止**监听，接受中断进行中的传输（构成 REQ-011 的显式例外）。 | 已解决 |
| ~~Q-21~~ | 已解决（2026-09-23）→ **DEC-19**：**尊重用户停止，不自动恢复**；其连带问题（下次打开应用时的呈现）转为 **Q-30**。 | 已解决 |
| ~~Q-22~~ | 已解决（2026-09-23）→ **DEC-26**：扫描间隔 5 分钟 / 单次发现 ≤60 秒 / 每 24 小时 ≤2 次失败 / 连续 2 次失败即判不通过 / 恢复观察窗口 30 分钟。 | 已解决 |
| ~~Q-23~~ | 已解决（2026-09-23）→ **DEC-20**：P1 失败不阻塞本期；已知限制不阻塞发布。 | 已解决 |
| ~~Q-24~~ | 已解决（2026-09-23）→ **DEC-27**：**授权**新增持久化键（不新增依赖）。 | 已解决 |
| ~~Q-25~~ | 已解决（2026-09-23）→ **DEC-22**：**保持"开"并显示"异常 / 暂不可被发现"**。 | 已解决 |
| ~~Q-26~~ | 已解决（2026-09-23）→ **DEC-23**：**缺失键按"开启"**（覆盖升级后首次启动须给出说明）。 | 已解决 |
| ~~Q-27~~ | 已解决（2026-09-23）→ **DEC-24**：**显示"无法检测（需你自行确认）" + 允许用户自述**。 | 已解决 |
| ~~Q-28~~ | 已解决（2026-09-23）→ **DEC-31**：原文已由所有者提供并**逐字补录**于 §2.2 / §2.3，逐条映射核对完毕（含"验收实为 11 项、此前记数错误"的更正）。 | 已解决 |
| ~~Q-31~~ | 已解决（2026-09-23）→ **DEC-32**：**全部 AC 的判据改为"可连接"**（定义：完成一次 prepare-upload 握手，不要求传完文件）；DEC-05 的通过判据部分被取代。 | 已解决 |
| ~~Q-32~~ | 已解决（2026-09-23）→ **DEC-35**：**AC-01 / AC-02 保留双判据**（"可被发现"经局域网发现链路 + "可连接"），其余 AC 仍只判"可连接"。 | 已解决 |
| ~~Q-33~~ | 已解决（2026-09-23）→ **DEC-33**：**开关只控制"后台常驻"** —— 应用在前台时接收能力不依赖该开关（沿用现状）；停止监听只发生在"不在前台 且 开关关闭"时。**DEC-18 据此收窄**（前台关闭不再停监听、不再中断传输）。 | 已解决 |
| ~~Q-34~~ | 已解决（2026-09-23）→ **DEC-34**：**短时点测项零失败**；统计容差（每 24 小时 ≤2 次失败、无连续 2 次失败）**只用于长跑项 AC-03/04/05**。**DEC-26 据此收窄**。 | 已解决 |
| ~~Q-35~~ | 已解决（2026-09-23）→ **DEC-36**：**前台关闭时发现与连接都沿用现状**（关闭只停"后台常驻"）。 | 已解决 |
| ~~Q-36~~ | 已解决（2026-09-23）→ **DEC-37**：**要求解锁前也能恢复**（选项 ②，新增需求）：存储改设备加密存储、清单加 `directBootAware` + `LOCKED_BOOT_COMPLETED`；解锁前能力边界由设计核实；该场景 L1 无法验证，记 L2 真机。 | 已解决 |
| ~~Q-30~~ | 已解决（2026-09-23）→ **DEC-30**：**用户主动打开应用即恢复**（按开关持久化状态启动）；"被用户停止"状态跨重启保持，直到打开应用。**DEC-19 的"下次启动不重启"一句由 DEC-30 取代**（其"不得通过后台机制恢复"仍有效）。 | 已解决 |

> Q-13 / Q-14 / Q-17 的**临时取向**（复核建议、未确认）见 §15 的 A-18：仅为让设计可推进，任何一条被否决时对应设计需返工。
> Q-20…Q-28 由第一次复核（R-01…R-16）新增，Q-29 / Q-30 由第二次复核（V-01…V-16）新增；编号续排以保持既有编号稳定。
> **当前状态（v1.4）**：**Q-16、Q-19…Q-36 已全部解决**（→ DEC-18…DEC-37）——本节又无「设计前必须」的开放项。未确认假设：A-16、A-18、A-19（**实现前须确认**，见 E-18）、A-20；不阻塞开放项：Q-06、Q-11、Q-13、Q-14、Q-17。
> **设计阶段的交付物（不是开放项）**：① 服务端生命周期状态机（DEC-33）；②「可连接」探测规程（§14.5）；③ 追踪矩阵（§14.2.1）；④ **解锁前能力边界的核实结论**（DEC-37，含存储方案与 `directBootAware` 清单改动）。

---

## 17. 与 `AGENTS.md` 的偏差与冲突记录

> 依据 `AGENTS.md` §10：指出冲突与影响，不静默修改。

| 编号 | 冲突/偏差 | 状态 |
|---|---|---|
| D-01 | §5 规定需求 ID 格式为 `REQ-<域>-<三位序号>`，与本次使用的 `REQ-001…` 冲突。 | **已解决（DEC-09）**：已按授权修订 `AGENTS.md` §5 为 `REQ-<三位序号>` / `NFR-<三位序号>`，并把该节内的需求正文路径引用由 `requirement.md` 更新为 `docs/requirements.md`。 |
| D-02 | §1 的文档入口表曾写需求文档为 `requirement.md`（目录未指定），实际为 `docs/requirements.md`；§2 第 3 条也引用了 `requirement.md`。 | **已解决（DEC-15）**：已按授权把 `AGENTS.md` §1 入口表与 §2 第 3 条的路径引用更新为 `docs/requirements.md`。 |
| D-03 | §3 规定默认只改 `app/`，改 `packages/` 需显式授权。 | **已解决（DEC-01）**：已获授权修改 `packages/localsend_isolates/**`。仍然**不**授权改 `packages/core`、`server/`、`cli/`（§5.2）。 |
| D-04 | §4 规定构建/测试命令在"工程初始化并实测后回填"，当前不可用；而本文件 §14 要求验收。 | **已解决（DEC-16）**：验收分层后，L1 自动化验收是任务完成判据、L2 真机验收仅发布前；工具链缺口改为 §14.6 的安装清单 + Q-19 的授权，**不再视为任务阻塞**。`AGENTS.md` §4 的命令回填条件仍待工具链安装并实测后满足。 |
| D-05 | 本文件新增了原想法之外的条目：REQ-011（不回归，源自原始想法 4，属落地而非扩充）、AC-12/AC-13（由 REQ-008/REQ-006 推导，用于可验证性）、BR-01…BR-09（对已有要求的归纳）、E-01…E-17（边界情况展开）、§3.4 平台核对（事实而非需求）。 | 无功能扩充。如认为其中任一超出你的意图，请指出，我移除。 |
| D-06 | 本文件与 `AGENTS.md` 的**默认行为变更**值得显式记录：BR-02 使应用首次启动即常驻并有通知，这是对既有默认行为的改变，与"不改变软件其他功能"（原始需求 4）在**默认值层面**存在张力 —— 该张力由 DEC-10 明确选择"默认开启"解决，且 REQ-011 保证功能语义不变。 | 已记录，无需处理；仅提醒后续复核者。 |
| D-07 | `README.md`（行 315–322）仍写"目标用户 / 要解决的问题 **待确认**"，并称"本 fork 未对这两份文档做改动、其中的命令与约束继续有效"；前者与现状（需求文档已确认、`AGENTS.md` 已含本 fork 规则）冲突。另：README 把 v1.18.2 的 `230fb692` 记为 fork 基线（**正确**，但本文件 v0.4 曾把代码基线写作 `fe5e5929`）。 | **未解决（复核 R-14）**。README **不在本次修订范围**，**未修改**；请授权后再改。本文件已把三种基线分开标注（头部表、NFR-010）。**处置层级（v0.6，复核 V-16）：进入实现前必须统一，不阻塞需求与设计。** **第三轮复核 W-08 重申**：README 与 `AGENTS.md` 的过期记录**不得带入实现阶段**。 |
| D-08 | `AGENTS.md` §11 的"环境现状"仍记录工具链全部缺失（`fvm`/`flutter`/`dart`/`cargo`/`rustc`/JDK/Android SDK），与本次实测不符（§14.6）。 | **本文件已修正**（§14.6 重测并注明偏差）；`AGENTS.md` 属协作规则文档，**未修改**，需单独授权。**处置层级（v0.6，复核 V-16）：进入实现前必须统一，不阻塞需求与设计** —— 否则执行者可能按过期环境结论判断命令可用性。**第三轮复核 W-08 重申**：这是一条会真实误导执行者的偏差（工具链已就位却被记为不可用）。 |

---

## 18. 参考资料

平台约束（§3.4）与平台行为（REQ-003、REQ-006、§14.3）的来源，核对日期 2026-09-23：

1. 前台服务的变更（含 Android 15+ 的 `BOOT_COMPLETED` 启动限制、`dataSync` / `mediaProcessing` 运行时长上限、**Android 16 起由前台服务发起的后台作业配额** → §3.4 P-06）：<https://developer.android.com/develop/background-work/services/fgs/changes>
2. 前台服务类型（含 `connectedDevice` 的权限与前置条件、各类型的超时 → §3.4 P-01…P-05）：<https://developer.android.com/develop/background-work/services/fgs/service-types>
3. Android 15 前台服务类型的变更（`dataSync` / `mediaProcessing` 的超时）：<https://developer.android.com/about/versions/15/changes/foreground-service-types>
4. **用户停止前台服务应用**（Android 13+ 的 Task Manager / "活动应用" Stop：终止整个应用、移除通知、**无回调**；建议检查 `ApplicationExitInfo.REASON_USER_REQUESTED`；官方测试命令 `cmd activity stop-app`）→ REQ-003、§14.3、DEC-19 + DEC-30：<https://developer.android.com/develop/background-work/services/fgs/handle-user-stopping>
5. Android 15 行为变更（**stopped state 只能由用户直接或间接操作解除**；进入该状态时取消全部待处理 intent）→ §14.3：<https://developer.android.com/about/versions/15/behavior-changes-all>
6. Direct Boot（`ACTION_BOOT_COMPLETED` 对**非** direct-boot-aware 应用在**用户解锁后**投递；`LOCKED_BOOT_COMPLETED` 与设备加密存储）→ REQ-006.5、**DEC-37**、§10.1：<https://developer.android.com/privacy-and-security/direct-boot>

> 以上为文档核对结论；**§3.4 的每一条都需在目标真机（Android 16）上复核**，复核结果回填本节。
> 第 4、5 条由 v0.5 的复核响应（R-06、R-16）新增；第 4 条中的测试命令**尚未在本机实测**（§14.6）。

---

## 19. 附：术语

| 术语 | 含义 |
|---|---|
| 后台接收 | 本需求引入的用户可选能力：应用在后台长期保持发现与监听（REQ-001…REQ-003） |
| 开机自启 | 独立的用户开关：控制设备重启后是否自动恢复后台接收（REQ-006） |
| 常驻服务 | 承载后台接收的长期前台服务，类型为 `connectedDevice`（REQ-002） |
| 传输期服务 | 现状已有的 `dataSync` 前台服务，仅在传输进行中使用（§3.2，DEC-02 下保持不变） |
| 可被发现 | 同网段设备在任意时刻发起扫描都能**发现**本机（广播/组播注册可达）。**AC-01 / AC-02 的验收判据之一**（**DEC-35**，须经局域网发现链路验证，不得用端口转发/直连绕过）；其余 AC 中仅作现象描述 |
| 可连接 | 对端能对本机**完成一次 prepare-upload 握手**（"发起传输 → 本机应答到'准备上传'"），**不要求传完文件**。**本组 AC 的验收判据**（AC-01/AC-02 与"可被发现"并列）—— §14.5、DEC-32、DEC-35 |
| 引导 | REQ-009 的一次性设置引导（电池优化、后台不受限、国产 ROM 自启） |
| 已知限制 | 由系统/ROM 行为导致、已实测记录的能力上限（BR-04） |

---

## 20. 独立复核意见的处理记录（2026-09-23，两轮）

> 来源：两轮独立只读复核 —— 第一轮 R-01…R-16（结论"不通过"，处理见 §20.1–§20.5），第二轮 V-01…V-16（结论"不通过"，处理见 §20.6）。
> **阅读须知（v0.7）**：§20.1–§20.6 是**当时的处理记录**，其中写有"待你定 / 待确认"的事项，其后已于 2026-09-23 全部选定。**当前状态以 §16.2（唯一开放项清单）与 §20.7（决定记录）为准**，本节仅作历史。
> 处理原则：**能由事实判定的一律修正；需要业务取舍的一律不替你决定**，改为写入 §16.2 并标注「设计前必须」。
> 涉及外部平台行为的，均以官方文档核对（§18）；涉及本机事实的，均以本次实测为准（§14.6）。

### 20.1 逐条处理（问题 ID → 修改位置 → 结论）

| ID | 修改位置 | 处理结论 |
|---|---|---|
| R-01 | §2（拆为 2.1/2.2/2.3 + 可审计性说明）；§16.2 Q-28 | **成立**。全仓检索确认原始 10 条实现要求与 10 项验收标准的原文**不在仓库内**，故删除"确保无遗漏、无扩写"的断言，改为可审计性说明 + 占位表待你补录；"10 项 → 11 条 AC"的对应关系列入 Q-28。**映射本身未改动**（不凭记忆编造原文）。 |
| R-02 | REQ-002 第 3/4 条改写、新增资源归属表与冲突说明；§16.2 Q-20 | **成立**。已消除"完全分离"与"共用同一常驻主体"的歧义（= 传输期间常驻服务不中断，而非把传输期服务并入）；新增资源级归属表。**REQ-008 与 E-12 在"HTTP 服务端为进程级单实例"前提下确实冲突**，列入 Q-20（设计前必须），未替你选择。 |
| R-03 | §14.4 判定口径新增"可测化缺口"；AC-07 行；§16.2 Q-22 | **成立**。"离散观测 vs 任何时间"的矛盾已显式写出；另发现并记录：AC-07 的"不设门槛"**超出 DEC-07 原文范围**（DEC-07 只覆盖耗电/资源阈值与 REQ-007 恢复时限）。阈值参数须你给出（Q-22）。 |
| R-04 | REQ-007、REQ-010 新增"判定闭合"；AC-11 行；Q-22 | **成立**。原文"不设时限"使验收不可终止；已改为"不设阈值、但必须设观察窗口"，窗口口径待你给（Q-22）。 |
| R-05 | §6.1 新增"三套门槛"；§16.2 Q-23 | **成立**。任务完成 / 本期完成 / 发布放行 已显式分开；P1 失败与"已知限制"各自的阻塞性待你定（Q-23）。 |
| R-06 | REQ-003（删除"唯一途径"、新增三条途径与可划掉性）；§8.3；§14.3（停止语义三分 + 新命令）；§14.4 AC-11；§18 第 4/5 条 | **成立，已按官方文档核对修正**。① Android 13+ 系统"活动应用"Stop 会终止整个应用、移除通知且**无回调**，应用只能靠 `ApplicationExitInfo.REASON_USER_REQUESTED` 事后识别，官方测试命令为 `cmd activity stop-app`；② `am force-stop` 使应用进入 stopped state，Android 15 起**仅用户操作可解除**且进入时取消全部待处理 intent ⇒ **不得**用于恢复类验收；③ `cmd activity stop-app` **本机未实测**，已按 `AGENTS.md` §4 标注"需实测确认"。**"用户停止后是否自动恢复"需你定（Q-21）**。 |
| R-07 | §16.2 Q-26 | **成立**。原文确实只规定"首次安装/首次启动"的默认值。首次安装 / 覆盖升级 / 清除数据 三种情形已列出选项，取舍需你定（Q-26）—— 未替你决定。 |
| R-08 | §8.3（补 4 行状态 + "生效中"口径）；§16.2 Q-25 | **成立**。已补入"无局域网 / 恢复中 / 监听不可用 / 被系统用户停止"四种状态，并明确"生效中"= 服务运行 **且** 监听可用；四处呈现方式未定者列入 Q-25，未统一写成"生效中"。 |
| R-09 | REQ-009 新增"可检测性边界"与三态取值；§10.1；§15 A-17；§16.2 Q-27；§14.4 前置条件 1 | **成立**。三项状态改为**已满足 / 未满足 / 无法检测**；"跳转 ≠ 已获准"、"系统实时状态与用户已读/已跳过分离"已写入。**这改变了 REQ-009 的可观察结果与 L2 前置条件表述 → 需你追认**（见 20.3）。 |
| R-10 | 文档头部状态；§16.2 标题与 Q-16/Q-14；§5.2 新增一行；§15 A-18 | **成立**。文档状态改为"需求正文已确认，设计前必须落定项未定"；**Q-16 标为「设计前必须」**；"暂停接收"按 `AGENTS.md` §10（未确认的新需求不实现）明确不进入本期；Q-13/Q-14/Q-17 的临时取向记为 A-18。 |
| R-11 | §14.2 M-07；新增 §14.2.1 最小覆盖集与追踪矩阵；NFR-006；NFR-010 | **成立**。M-07 的循环判据改为"任务**预先声明**的断言全部通过"；最小覆盖集（9 类场景）与"追踪矩阵"列为设计要求；非 Android 构建检查因 VS 组件缺失**当前不可执行**，已明确只能写"未验证 + 原因"。 |
| R-12 | §14.6 重测重写；§16.2 Q-19 重述；§17 D-08 | **成立，且其事实前提已变化**。本次实测：工具链**已就位**（`fvm` + Flutter 3.41.9 / `cargo`+`rustc` 1.97.1 / JDK 17 / Android SDK 36 + AVD `localsend-api36`），原"全部缺失"记录作废；仍缺：SDK 许可未接受、模拟器实例 offline、VS C++ 工作负载、`flutter_rust_bridge_codegen`。Q-19 已从"授权安装"改为"**授权执行构建/测试与许可接受**"，并明确它**不阻塞设计、但阻塞实现任务的验收完成**。 |
| R-13 | §10.1（候选 vs 已确认）；§15 A-14；§16.2 Q-24 | **成立**。`shared_preferences` 与引导状态的取值形态已标为**候选方案**（由设计确定），已确认的只是"不新增依赖、不上传、不写外部存储"；新增持久化键是否属 §8 的"持久化格式变更"列为 Q-24，**未单方判断**。 |
| R-14 | 文档头部（三种基线）；NFR-010；§17 D-07/D-08 | **成立**。已区分上游代码基线 `230fb692` / 协作规则基线 `fe5e5929` / 本文档基线 `b8f4900c`；README 的偏差记入 D-07 但**未修改**（不在本次修订范围），需授权。 |
| R-15 | §13.1 新增分类；NFR-007 测量条件；NFR-006 / NFR-010 可验证方式 | **成立**。NFR-002/003 明确为**证据采集型、不参与质量放行**；NFR-007 改为 5 条可枚举问题；NFR-006/NFR-010 由主观表述改为可枚举检查项。 |
| R-16 | REQ-006 新增第 5/6 条；AC-10；§15 A-15/A-16；§18 第 6 条 | **成立，已按官方 Direct Boot 文档核对**：**非** direct-boot-aware 应用的 `BOOT_COMPLETED` 在**用户解锁后**才投递，且 `shared_preferences` 位于凭据加密存储 ⇒ 本期只承诺"**首次解锁后**恢复"（A-15）；覆盖安装后是否恢复列为 A-16。**AC-10 判定口径据此改变 → 需你追认**（见 20.3）。 |

### 20.2 未采纳或未完全采纳的意见及依据

| 意见 | 处理 | 依据 |
|---|---|---|
| R-01 称可能"少一项、重复一项或夹带推导项" | **部分不采纳**：AC-12/AC-13 在 §14.4 已明确标注为"由 REQ-008/REQ-006 推导"，故"夹带推导项"不成立；真实问题只有"原始 10 项与 AC-01…AC-11 的逐项对应无载体"。 | §14.4 原文；§2.3 补充说明 |
| R-06 建议用 `cmd activity stop-app` 作为用户停止的测试手段 | **采纳为候选，但不标为"可用"**：本机未执行过该命令，按 `AGENTS.md` §4 只能写"需实测确认"。 | `AGENTS.md` §4；§14.3、§18 第 4 条 |
| R-11 要求补"非 Android 构建命令" | **部分不采纳（无法补）**：本机缺 Visual Studio 的 C++ 工作负载，非 Android 桌面构建**不可用**；补一条跑不通的命令违反 `AGENTS.md` §4。改为标注"未验证 + 原因"，并保留可执行的静态隔离检查。 | §14.6 实测输出；NFR-006 |
| R-12 称 D-04 与 Q-19 自相矛盾 | **采纳其区分**（不阻塞设计 / 阻塞实现验收），但指出 D-04 的事实前提已因环境变化而部分消失（工具链已就位）。 | §14.6 实测 |
| R-14 要求修 README | **记录但未执行**：本次只修改目标文档；README 需单独授权。 | §17 D-07；`AGENTS.md` §10 |

### 20.3 影响已确认需求或关键设计的改动（**已于 2026-09-23 追认**）

以下 6 处改变了此前"已确认"的表述。**状态：已由仓库所有者在 2026-09-23 追认**（当时选择"追认这 6 处改动并提交"，见提交 `2bf1cc3f` 的提交信息）；v0.6 在此补记该追认，以消除"头部称已确认、本节称待追认"的矛盾（复核 V-03）。每处都集中在对应小节，日后若要撤销，回退成本仅为还原该段：

| # | 改动 | 位置 | 原表述 → 现表述 |
|---|---|---|---|
| 1 | 开机恢复时刻 | REQ-006.5、AC-10、A-15 | "系统启动完成后" → "**用户首次解锁后**"（平台事实） |
| 2 | 引导状态取值 | REQ-009、§14.4 前置条件 1、A-17 | "已满足/未满足" → "**已满足/未满足/无法检测**"；L2 前置由"三项均显示已满足" → "三项均无'未满足'" |
| 3 | Doze 延迟门槛 | AC-07、§14.4 判定口径 | "记录延迟、不设门槛（DEC-07）" → "**门槛待定（Q-22）**"，并指出原表述**超出 DEC-07 原文范围** |
| 4 | 恢复时限 | REQ-007、REQ-010、AC-11 | "不设时限" → "**不设阈值，但必须设观察窗口**"（否则验收不可终止） |
| 5 | 文档状态 | 头部表 | "已确认" → "**需求正文已确认；设计前必须落定项未定**" |
| 6 | REQ-002 资源归属 | REQ-002 资源表 | DEC-02 的"分离"具体化为资源级归属；其中"HTTP 服务端 + 发现机制"一行标为**冲突点**（Q-20），未决定 |

### 20.4 仍需你决定的问题（**唯一清单在 §16.2**）

本节**不再维护第二份清单**（v0.6 修订，依据复核 V-04）。判定以 **§16.2 表格中的逐行标记**为准；此处只做统计（**v0.7 更新**）：

- **已解决（2026-09-23）**：**Q-16、Q-19…Q-34 全部解决** → 落为 **DEC-18…DEC-35**（选择过程见 §20.7 / §20.8 / §20.10 / §20.11 / §20.13）。
- **「设计前必须」：无开放项**（v1.2 起）。
- **不阻塞**：Q-11（L2 机型）、Q-13 / Q-14 / Q-17（A-18 的临时取向）、Q-06（写翻译文件前授权）。

### 20.5 本次修订未做的事（明确边界）

- **未修改任何代码**，未执行构建、测试或模拟器操作（`AGENTS.md` §6 需单独授权）。
- **未改动 `AGENTS.md`、`README.md`**（分别记入 D-08、D-07，需授权）。
- **未替你做任何业务取舍**：§16.2 的开放项只列选项，不预设结论；§15 的 A-15…A-18 明确标为未确认。
- **未补录原始条目原文**（原文不在仓库内，不能凭记忆编造，见 §2.3）。

### 20.6 第二轮复核（V-01…V-16）的处理记录（v0.6）

| ID | 修改位置 | 处理结论 |
|---|---|---|
| V-01 | §2 的可审计性说明、§2.1 标题与表下注 | **成立**。全仓检索确认 5 条原始想法**只在 §2.1 出现**、`README.md` 没有记录它们 ⇒ v0.5 的"README 有载体"说法**有误，已更正**；§2.1 改标为"本文件转录，待所有者再次确认"（并入 Q-28 第 ① 组）。 |
| V-02 | §16.2 Q-28 | **成立**。Q-28 已扩写为**三组**（5 条想法 / 10 条实现要求 / 10 项验收标准）分别补录并逐项映射，避免第 ② 组被遗漏。 |
| V-03 | 头部表状态行、§20.3 引言 | **成立，但按事实采用另一种修法**。所有者已于 2026-09-23 追认那 6 处改动（提交 `2bf1cc3f` 的信息可查），故不是"待追认"而是"**已追认但文档未记录**"—— v0.6 在头部与 §20.3 双处记录该追认，并明确"**除 §16.1 与已追认的 6 处外，正文不构成已确认需求**"，状态改为"**修订稿 v0.6，尚不宜进入设计**"。 |
| V-04 | §16.2 新增"标记图例"、逐行补标记（Q-19、Q-23…Q-29）、§20.4 改为指向 | **成立**。标记已逐行统一（「设计前必须」/「实现前必须」/无标记），§20.4 不再维护第二份清单，判定以 §16.2 为准。 |
| V-05 | §16.2 Q-20 | **成立**。三个方案保持并列（无默认值），并明确：选 ① 将构成对 **REQ-011 的显式例外**，需单独确认。取舍待你选（见 §20.7）。 |
| V-06 | §16.2 Q-21 | **成立**。不替你决定；两条选项各自的连带后果已写清（选 ① 需给 REQ-004/REQ-006 加"用户主动停止除外"的例外；选 ② 需说明与 BR-01 如何共存）。取舍待你选。 |
| V-07 | §14.5 通过/失败定义 | **成立**。§14.5 已改为**以 Q-22 为条件**：Q-22 落定前只产出证据、不给"通过"结论（消除 v0.5 的两套口径）；落定后按四个参数重写。 |
| V-08 | §6.1 表下注 | **成立**。"本期可以在存在已知限制时结束"是**未确认的断言**，已删除，改为"取决于 Q-23，未定前不得把未定项写成规则"。 |
| V-09 | §16.2（Q-16、Q-25、Q-26、Q-27 标「设计前必须」） | **成立**。四项均已在 §16.2 标为设计前必须，并列入 §20.7 的选择题；A-17/A-18 仍为**未确认假设**，不作规范依据。 |
| V-10 | REQ-001、REQ-006.1/6.5、§10.1 | **成立**。三处矛盾均已修正：① 正文不再指定 `shared_preferences`（持久化是需求、存储是设计选择）；② "已确认不新增依赖"与 DEC-01 冲突 ⇒ 更正为"**允许**新增依赖，但不无必要地新增"；③ 补充"首次解锁后恢复"结论**依赖凭据加密存储**这一前提，存储若变需重评。 |
| V-11 | NFR-006 新增"允许的改动范围"、NFR-010 更换比较基准 | **成立**。NFR-006 给出显式允许清单（含 NFR-009 要求的 `docs/**`、`app/assets/i18n/**`、`app/lib/gen/**` 与测试）；NFR-010 改为比较"本任务起点..HEAD"，并声明与 `230fb692` 的整体 diff **不是**判据。 |
| V-12 | AC-06 行、§14.3 的 L2 表 | **成立**。AC-06 删去 `am force-stop` 近似，改为"无可靠近似：静态检查 + L2 真机"；同义说明并入 §14.3 的"模拟器不可靠覆盖"表。 |
| V-13 | §16.2 新增 Q-29、Q-19 增加第 ④ 项、NFR-006 ③ 的执行层级 | **成立**。给出三条路：降为发布门槛 / 保留每任务门槛（需先装 VS 工作负载或指定 CI）/ 静态替代但记明不等价。取舍待你选（Q-29）。 |
| V-14 | §8.3 新增过渡态行、新增 E-18、§15 A-19 | **成立，按"假设"处理**（复核亦建议可带假设推进）："最后一次明确用户意图优先 + 幂等"记为 **A-19（未确认）**，并要求在设计状态机中显式验证；未当作已确认需求。 |
| V-15 | §10.2 两行改为"候选新增"、表下新增读法说明 | **成立**。`REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` 与 `CHANGE_WIFI_MULTICAST_STATE` 均改为「候选新增（由设计确定）」，并要求设计给出最小权限集与"不声明/被拒时的行为"。 |
| V-16 | §17 D-07 / D-08 | **成立（仅记录层级）**。两项均已标注"**进入实现前必须统一，不阻塞需求与设计**"；README 与 `AGENTS.md` 本次**仍未修改**（不在授权范围）。 |

**未采纳的意见**：无整条未采纳项。唯一与复核建议不同的一处是 **V-03 的修法** —— 复核建议把状态降为"修订稿待确认"，本文件改为"确认已发生、只是未记录"（依据：所有者 2026-09-23 的追认可查于提交 `2bf1cc3f`），同时把状态明确写成"修订稿"，比 v0.5 的"需求正文已确认"更保守。

> **后续（v0.7）**：上面 V-05 / V-06 / V-09 / V-13 中标注"待你选"的项，已于 2026-09-23 全部选定，见 §20.7 与 DEC-18…DEC-29。

### 20.7 第二轮引出的取舍：**已决定**（2026-09-23，确认人：仓库所有者）

以下 12 项由所有者逐项选定，已回填 §16.1（DEC-18…DEC-29）与相应正文：

| 对应 | 问题 | **你的选择** | 落为 |
|---|---|---|---|
| Q-20 | 关闭「后台接收」时共享 HTTP 服务端 / 监听的处置 | **①立即停止**（接受中断进行中的传输） | DEC-18（+ BR-10：REQ-011 的显式例外） |
| Q-21 | 系统"活动应用"Stop 后是否自动恢复 | **①尊重用户停止，不自动恢复** | DEC-19（连带问题转 **Q-30**） |
| Q-23 | P1 失败与"已知限制"阻塞到哪一层 | **①P1 不阻塞本期；已知限制不阻塞发布** | DEC-20（§6.1 三套门槛） |
| Q-16 | 引导是否允许跳过 | **①允许跳过** | DEC-21 |
| Q-25 | 服务已启动但监听不可用时的开关呈现 | **①保持"开" + 显示"异常"** | DEC-22（§8.3、§10.3） |
| Q-26 | 覆盖升级后缺少新键时的默认值 | **①缺失键按"开启"** | DEC-23（三种初始化情形；升级后须说明） |
| Q-27 | 引导第 3 项不可检测时的呈现 | **①"无法检测" + 用户自述** | DEC-24（A-17 已解决） |
| Q-29 | NFR-006 的非 Android 构建检查属于哪一层 | **①降为发布门槛** | DEC-25（§14.1、§14.2、NFR-006） |
| Q-22 | "可被发现"的可测参数 | **①实际口径**：扫描间隔 5 分钟 / 单次 ≤60 秒 / 每 24 小时 ≤2 次失败 / 连续 2 次失败判不通过 / 恢复窗口 30 分钟 | DEC-26（§14.4、§14.5） |
| Q-24 | 新增持久化键是否授权 | **①授权新增** | DEC-27（§10.1） |
| Q-19 | 是否授权执行构建 / 测试 / 许可接受 | **①全部授权** | DEC-28（§14.6） |
| Q-28① | §2.1 五条原始想法是否准确 | **①确认无误** | DEC-29（§2.1 已标注"所有者确认"） |

**仍未决定**：**无** —— Q-28 → DEC-31（§20.10）、Q-31 → DEC-32（§20.11）、Q-30 → DEC-30（§20.8）。当前开放项见 §16.2 的"当前状态"行。

### 20.8 第三轮：Q-30 的选定（v0.8，2026-09-23）

| 对应 | 问题 | **你的选择** | 落为 |
|---|---|---|---|
| Q-30 | 被系统"活动应用"Stop 后，"下次打开应用"的语义与呈现 | **③ 下次打开应用即自动恢复** | **DEC-30**：应用启动时按开关的持久化状态恢复常驻与监听；"被用户停止"状态**跨设备重启保持**，直到用户打开应用为止 |

**由此产生的修订（显式记录，不静默改动）**：DEC-19 原有一句"应用下次启动时**不得**静默重启常驻服务"，与 ③ 直接冲突 —— 已**收窄 DEC-19**：其"不自动恢复"现在只约束**后台机制**（开机自启、恢复重试、网络变化），"用户打开应用"这条路径由 DEC-30 规定。受影响正文：REQ-003 途径 3、REQ-004 例外、REQ-006.2 例外、§8.3。
**语义提示（供复核确认无歧义）**：用户从系统停止后 —— 应用未运行期间**不会**自行回来（尊重停止，Q-21/DEC-19 仍然有效）；**用户主动打开应用**这一次动作即视为重新表达意图，常驻随之恢复。若你的本意是"打开应用后仍需用户再手动开一次开关"，请指出，那需要把 DEC-30 改为另一条规则。

### 20.9 Q-28 的完整说明（**已于同日关闭 → 见 §20.10；本节保留当时的说明**）

**要解决的问题**：本文件 §2 声称把"仓库所有者提出的原始条目"逐条落位，但**这些原始条目从来没有进入过仓库** —— 它们是在对话里提出的。因此现在有两组内容**只有映射关系、没有原文**：

| 组 | 文件里的现状 | 缺什么 | 影响 |
|---|---|---|---|
| **② 10 条实现要求** | §2.2 只有合并映射（"实现要求 1→REQ-002、2→REQ-003…"）与 10 行占位表 | 每条要求的**原文** | 无法核对是否"一对多 / 一对零 / 重复"；若其中一条**没有被任何 REQ 覆盖**，本期就漏了一条需求，而设计阶段会照着不完整的集合设计 |
| **③ 10 项验收标准** | §2.3 只有范围"AC-01…AC-11"与 10 行占位表 | 每项的**原文** | 原始 **10** 项 vs 现有 **11** 条 AC，数量不符：可能是其中一项被拆成两条，也可能是两条被合并、或某条被改写。不确认就无法说明"11 条与原始 10 项的关系" |

**为什么这被列为「设计前必须」**：`AGENTS.md` §9 要求"无证据的需求视为未完成"，§10 要求"缺失的关键信息集中提问"；本文件 §2 又给了"确保无遗漏、无扩写"的承诺 —— 在没有原文的情况下这个承诺**无法被任何人验证**（第一轮复核 R-01、第二轮复核 V-01/V-02 都因此判定不通过）。设计一旦开始，后续改动成本会显著高于现在补录。

**你可以怎么处理（三种，任选其一即可）**：

1. **补录原文**：把两组原文（逐字或等义均可）贴给我，我填入 §2.2 / §2.3 的占位表并逐条核对映射，Q-28 随即关闭。这是唯一能让"无遗漏、无扩写"重新成立的做法。
2. **以本文件现有内容为准**：即宣布 §2.2 的合并映射与 §14.4 的 AC-01…AC-13 列表**就是**需求集合本身，同时**删除"原始 10 条 / 10 项"这一出处主张**（改为"本期需求由本文件定义"）。代价：放弃"与原始条目一一对应"的可审计性，但文档不再含无法验证的断言。
3. **保留为已知缺口**：§2 明确标注"原文不可考、映射未经核对"，Q-28 转为不阻塞项。代价：后续复核者会反复提出同一问题，且漏项风险由设计/实现阶段承担。

> 三种选择都不影响已确认的 REQ/NFR/AC 内容本身 —— 它们只决定"这些条目与原始表述的对应关系是否可被证明"。

### 20.10 第四轮：原始条目原文补录（v0.9，2026-09-23）

| 对应 | 结果 | 落为 |
|---|---|---|
| Q-28 | **关闭** —— 所有者提供了"后台运行策略"的实现要求（10 条）与验收标准原文 | **DEC-31**（原文见 §2.2 / §2.3） |

**核对发现（其中第 2 条改写了此前两轮的判断）**：

1. **10 条实现要求**：与 REQ / §5.2 / NFR-001 的映射**逐条一一对应** —— 无一对多、无一对零、无重复、无未覆盖；v0.4 的合并映射经原文核对**完全正确**。
2. **原始验收标准实为 11 项，不是 10 项**：与 AC-01…AC-11 **逐条同序对应**。因此第一轮 R-01、第二轮 V-02 与 §20.9 反复质疑的"原始 10 项 vs 现有 11 条 AC 数量不符"，**根因是本文件自身的记数错误**，并非拆分或合并所致。已更正 §2.3 及相关表述；AC-12 / AC-13 仍为推导项（§14.4 已标注）。
3. **唯一的实质差异**：原始第 3、4 项写"仍可**连接**"，而 DEC-05（已确认决策）把通过判据定为"可**被发现**"。二者强弱不同 ⇒ 当时新增 **Q-31**（设计前必须），**未替所有者选择**；该问随后由所有者选定（**DEC-32**，见 §20.11）。
4. **§2.2 的两处展开属"已记录的展开"、非隐藏新增**：原文第 9 条写"**等**明显滥用系统机制的方案"，本文件在 §5.2 / NFR-001 中展开出"多进程互相守护""伪造前台通知的空壳服务"两项，理由已写在 §5.2。

**本次补录未改动任何需求内容**：只填写 §2.2 / §2.3 的原文与核对列，并更正 §2.3 的记数；**未新增、删除或改写任何 REQ / NFR / AC**。

### 20.11 第五轮：Q-31 的选定（v1.0，2026-09-23）

| 对应 | 问题 | **你的选择** | 落为 |
|---|---|---|---|
| Q-31 | AC-03/AC-04 及其余 AC 的判据用"可被发现"还是"可连接" | **③ 全部改为"可连接"** | **DEC-32** |

**判据定义**：**可连接** = 对端能对本机**完成一次 prepare-upload 握手**（"发起传输 → 本机应答到'准备上传'"），**不要求传完文件**。该定义沿用你在选项 ②③ 中看到的口径；**若你要的是更强的"能传完一份文件"**，请指出 —— 那会把每个观测点的成本提高一个量级，并需重写 §14.5 与 §14.3 的手段列。

**对已确认决策的修订（均在原行内标注，未静默改写）**：

- **DEC-05**："通过判据仅为可被发现"一句**由 DEC-32 取代**；其"不要求完成传输"**继续有效**。
- **DEC-26**："单次发现 ≤60 秒"按 DEC-32 读作"单次**可连接**确认 ≤60 秒"。

**连带更新的正文**：§1.3 成功定义、§14.4 判定口径与 AC-01…AC-12 的判定列、§14.5 判据与通过/失败、§14.3 新增判据手段行、§19 术语新增"可连接"。

**代价与收益（一并记录，便于日后回看）**：

- **收益**：判据由"可发现"升为"可连接"后，**"服务已启动但监听不可用"这类假达标无法再通过验收**，与 DEC-22（显示"异常"）形成闭环；也与原文第 3、4 项的措辞一致。
- **代价**：AC-01…AC-11 的每个观测点都要多做一次握手；长跑项（AC-03/04/05）的观测成本与失败面都随之上升。这是本决策的固有取舍，已在 §14.5 注明。

### 20.12 第三轮复核（W-01…W-08）的处理记录（v1.1，2026-09-23）

| ID | 修改位置 | 处理结论 |
|---|---|---|
| W-01 | §14.4 的"强度提示"段、§14.5 判据定义；新增 **Q-32** | **成立**。v1.0 据"可连接比可发现强"推断"同时覆盖发现"，**该推断错误**（经端口转发/直连探测完全绕过组播发现链路）—— 已删除该推断并写明反例；"发现是否仍需单独验收"列入 **Q-32**（设计前必须），**未替你决定** |
| W-02 | §14.5 新增「探测规程」（5 条强制性质） | **成立**。已写明：探测是**真实接收请求**且会占用**唯一的**上传会话；规程必须**可重复 / 可清理 / 让位于真实传输（记为"跳过"不判失败）/ 成功=协议合法应答 / 与 AC-05"无交互"的关系**；**设计阶段必须先产出该规程**，否则 L1 无法执行；若无法同时满足则须回到本文件重定判据，不得静默降级 |
| W-03 | REQ-001 新增"前台关闭"边界；新增 **Q-33** | **成立**。原文只把"中断进行中的传输"列为例外，**未覆盖"应用在前台时关闭会连带停掉前台接收"**（因服务端是进程级单实例）—— 是否一并禁用列入 **Q-33**（设计前必须），未替你决定 |
| W-04 | REQ-001 的"在传输进行中关闭"边界 | **成立，已按 DEC-18 修正**：原文写"仅关闭常驻语义、进行中的传输继续"，与 DEC-18"立即停止、传输中断"**正好相反**；已统一并就地标注更正（未重新取舍） |
| W-05 | 新增 **Q-34** | **成立**。AC 表的"观测点均可连接"与 §14.5 的统计容差（每 24 小时 ≤2 次失败）对**短时 AC** 口径不一，同一份记录可两判 —— 取舍列入 **Q-34**（设计前必须） |
| W-06 | §9 的 BR-10 | **成立，已修正**：删除"其余**任何情形**都不得中断进行中的传输"这一**应用无法保证的绝对承诺**，改为"**本次新增的逻辑不得主动中断**既有传输"；外部原因造成的中断按现状语义**如实记录** |
| W-07 | §14.5 新增「负向判据的观察窗口」、AC-13 行、§15 **A-20** | **成立，按测试假设处理**（复核亦建议可带假设推进）：负向判据（AC-13 等）同样使用 **30 分钟**观察窗口，记为 **A-20（未确认）**，待设计验收用例确认 |
| W-08 | §17 D-07 / D-08 | **成立（已知偏差，重申）**：README 与 `AGENTS.md` 的过期记录**仍未修改**（不在本次授权范围）；层级保持"进入实现前必须统一，不阻塞需求与设计" |

**未整条拒绝的意见**。唯一与复核建议的写法差异是 **W-01**：复核建议"保留发现专项判据"，本文件改为**先删除错误推断、把选择交回 Q-32** —— 因为"发现是否单独验收"会改变 AC 的判定口径，属所有者的取舍，不是可以顺手改写的文案。（该问随后由所有者选定为 **DEC-35**，见 §20.13。）

### 20.13 第三轮引出的三个取舍：**已决定**（v1.2，2026-09-23）

| 对应 | 问题 | **你的选择** | 落为 |
|---|---|---|---|
| Q-32 | "可发现"是否仍单独验收 | **① AC-01 / AC-02 保留双判据**（发现 + 连接都要过），其余 AC 只判"可连接" | **DEC-35**（§14.4、§14.5、AC-01/02、§19） |
| Q-33 | 关闭开关是否连"前台接收"一并禁用 | **② 只停常驻语义**：前台运行时服务端继续可用；停止监听只发生在"不在前台 且 开关关闭"时 | **DEC-33**（REQ-008 重写；REQ-001/002、BR-10、E-12、AC-12、§8.2/§8.3 同步） |
| Q-34 | 短时 AC 是否允许失败 | **① 短时点测项零失败**，统计容差只用于长跑 AC-03/04/05 | **DEC-34**（§14.4、§14.5、DEC-26 收窄） |

**由这些选择产生的连带修订（均已在原处标注，未静默改写）**：

| 被收窄/重写的旧文 | 原内容 | 现状态 |
|---|---|---|
| **DEC-18**（已确认） | 关闭即停止监听、中断传输 | **只在"不在前台 且 开关关闭"时成立**；前台关闭不停监听、不中断传输（v1.2 按 DEC-33 收窄） |
| **DEC-26**（已确认） | 每 24 小时允许 ≤2 次失败 | **只用于长跑项 AC-03/04/05**；短时项零失败（v1.2 按 DEC-34 收窄） |
| **REQ-008** | "彻底停止"= 停止服务与监听 | 按前台/后台**分两种情形**重写；"彻底停止"指保活组件全停 + "不在前台时"监听停止 |
| **BR-10** | 关闭中断传输 = REQ-011 唯一例外 | 例外限定为"用户主动关闭 **+ 不在前台**"；前台关闭不构成例外 |
| **AC-12** | 关闭后本机不再可连接 | 限定为"**不在前台时**不可连接"（前台打开着仍可连接） |
| **§8.2 / §8.3** | 停止流程只写"服务与通知消失" | 补充"应用在前台时接收仍可用"，并新增一条前台关闭的状态行 |

**代价与收益（一并记录）**：

- **收益（Q-33=②）**：**前台接收完全沿用现状**，REQ-011 少了一处例外 —— 用户在前台关掉开关不会被"顺手剥夺"收文件的能力。
- **代价（Q-33=②）**：服务端生命周期不再由常驻服务单独决定，需要一个新的状态机（前台/后台 × 开关），这是**改变现状的"随进程启动一次"**；已写成设计阶段的交付物。
- **代价（Q-34=①）**：短时项零失败意味着模拟器/环境抖动会直接判失败、需要重跑 —— 换取的是判定不含糊（同一份记录不会再两判）。
- **收益（Q-32=①）**：发现链路的退化不会被"连接成功"掩盖；代价是 AC-01/AC-02 的 L1 依赖 §14.3 中"需实测确认"的模拟器组播可达性，若 L1 无法可靠验证，须记为 L2 或"未验证 + 原因"。

### 20.14 第四轮复核（01…07）的处理记录（v1.3，2026-09-23）

| ID | 修改位置 | 处理结论 |
|---|---|---|
| 01 | REQ-001 可观察结果/正常、REQ-002 资源表与冲突注、BR-06、S-07；新增 **Q-35** | **成立**。DEC-33 之后正文未同步的三处（REQ-001"关闭后不可被发现"、REQ-002 冲突注仍写"关闭即停止监听并中断传输"、BR-06 仍禁"关后仍在监听"）**已按 DEC-33 统一**；**"前台关闭时发现机制是否也沿用现状"未定 → Q-35**（设计前必须），**未替你决定** |
| 02 | NFR-006 的"允许的改动范围" | **成立**。清单**漏了 `app/lib/**`**（而本文件自己把落点写在那里）⇒ 正常实现会被判越界；已补入 **`app/lib/**`（含 `app/lib/gen/**`）与 `app/test/**`**，并保留"非 Android 平台行为不变"的实质约束。NFR-010 引用同一清单，随之生效 |
| 03 | §14.5 的"发现判据的手段与采样" | **成立**。原参数只定义在"可连接"探测上，**发现侧无采样与容差** ⇒ 已补：发现与连接**同一节奏（每 5 分钟）、同一超时（≤60 秒）**，每周期先发现、后连接；短时项**零失败**；观察窗口与项目时长一致。**不得**用连接成功替代发现（DEC-35） |
| 04 | §14.5 新增"三类判据与采样起点"；§14.4 判据行 | **成立**。已把判据分为 **A 正向可达（AC-01…AC-11）/ B 负向不出现（AC-12、AC-13）/ C 恢复窗口（AC-08/10/11 的恢复部分）**，分别给出**采样起点**与**容差**；明确 **AC-12/AC-13 不适用"每次探测都必须成功"**，以及**恢复窗口内的"尚未恢复"不计失败** —— 消除了"同一记录被两类规则判成相反结果" |
| 05 | 新增 **Q-36**；§15 A-15 加注 | **成立**。A-15 已**被写进 REQ-006.5 的承诺与 AC-10 的前提**，因此不能再当"可带走的假设"；已改为**必须先落定**（Q-36，设计前必须）—— 是"确认仅首次解锁后恢复"还是"要求解锁前也恢复（新增需求）"，**由你决定**，文件中**未假定答案** |
| 06 | §11 的 E-18 | **成立**。E-18 原写成命令式规则、与 A-19"未确认假设"矛盾 ⇒ 已改为"**按假设 A-19 建模**"，并写明**实现前须确认 A-19**（否则状态机需另设计） |
| 07 | §17 D-07 / D-08 | **不处理（按文档既定边界）**：README 与 `AGENTS.md` 的过期记录属"进入实现前必须统一"，本轮**仍未修改**（不在授权范围内）。复核结论亦认为不应在只读复核中改 |

**未整条拒绝的意见**。**两处需你决定**（Q-35、Q-36）已在 §16.2 标为「设计前必须」，并保留在未决状态；**其余五处（01 的正文同步、02、03、04、06）均属"统一已确认规则与验收文字"，已直接修完，未新增任何需求**。

### 20.15 第四轮引出的两个取舍：**已决定**（v1.4，2026-09-23）

| 对应 | 问题 | **你的选择** | 落为 |
|---|---|---|---|
| Q-35 | 前台关闭开关后，发现机制是否也沿用现状 | **① 沿用现状**（发现 + 连接都保留） | **DEC-36**（REQ-001/002、BR-06、§8.3） |
| Q-36 | 本期是否只要求"首次解锁后"自动恢复 | **② 要求解锁前也能恢复** | **DEC-37**（**新增需求**；REQ-006.5 重写、AC-10 判据、§10.1 存储、§14.3 的 L1 限制、A-14/A-15） |

**Q-36 = ② 的性质与代价（请留意，这是本批次里唯一的新增需求）**：

- **性质**：原始需求第 7 条只写"设备重启后，在 **Android 规则允许范围内**自动恢复"。本决策把"允许范围"**明确要求到用户解锁之前** —— 属于**加强**，不是对既有条目的解释。因此它**新增**了存储与清单两类改动。
- **代价 1（存储）**：两个开关状态必须**解锁前可读** ⇒ 需改为 / 镜像到**设备加密存储**。原候选方案（"沿用 `shared_preferences` 现有键空间"）**失效**，A-14 随之变更；DEC-27 的键授权覆盖该改动，但**具体方案（改存位置 vs 镜像一份 + 解锁后对齐）留待设计**。
- **代价 2（清单）**：新增 `directBootAware="true"` 接收器监听 `LOCKED_BOOT_COMPLETED`（并用 `ACTION_USER_UNLOCKED` 做解锁后收尾）。
- **未定项（设计阶段必须核实，本文件不预设结论）**：解锁前能否启动前台服务、HTTP 服务端与 TLS 证书是否可用、下载目录是否可写、局域网权限是否可用。**若核实结果是不允许，须回到本文件修订，不得静默降级为"解锁后恢复"。**
- **验收影响**：AC-10 的判据随之变为"**解锁前**即可被发现且可连接"，采样从"系统启动完成（锁屏出现）"起算、窗口 30 分钟（§14.5）。**验证路径待实测核实**（v1.5 更正：先查 `ro.crypto.type` 与锁屏重启行为，能自动化则纳入 L1；确实不能才记 L2）。**若最终只能 L2，则 AC-10 的达成与否在发布前才有定论。**
- **优先级（v1.5 补充，复核 04）**：**"尊重用户停止"（DEC-19 / DEC-30）优先于本条** —— 用户在重启前从系统"活动应用"停止过应用时，**解锁前也不得恢复**（BR-01 同理）。该组合已列入 §14.2.1 的追踪矩阵必测项。

### 20.16 第五轮复核（01…06）的处理记录（v1.5，2026-09-23）

| ID | 修改位置 | 处理结论 |
|---|---|---|
| 01 | REQ-001 可观察结果、REQ-002 的"前台关闭"段 | **成立**。Q-35 已由 DEC-36 落定，但正文两处仍写"见 Q-35 / 未定前不得实现" ⇒ 已改为**直接陈述 DEC-36**（前台关闭时发现与连接都沿用现状）。**不是重新取舍** |
| 02 | REQ-006 可观察结果、§14.5（发现判据采样 + 三类判据表 A 行）、§14.4 判据行、AC-10 行 | **成立**。DEC-37 之后仍有三处停留在"首次解锁后"口径：REQ-006 可观察结果、三类判据表的 A 类采样起点、以及 **AC-10 新增的发现判据没有采样规则** —— 均已统一为"**系统启动完成（锁屏出现）即开始采样、窗口 30 分钟**"，并把 AC-10 纳入双判据（发现 + 连接）。**未保留并列的旧口径** |
| 03 | §14.5 的"通过 / 失败"总则、§1.3 成功定义、DEC-34 澄清 | **成立**。三类判据虽已区分，但紧接着的"通过"总则仍要求 AC-06…AC-13"每次可连接且全部成功"、成功定义也称所有场景都要"发现并连接" ⇒ 已把**总则改为服从三类判据**（A / B / C 各自写通过条件），§1.3 同步说明正向与负向场景；DEC-34 加注"零失败只适用于正向项" |
| 04 | DEC-37 行、AC-10 行、§14.2.1 覆盖表 | **成立（按设计输入处理）**。已明确 **DEC-19 / DEC-30 优先于 DEC-37**（被用户停止则解锁前也不得恢复），并把该组合列入追踪矩阵必测项。**这是检验既有规则、不新增功能** |
| 05 | §14.3 的 Direct Boot 行 | **成立**。v1.4 断言"API 36 模拟器不支持 Direct Boot ⇒ 只能 L2"**过强** —— 官方只把 `sm set-emulate-fbe` 的**模拟模式**限定在 Android 13 及以下，并给出了**文件级加密设备**上的 Direct Boot 测试方法；本机模拟器的加密状态**尚未实测** ⇒ 已改为"**验证路径待实测**：先查加密状态与锁屏重启行为，能自动化则纳入 L1，确实不能才记 L2"，并写明**不得预先断言"L1 必然不可能"** |
| 06 | 无需修改 | **不处理（按复核意见）**：A-16/A-18/A-19/A-20 与 D-07/D-08 的层级已如文档所载 —— 设计可带**显著标记**推进，A-19 在实现前确认，README / `AGENTS.md` 在实现前统一（需授权）。本轮**未修改** |

**未整条拒绝任何意见**；**01～03 属"统一已确认规则与验收文字"，04/05 属"按既有规则补足与更正过强断言"**，均**未新增需求**、未改动其他文件。

**已选定（2026-09-23）：第 ① 种 —— 由所有者补录两组原文**，**并已于同日完成**（原文见 §2.2 / §2.3，核对结果与记数更正见该两节；DEC-31）。作为历史记录保留当时的登记规则：

| 情形 | 登记方式 |
|---|---|
| 所有者能给出逐字原文 | §2.2 / §2.3 记"原文（所有者提供，日期）"，并逐项核对映射 |
| 只能给出等义复述 | 记"等义复述（所有者提供，日期；非逐字）"，同样逐项核对 |
| 只能给出部分条目 | 已给出的逐项核对；未给出的在表中明确标"仍未提供"，**不得**用推断补齐 |
| 提供的条目与现有 REQ/AC 无法对应 | 在 §2.2 / §2.3 注明差异，并列为待确认项，**不擅自新增或删除 REQ/AC** |
