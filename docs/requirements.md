# 需求文档：Android 后台常驻接收与开机自启

| 项目 | 内容 |
|---|---|
| 文档状态 | **需求正文已确认（2026-09-23）**，**但尚不宜进入设计**：§16.1 的 17 项决策已由仓库所有者落定，而 §16.2 中标为「**设计前必须**」的开放项（Q-16、Q-20…Q-28）尚未落定 |
| 版本 | v0.5（2026-09-23）—— 依据独立复核意见 R-01…R-16 修订，逐条处理结论见 §20 |
| 适用仓库 | `localsend_x`（LocalSend 私有魔改 fork） |
| 上游代码基线 | `230fb692`（v1.18.2，2026-09-14）—— 本 fork 代码演进的起点 |
| 协作规则基线 | `fe5e5929` —— `AGENTS.md` 启动版的确认提交 |
| 本文档基线 | `b8f4900c` —— 本文件上一版（v0.4）所在提交 |
| 依据 | 仓库所有者的原始想法、后台运行策略说明，及其对 v0.1 待确认问题的逐项答复（见 §16.1）；`AGENTS.md`（协作规则，下半部分）；现状事实来自 §3 的只读复核；平台约束来自 §18 的官方文档核对 |
| 本期范围 | 仅 Android 端后台保活与开机自启；不改其他平台、不改既有功能语义 |

> 本文件是**需求**文档，不含实现方案与代码。
> §16.1 是**已确认决策**（有出处、可追溯）；§16.2 是仍需决定的开放项，其中标为「**设计前必须**」者必须先落定，其余不阻塞架构设计。
> 与 `AGENTS.md` 的规则偏差记录在 §17；本次独立复核意见 R-01…R-16 的逐条处理见 §20。

---

## 1. 目标与用户

### 1.1 目标

让 LocalSend Android 客户端成为一台**可被长期发现在线的局域网接收端**：用户开启「后台接收」后，本机在应用退到后台、返回桌面、锁屏息屏、乃至设备重启之后，仍持续保持局域网设备发现与连接监听，使同一网络内的其他设备**在任何时间**都能发现本机并发起文件传输，无需用户提前打开应用。

### 1.2 用户

| 角色 | 描述 | 与本需求的关系 |
|---|---|---|
| 主用户（仓库所有者） | 把本机作为常驻接收端，希望随时从自己的其他设备向手机发送文件 | **L2 真机最终验收**的执行人（发布前，用自己的 Android 16 手机）；日常 L1 验收由 AI 在本机自动化完成（DEC-16，§14.1） |
| 发送方（同网络的其他人/自己的其他设备） | 使用任意 LocalSend 客户端或本 fork 的其他实例 | 只感知"对方在线"，不感知本需求的存在 |
| 维护者 | 后续在本 fork 上继续演进的人/模型 | 需要本文件与 `AGENTS.md` 保持一致 |

### 1.3 成功定义（一句话）

用户在**默认开启**的两个开关下（§16.1 DEC-10）完成一次性引导后，**在所有验收场景中任意时刻**，另一台同网段设备都能发现本机（§16.1 DEC-05）。

---

## 2. 原始需求逐条落位

> **可审计性说明（v0.5 修订，依据复核 R-01）**：第 1 组（5 条原始想法）在 `README.md` 的 fork 段与 §3.3 中有载体，可逐条核对；**第 2 组（10 条实现要求）与第 3 组（10 项验收标准）的原文从未入库** —— 全仓检索 `实现要求` 只命中本文件自身。
> 因此本节原先的"确保无遗漏、无扩写"**无法被独立验证**，该断言已删除。为使映射重新可审计，请把这两组原文（逐字或等义）补录进 §2.2 / §2.3 的占位表，补录后逐项核对落位。

### 2.1 原始想法（5 条，有载体）

| 原始条目 | 落位 |
|---|---|
| 1. 软件可以设置开机自启 | REQ-006（**独立「开机自启」开关**，另见 §16.1 DEC-10） |
| 2. 软件可以在锁屏状态下后台运行 | REQ-004 |
| 3. 软件可以在任务栏中常驻来保活 | REQ-002（常驻主体）+ REQ-003（通知可见性） |
| 4. 不改变软件其他功能 | REQ-011、NFR-006 |
| 5. 只需要修改安卓端，其他平台不进行修改 | §5.2 明确不做、NFR-006 |

### 2.2 后台运行策略（原始 10 条实现要求；**原文待补录**）

| 序号 | 原文（**待仓库所有者补录**） | 现落位 | 状态 |
|---|---|---|---|
| 1 | 待补录 | REQ-002 | 待核对 |
| 2 | 待补录 | REQ-003 | 待核对 |
| 3 | 待补录 | REQ-002 | 待核对 |
| 4 | 待补录 | REQ-004 | 待核对 |
| 5 | 待补录 | REQ-005 | 待核对 |
| 6 | 待补录 | REQ-009 | 待核对 |
| 7 | 待补录 | REQ-006 | 待核对 |
| 8 | 待补录 | REQ-007 | 待核对 |
| 9 | 待补录 | §5.2 + NFR-001 | 待核对 |
| 10 | 待补录 | REQ-008 | 待核对 |

> 上表的"现落位"沿用 v0.4 的合并映射（原文为一行式，"实现要求 N→REQ-xxx"），**未做任何新增推导**；补录原文后需逐条确认是否存在一对多、一对零或重复。

### 2.3 原始验收标准（原始 10 项；**原文待补录**）

| 序号 | 原文（**待仓库所有者补录**） | 现落位 | 状态 |
|---|---|---|---|
| 1…10 | 待补录 | v0.4 只给出范围：AC-01…AC-11 | **数量不符：原始 10 项 → 现有 11 项**，须确认是拆分、合并还是改写所致（§16.2 Q-28） |

> 补充说明：`AC-12`（由 REQ-008 推导）与 `AC-13`（由 REQ-006 推导）在 §14.4 已明确标注为**推导项**，不计入原始 10 项，因此**不存在未标注的扩写**；真正的问题只有一处 —— 原始 10 项与 AC-01…AC-11 的**逐项对应关系没有载体**，无法证明是 10 项而非 11 项。
> 任务级自动化验收（§14.2）**不属**原始验收标准，是本期新增的工程要求，已单独说明来源。

---

## 3. 现状（只读复核事实，2026-09-23）

以下为**已核实的事实**，是本需求要改变或复用的基线。未在此列出的行为不视为已知。

### 3.1 Android 端现状

| 事实 | 位置 | 对本需求的意义 |
|---|---|---|
| 已存在一个前台服务 `com.pravera.flutter_foreground_task.service.ForegroundService`，`android:foregroundServiceType="dataSync"` | `app/android/app/src/main/AndroidManifest.xml:93-96` | 已有载体，但类型为 `dataSync`，与"全天候常驻"冲突（实现要求 3 明确要求改按 `connectedDevice` 设计；平台依据见 §3.4） |
| 已声明 `FOREGROUND_SERVICE_DATA_SYNC` | 同上 `:23` | 改为 `connectedDevice` 需相应调整权限声明 |
| **无** `RECEIVE_BOOT_COMPLETED` 权限、**无**任何开机广播接收者 | 全仓 `grep BOOT_COMPLETED / RECEIVE_BOOT` 无命中 | REQ-006 从零开始 |
| **无**电池优化白名单相关权限（如 `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`）与引导 | 同上 | REQ-009 从零开始；可用性依据 §16.1 DEC-06 |
| `compileSdkVersion 36`、`targetSdkVersion 37`、`minSdkVersion flutter.minSdkVersion`（**未显式声明**） | `app/android/app/build.gradle:27,47,48` | targetSdk 已高于 Android 16 ⇒ 新的前台服务与 Doze 约束全部生效（§3.4）；minSdk 的处理见 §16.1 DEC-04 |
| `applicationId "org.localsend.localsend_app"`；已有 `QuickTileService`（快捷设置磁贴，仅用于启动 App，跑在 `:quick_tile_service` 独立进程）；`MainActivity` 为 `singleTask`，已有 `ACCESS_LOCAL_NETWORK` 运行时权限（API 37 起必需） | 同上 `:44`；`AndroidManifest.xml:78-89`；`MainActivity.kt` | 磁贴**不**保活，本需求不复用它；`ACCESS_LOCAL_NETWORK` 是"可被发现"的前置权限 |
| 既有多媒体/存储类权限（`READ_MEDIA_*`、`QUERY_ALL_PACKAGES`、`REQUEST_INSTALL_PACKAGES` 等） | `AndroidManifest.xml:5-19` | 本期不动（REQ-011） |

### 3.2 Dart / 服务层现状

| 事实 | 位置 | 对本需求的意义 |
|---|---|---|
| 前台服务对 Dart 侧的封装 `ForegroundService` 是**传输期专用**：由传输启动、无传输时停止；注释明确"必须在 App 位于前台时启动，Android 12+ 拒绝从后台启动前台服务" | `packages/localsend_isolates/lib/util/foreground_service.dart:37-87` | REQ-002 需要把"空闲期也常驻"这一新语义叠加进来，改动落点在此文件（已获授权，§16.1 DEC-01） |
| 该封装使用固定 `_serviceId = 1`、固定通知渠道 `localsend_foreground_service`，`autoRunOnBoot: false`、`autoRunOnMyPackageReplaced: false`、`allowWakeLock: true`、`allowWifiLock: true` | 同上 `:8-9,164-171` | 唤醒锁与 WiFi 锁**已开启**（对 REQ-004/REQ-010 有利）；开机自启被显式关闭；常驻服务将**另建渠道/服务**（§16.1 DEC-02） |
| 通知由 `TransferNotification` **引用计数**驱动：一个通知服务多路并发传输，首个传输启动服务、最后一个传输结束服务，通知展示各传输进度之和 | `packages/localsend_isolates/lib/util/transfer_notification.dart:1-45` | 与 REQ-003 的"永久通知"的冲突已按下述决策解决：**另建渠道/服务**，两者互不干扰（§16.1 DEC-02） |
| HTTP 服务端在 App 启动引导时启动一次（`startServerFromSettings()`），其生命周期与 App 进程一致 | `app/lib/config/init.dart:208`；`app/lib/provider/network/server/server_provider.dart:102` | 进程死则服务端死 ⇒ REQ-002 的常驻对象正是"承载服务端的进程" |
| 生命周期回调只在 `resumed` 时重建 socket（`ensureRunning()`），`detached` 时释放 isolates；**没有**针对 `paused`/后台的网络恢复路径 | `app/lib/main.dart:57-80` | 后台/息屏场景下的网络中断恢复（REQ-010）需要新的触发路径 |
| App 依赖 `wakelock_plus`（用户可保持屏幕常亮）；**不**依赖 `permission_handler` 等权限引导库 | `app/pubspec.yaml:68` | REQ-009 的引导跳转落地方式见 §16.2 Q-13；新增依赖已获授权（DEC-01） |
| 设置持久化已在用 `shared_preferences` | `app/pubspec.yaml:59` | REQ-001/REQ-006 的开关持久化无需新增依赖 |

### 3.3 复核结论

- 现状**已有一半零件**（前台服务封装、唤醒锁、WiFi 锁、快捷磁贴），但全部服务于"传输进行中"这一短时场景。
- 用户 5 条原始想法中，**第 1 条（开机自启）与第 3 条（常驻保活）在现状中完全不存在**；第 2 条（锁屏后台运行）仅在传输进行中被满足。
- 本需求不是从零新建，而是**给现有传输期前台服务旁新增一条空闲期常驻链路**（另建服务/渠道，DEC-02）并新增开机自启链路。这决定了 REQ-011（不回归）是本期最大风险面。

### 3.4 平台约束核对（2026-09-23，来源见 §18）

以下为**官方文档核对结论**，是 REQ-002 选择 `connectedDevice` 的直接依据；**设计阶段仍须在真机上复核**。

| 编号 | 结论 | 依据 |
|---|---|---|
| P-01 | `targetSdk ≥ 35` 时，**禁止**从 `BOOT_COMPLETED` 广播启动 `dataSync` / `camera` / `mediaPlayback` / `phoneCall` / `microphone` 类型的前台服务（否则抛 `ForegroundServiceStartNotAllowedException`）。**`connectedDevice` 不在受限名单内。** | FGS 变更文档 |
| P-02 | `dataSync` 自 Android 15 起有累计运行时长上限（约 6 小时 / 24 小时），`mediaProcessing` 同有 6 小时限制；**`connectedDevice` 未见运行时长上限**。 | FGS 类型文档、Android 15 FGS 类型变更 |
| P-03 | `connectedDevice` 的前提条件 = 声明 `FOREGROUND_SERVICE_CONNECTED_DEVICE`（+ 基础 `FOREGROUND_SERVICE`），**且至少满足其一**：声明 `CHANGE_NETWORK_STATE` / `CHANGE_WIFI_STATE` / `CHANGE_WIFI_MULTICAST_STATE` / `NFC` / `TRANSMIT_IR` 之一；或取得 `BLUETOOTH_CONNECT`/`BLUETOOTH_SCAN`/`BLUETOOTH_ADVERTISE`/`UWB_RANGING` 运行时权限；或取得 USB 设备访问。 | FGS 类型文档 |
| P-04 | P-03 的第一组权限（含 `CHANGE_WIFI_MULTICAST_STATE`）均为**安装时授予的普通权限，不产生运行时弹窗**。 | 同上 |
| P-05 | API 34 起必须为每个前台服务声明类型；在 Play Console 申报类型的要求仅适用于上架分发 —— 本需求为**私有侧载**，无需申报（§16.1 DEC-06）。 | 同上 |
| P-06 | Android 16（API 36）起，**由前台服务发起的后台作业**（`JobScheduler` / WorkManager / `DownloadManager`）须遵守运行时配额限制；`user-initiated data transfer` 作业不受该配额限制。**与 `targetSdk` 无关，仅取决于运行版本。** | FGS 变更文档（§18 第 1 条） |

**结论**：用户"优先按 `connectedDevice` 设计，不使用 `dataSync` 作为全天候保活服务"的选择（实现要求 3）在平台上**成立且必要** —— 它同时解开了 REQ-006（开机自启在 `targetSdk 37` 下唯一可行的一组类型）与 REQ-002（无运行时长上限）两个阻塞。

---

## 4. 核心场景

| 编号 | 场景 | 触发 | 期望结果 |
|---|---|---|---|
| S-01 | 首次安装即为常驻态 | 用户首次安装/首次启动（两个开关默认开启，DEC-10） | 引导用户完成设置；常驻服务启动、常驻通知可见；关闭应用后本机仍可被发现 |
| S-02 | 息屏后随时接收 | 设备息屏数小时，另一台设备发起传输 | 对方在任意时刻都能发现本机；用户无需先点亮屏幕或打开应用 |
| S-03 | 划掉最近任务后仍在线 | 用户从最近任务列表划掉 LocalSend | 本机仍可被发现（尽力而为，见 REQ-005） |
| S-04 | 设备重启后自动恢复 | 设备重启完成，用户未打开应用 | 本机自动恢复可被发现（需「开机自启」开启，REQ-006） |
| S-05 | 网络环境变化后自愈 | Wi-Fi 断开重连 / 路由器重启 / 切换网络 | 无需用户操作，本机重新可被发现 |
| S-06 | 服务被系统回收后自愈 | 系统内存压力杀掉进程 / 服务异常退出 | 在 Android 允许的机制下恢复常驻 |
| S-07 | 用户主动关闭后台接收 | 用户在设置页或常驻通知中关闭 | 服务与通知彻底停止，本机不再可被发现，功耗回归基线 |
| S-08 | 常驻状态下正常传输 | 常驻状态下收到传输 | 传输流程与现状一致（REQ-011）；常驻通知与传输进度通知各自独立、互不破坏（DEC-02） |

---

## 5. 本期范围

### 5.1 做什么

1. 「后台接收」开关，**默认开启**（REQ-001，DEC-10）。
2. 长期前台服务承载空闲期的设备发现与连接监听，**按 `connectedDevice` 设计**（REQ-002，§3.4）。
3. 后台接收开启期间的常驻通知，**使用独立渠道/服务**（REQ-003，DEC-02）。
4. 后台/返回桌面/锁屏后持续运行（REQ-004）。
5. 从最近任务划掉后的尽力保持（REQ-005）。
6. **独立的「开机自启」开关**与设备重启后自动恢复（REQ-006，DEC-10）。
7. 异常退出后的恢复（REQ-007）。
8. 关闭开关时彻底停止（REQ-008）。
9. 一次性引导流程（电池优化、后台不受限、国产 ROM 自启）（REQ-009）。
10. 网络变化后的自动恢复监听（REQ-010）。
11. 既有功能不回归的约束（REQ-011）。
12. §13 的非功能要求。

### 5.2 明确不做

| 不做的事 | 理由 |
|---|---|
| 修改 iOS / macOS / Windows / Linux / Web 的任何行为 | 原始需求第 5 条；`ForegroundService` 现有实现已按平台隔离（`_isSupported`） |
| 修改 `server/`（WebRTC 信令服务）与 `cli/` | 与本期目标无关；改动会扩大与上游差异 |
| 修改 `packages/core`（Rust 协议层） | 保活是宿主进程问题，不涉及协议、加密或 HTTP 语义（已确认的授权范围仅为 `packages/localsend_isolates`，DEC-01） |
| 1 像素 Activity 保活 | 实现要求 9；NFR-001 明确禁止 |
| 无限循环 Alarm / 高频 WorkManager | 实现要求 9；NFR-001 明确禁止 |
| 多进程互相守护、循环唤醒等对抗式保活 | 实现要求 9 的精神；与"可靠"目标相反（耗电与不可预期性） |
| 修改 `applicationId` / 包名 / 图标 / 应用名 | `AGENTS.md` §8 要求单独授权；本期无此需要 |
| 修改签名配置、发布签名流程 | 同上 |
| **改变 minSdk 或任何 SDK 级别** | 见 §16.1 DEC-04：本期保持现状；如需变更须另行授权 |
| 新增任何外部上报、统计、第三方服务 | NFR-008 |
| 移除任何现有功能（含 FOSS 剥离标记相关代码） | `AGENTS.md` §10 |
| 常驻通知上的"暂停接收"按钮 | §16.2 Q-14 只把它列为候选。**未确认的新需求不进入本期**（`AGENTS.md` §10）；Q-14 因此默认限定为"本期只有停止按钮"。若你要新增"暂停"，需先确认这是新需求而非既有条目的实现细节，再回到本节撤销本行 |
| AI 相关功能 | 本期无 AI 功能，故"质量评估 / 成本延迟 / 失败兜底"要求在本文件中不适用（§12） |

---

## 6. 优先级

| 优先级 | 条目 | 说明 |
|---|---|---|
| P0（本期必须） | REQ-001、REQ-002、REQ-003、REQ-004、REQ-008、REQ-011、NFR-001、NFR-006 | 没有它们需求不成立：开关、常驻主体、可见性、后台持续、可控关闭、不回归 |
| P0（本期必须，但依赖系统行为） | REQ-009 | 引导是国产 ROM 与 Doze 场景下唯一可行的用户侧手段 |
| P1（本期应有） | REQ-005、REQ-006、REQ-007、REQ-010 | "高可靠长期常驻"的核心自愈能力；受系统规则限制，验收按 §14 的判定口径 |
| P2（可观测即可，可后续迭代） | NFR-002、NFR-003、NFR-007 的量化部分 | 已确认**不设阈值**（DEC-07），本期只保证可观测 |

> 优先级不代表"可省略"：P0 任一条不满足即视为本期未完成（`AGENTS.md` §9：无证据的需求视为未完成）。

### 6.1 三套门槛（v0.5 新增，依据复核 R-05）

原文只明确了"P0 任一条不满足即本期未完成"，但没有区分**任务完成 / 本期完成 / 发布放行**这三件不同的事，也没有说明 P1 失败与"已知限制"（BR-04）各自阻塞什么。三者定义如下：

| 门槛 | 定义 | 现状 | **待确认** |
|---|---|---|---|
| ① 任务完成 | 单个开发任务的 L1 自动化验收（§14.2）通过 | 已明确（§14.1、DEC-16）：L1 未通过即任务未完成 | — |
| ② 本期完成 | 本期范围（§5.1）内的条目全部达成 | 只明确了 P0 | **P1 失败（REQ-005/006/007/010）是否阻塞本期交付**（§16.2 Q-23） |
| ③ 发布放行 | L2 真机验收通过（§14.4）且无未处理缺陷 | 部分：DEC-16 要求发布前一次性人工验收 | **BR-04 的"已知限制"是否阻塞发布**（§16.2 Q-23） |

> 三者相互独立：一个任务可以在 P1 上失败而仍算"任务完成"；本期可以在存在已知限制时结束；发布则需要人工判定。
> 在 Q-23 落定前，任何"是否通过"的结论都必须写清它用的是**哪一套**门槛。

---

## 7. 功能需求

格式：**触发/输入 → 处理规则 → 可观察结果**，并给出正常、失败、边界三类场景。
文中 `DEC-xx` 指 §16.1 已确认决策，`A-xx` 指 §15 剩余假设，`Q-xx` 指 §16.2 开放项。

### REQ-001 「后台接收」开关

- **触发/输入**：用户在设置页操作「后台接收」开关；或应用首次安装/首次启动读取默认值。
- **处理规则**：**默认值为开启**（DEC-10）。开关状态持久化（沿用 `shared_preferences`），在 App 重启、设备重启后保持；开启 → 进入 REQ-002 启动流程并按 REQ-003 展示通知；关闭 → 执行 REQ-008 的彻底停止流程。开关操作幂等（重复开启/关闭不产生副作用）。
- **可观察结果**：设置页存在一个可切换的「后台接收」项及其当前状态；开启后下拉通知栏可见常驻通知，且另一台设备在任意时刻可发现本机；关闭后通知消失、本机不可被发现。
- **正常**：开启 → 通知出现 → 另一台设备可发现；关闭 → 通知消失。
- **失败**：常驻服务启动被系统拒绝（权限未授予、Android 12+ 后台启动限制等）→ **开关回滚为"关闭"并提示原因**（DEC-03），提示中给出引导入口（REQ-009）。不允许出现"开关为开但服务未运行"的状态。
- **边界**：
  - **首次启动即为开启**：需要在该次启动中请求通知权限并展示引导（S-01）；通知权限被拒的表现见 REQ-003 失败分支。
  - 引导未完成时开启：允许开启，并提示"可能无法长期生效"。
  - 在传输进行中关闭：仅关闭常驻语义，进行中的传输按现状语义继续（DEC-02 使两者互不依赖）。

### REQ-002 常驻前台服务（后台接收主体）

- **触发/输入**：REQ-001 开启；或设备重启后的恢复（REQ-006）；或服务退出后的恢复（REQ-007）。
- **处理规则**：
  1. 以**长期前台服务**作为后台常驻主体，承载**空闲期**的局域网设备发现与连接监听（即 HTTP 服务端 + 发现机制的宿主进程）。
  2. 前台服务类型按 **`connectedDevice`** 设计，**不**将 `dataSync` 用作全天候保活服务（实现要求 3）。平台依据与前提条件见 §3.4 P-01…P-04：需声明 `FOREGROUND_SERVICE_CONNECTED_DEVICE`，并以 `CHANGE_WIFI_MULTICAST_STATE` 等**普通权限**满足前置条件（不产生额外运行时弹窗）。
  3. 与传输期的前台服务**完全分离**：常驻服务是**独立的服务实例**、**独立的通知渠道**，与传输期服务互不影响（DEC-02）。
  4. **传输进行期间，常驻服务不停止、不重启、不降级**：传输会话由现状的传输期服务承载，常驻服务不承载传输，因此在传输开始/结束时都不发生启停切换，监听不中断（S-08、E-16）。原文"空闲期与传输期共用同一常驻主体"表达的就是这一点，**不**表示传输期服务被并入常驻服务（v0.5 按复核 R-02 改写，消除歧义）。
  5. 保持 WiFi 锁与唤醒锁（现状已开启）。
- **资源归属（v0.5 新增，依据复核 R-02；事实来源 §3.2 + DEC-02）**：

| 资源 | 归属 | 传输进行时 | 关闭「后台接收」时（REQ-008） |
|---|---|---|---|
| 常驻前台服务实例（`connectedDevice`） | **本需求新增** | 继续运行 | 停止 |
| 常驻通知及其渠道 | **本需求新增** | 继续存在 | 移除 |
| 传输期前台服务实例（现有 `dataSync`） | 现状，不改 | 由传输驱动 | **不**受影响 |
| 传输通知及其渠道 | 现状，不改 | 由传输驱动 | **不**受影响 |
| HTTP 服务端 + 发现机制（组播） | 现状为**进程级单实例**（`startServerFromSettings()` 随进程，§3.2） | 被传输使用 | **冲突点，见下** |
| 唤醒锁 / WiFi 锁 | 现状已开启 | 不变 | 释放 |

- **待决策冲突（v0.5 新增，依据复核 R-02）**：现状的 HTTP 服务端是**进程级单实例**，而 REQ-008 要求关闭「后台接收」时"停止设备发现与**连接监听**"，REQ-008 的边界与 E-12 又要求"进行中的传输不受影响"。在单实例前提下两者不可能同时成立（停监听即断掉进行中的传输）。取舍见 §16.2 **Q-20**；**在 Q-20 落定前不得按任一读法实现**，也不得把本表当作已确认的授权。
- **可观察结果**：无任何传输任务时，进程保持存活、常驻通知存在、HTTP 服务端与发现机制在监听，另一台设备在任意时刻可发现本机。
- **正常**：开启后空闲 30 分钟，另一台设备仍能发现本机。
- **失败**：系统拒绝启动前台服务 → 按 REQ-001 的失败路径回滚并记录日志（NFR-007）。
- **边界**：同一时刻只有一次常驻服务实例（不得重复常驻）；`dataSync` 的运行时长上限**不**适用于本服务（§3.4 P-02）；若 `connectedDevice` 前置条件在目标真机上未被接受（§3.4 的复核项），须在设计阶段回退到"其他允许的类型或路径"。

### REQ-003 后台接收期间的常驻通知

- **触发/输入**：REQ-002 的服务在运行。
- **处理规则**：后台接收开启期间**允许**通知栏显示永久通知（实现要求 2）；使用**独立于传输通知的渠道与服务**（DEC-02）；通知内容表明"后台接收中"这一状态；通知提供**停止**入口（点按即执行 REQ-008）。
- **用户可用的停止途径（v0.5 修订，依据复核 R-06 与官方文档核对）**：共**三**条 —— 原先"唯一途径是开关或通知内的停止操作"的表述与平台事实不符，已删除：
  1. 设置页关闭「后台接收」开关（REQ-001 → REQ-008）；
  2. 常驻通知内的停止操作（REQ-008）；
  3. **Android 13+ 系统"活动应用 / Task Manager"中的 Stop**：系统会**终止整个应用进程、移除常驻通知，且不向应用发送任何回调**；应用只能在下次启动时通过 `ApplicationExitInfo.REASON_USER_REQUESTED` 得知这是用户主动停止。该情形下**是否自动恢复常驻尚待决策（§16.2 Q-21）**，未落定前不得实现自动恢复。
- **通知可划掉性**：前台服务通知通常不可被用户单独划掉，但**本文件未在目标 API 级别实测确认**，因此不再把"不可划掉"作为需求前提。替代约束（与平台行为无关）：**无论该通知是否可见或被用户清除，常驻服务与设置页状态都不得因此改变**（不得因通知消失而停止服务或把开关显示为关，BR-05、NFR-005）。
- **可观察结果**：通知栏存在一条**独立于传输通知**的常驻通知；点其"停止"后服务停止且通知消失；传输进行时两条通知同时存在且各自正确。
- **正常**：开启 → 常驻通知出现且长期存在；点停止 → 通知消失。
- **失败**：Android 13+ 通知权限被拒 → 前台服务仍可运行但常驻通知不可见；此时设置页状态仍须准确（不得因为"看不到通知"而误判为未开启），并提示授予通知权限（NFR-005）。
- **边界**：与传输通知的渠道命名/ID 需在设计中避免冲突；用户单独关闭常驻通知的渠道时的表现（应提示"通知已关闭，后台接收仍运行"，文案待定，Q-14）；通知文案的语言与翻译来源（Q-06）。

### REQ-004 后台、返回桌面、锁屏后持续运行

- **触发/输入**：应用进入后台 / 用户按 Home 返回桌面 / 设备锁屏或息屏。
- **处理规则**：服务继续运行；设备发现与连接监听**不因**上述状态变化而停止或降级（实现要求 4）。息屏后网络链路的保持依赖 WiFi 锁（现状已开启）；若系统仍断链，由 REQ-010 的恢复路径接管。
- **可观察结果**：应用处于后台、用户返回桌面、设备锁屏后（含息屏），另一台设备在任意时刻均可发现本机（对应 AC-01/AC-02/AC-03/AC-04）。
- **正常**：息屏 1 小时后另一台设备可发现本机。
- **失败**：系统进入深度 Doze 导致链路不可达 → 属系统行为，按 §14 的判定口径记录；不得为规避而使用 NFR-001 禁止的手段。
- **边界**：Doze 维护窗口期间的可达性（AC-07）；用户启用系统的"省电模式/超级省电"（可能强制停用后台，需引导说明）；多任务界面停留但不交互。

### REQ-005 从最近任务列表划掉后保持

- **触发/输入**：用户在最近任务列表划掉 LocalSend（实现要求 5）。
- **处理规则**：应用**不主动**在任务移除时停止常驻服务（即不随任务移除而停止服务）；若系统/ROM 强制终止进程，则交由 REQ-007 的恢复机制处理，不采用对抗手段（§5.2）。
- **可观察结果**：划掉后本机**尽量**仍可被发现；判定为"尽力而为"，允许在部分 ROM 上失败并记为已知限制。
- **正常**：划掉后 1 分钟内另一台设备仍可发现本机。
- **失败**：划掉后服务被系统终止且恢复机制未能恢复 → 记录机型与 ROM，计入已知限制（按 §14.5 的口径记录，不作为通过）。
- **边界**：连续多次划掉；在传输进行中划掉（进行中的传输是否中断由系统行为决定，本需求不承诺不影响传输结果）。

### REQ-006 独立的「开机自启」开关与设备重启后自动恢复

- **触发/输入**：用户在设置页操作「开机自启」开关（**独立于 REQ-001 的开关**，DEC-10）；或设备重启完成。
- **处理规则**：
  1. 「开机自启」是一个**独立的用户开关**，状态持久化，**默认值为开启**（DEC-14）。
  2. 「开机自启」开启**且**「后台接收」开启时：设备重启后，在 **Android 规则允许的范围内**自动恢复后台接收，使本机在用户**未打开应用**的情况下即恢复可被发现。
  3. 「后台接收」关闭时，「开机自启」不产生任何行为（常驻能力本身不存在，开机恢复无对象）——UI 上须说明该依赖关系，避免开关呈现"开着但无用"的误导（BR-05）。
  4. 恢复路径需使用系统允许的机制；平台依据见 §3.4 P-01（`connectedDevice` 允许从 `BOOT_COMPLETED` 启动）。
  5. **恢复时刻（v0.5 新增，依据复核 R-16 与官方 Direct Boot 文档）**：本应用**不是** direct-boot-aware 应用，两个开关的状态存于 `shared_preferences`（凭据加密存储，解锁后才可读），且系统对普通应用的 `BOOT_COMPLETED` 投递发生在**用户首次解锁之后**。因此本期只承诺"**首次解锁后**恢复"，**不承诺**解锁前恢复 —— `LOCKED_BOOT_COMPLETED` 需要 `directBootAware` + 设备加密存储（`createDeviceProtectedStorageContext()`），与本期的存储选择不兼容。此点记为**未确认假设 A-15**：若你要求解锁前恢复，属新增需求，需另行决定存储位置与清单改动。
  6. **覆盖安装（应用升级）后的行为（v0.5 新增，依据复核 R-16）**：现状 `autoRunOnMyPackageReplaced: false`（§3.2），系统不会在覆盖安装后重建前台服务；本期是否要求此时也恢复（由 REQ-007 的恢复路径接管，或明确不做）**未确认**，记为**假设 A-16**，暂列后续。
- **可观察结果**：设备重启并在**用户首次解锁**后，无需用户打开应用，另一台设备即可发现本机（AC-10 的判定口径按第 5 条更新）。
- **正常**：两开关均开启时，重启后自动恢复。
- **失败**：ROM 拦截自启（国产 ROM 常见）→ 引导用户开启自启动权限（REQ-009）；失败时记录日志（NFR-007）。
- **边界**：用户在开机过程中关闭「后台接收」（以用户意图为准，不恢复，BR-01）；仅在「开机自启」关闭时重启（不恢复，但用户下次打开应用后常驻能力仍正常）；Android 不允许从该广播启动相应类型前台服务的情形（§3.4 P-01）；快速连续重启；**重启后尚未解锁**（不在本期承诺范围，第 5 条、A-15）；**覆盖安装应用**后的恢复（第 6 条、A-16）。

### REQ-007 服务异常退出后的恢复

- **触发/输入**：服务因异常或系统资源压力退出（实现要求 8）。
- **处理规则**：使用 **Android 允许的机制**进行恢复（§5.2 排除项之外）；恢复尝试需可观测、有界，不产生无限唤醒（NFR-001）。
- **可观察结果**：人为模拟服务退出后，本机重新可被发现（AC-11）。模拟手段见 §14.3：**不得**使用 `am force-stop`（它会让应用进入 stopped state，必然产生"无法恢复"的假失败）。
- **判定闭合（v0.5 新增，依据复核 R-04）**：DEC-07 确认的是"**不设性能阈值**"，但验收必须能终止 —— 若只写"不设时限"，则"一直未恢复"也永远不必判失败。因此本文件继续不设阈值，但**必须有观察窗口**（窗口口径待定，§16.2 Q-22）。窗口落定前，AC-11 的判定写法为"记录实际恢复耗时 + 窗口内是否恢复"，**不得**表述为"已通过"。
- **正常**：服务被终止后自动恢复。
- **失败**：恢复机制被 ROM 完全禁止 → 记录为已知限制并引导用户（REQ-009）。
- **边界**：短时间内反复被杀（应避免恢复风暴）；设备处于深度 Doze（恢复可能被推迟，验收记录实际耗时即可，不作门槛）。

### REQ-008 关闭「后台接收」时彻底停止

- **触发/输入**：用户在设置页关闭开关，或在常驻通知中点"停止"（实现要求 10）。
- **处理规则**：停止前台服务、移除常驻通知、停止设备发现与连接监听、释放唤醒锁/WiFi 锁、取消任何为保活而注册的恢复机制；**不残留**任何为常驻目的注册的定时任务或监听。已配置的开关偏好保留（下次开启可直接使用）。
- **可观察结果**：常驻通知消失、本机不再被其他设备发现、进程可被系统正常回收（对应 AC-12）。
- **正常**：关闭后本机不再可被发现；重新开启后功能恢复正常（可重复开关）。
- **失败**：通知移除失败或服务停止失败 → 记录日志；不允许出现"开关已关但通知仍在"的不一致（如出现须视为缺陷，BR-06）。
- **边界**：在传输进行中关闭（常驻服务与传输服务独立，DEC-02：进行中的传输不受影响）；快速连续开关。

### REQ-009 一次性引导流程

- **触发/输入**：首次启动/首次开启（默认开启，DEC-10）；或检测到引导项未满足时（实现要求 6）。
- **处理规则**：引导用户完成以下三项，并对每项给出**当前状态**与**跳转到系统对应设置页的入口**：
  1. 关闭应用电池优化；
  2. 将应用后台运行设置为"不受限制"；
  3. 国产 ROM：开启自启动权限与后台运行权限。
  引导当前按**允许跳过**设计（A-09），跳过时明确告知"后台接收可能无法长期生效"。**但"是否允许跳过"本身仍是开放项，且已定为设计前必须落定（§16.2 Q-16）** —— 若改为强制完成，业务流程与状态机随之变化。
- **可检测性边界（v0.5 新增，依据复核 R-09）**：第 3 项（厂商自启动）在 Android 上没有统一的查询接口，部分 ROM 上的"后台运行不受限"同样**无法可靠查询**。因此：
  - 每项状态的取值扩展为三态：**已满足 / 未满足 / 无法检测（需人工确认）**；"无法检测"是合法且预期的取值，**不得**为凑齐两态而伪造检测结果；
  - **不得**把"用户跳转过设置页"当作"已满足" —— 跳转成功只表示打开过页面；
  - **区分两类状态**：系统实时状态（能查到才查）与"用户已阅读 / 已跳过"（持久化数据）。后者只用于避免重复打扰，**不得**用于判断能力是否具备（对应 §10.1 的引导状态数据）；
  - 对"无法检测"项，允许用户手动标记"我已设置"，界面须标注为**用户自述**而非系统确认（BR-05：不得呈现假象）。默认呈现方式记为**未确认假设 A-17**；
  - 某项在目标设备上不适用时显示"该设备无需此项"（见下方边界）。
- **可观察结果**：引导页/引导区列出三项及其状态（**已满足 / 未满足 / 无法检测**）；点按可跳转到对应系统设置；用户完成后状态更新为已满足（能查到实时状态时）或标注为用户自述（查不到时）。
- **正常**：三项均可跳转并可在返回后反映状态。
- **失败**：某 ROM 无对应设置页或跳转失败 → 给出该 ROM 的手动操作说明（文案待定）；不崩溃。
- **边界**：用户完成后又手动在系统设置里改回（下次进入引导时应重新显示为未满足）；部分设备不支持某项（应显示"该设备无需此项"而非"未满足"）。

### REQ-010 网络变化后自动恢复监听

- **触发/输入**：Wi-Fi 断开后重连、路由器重启后网络恢复、IP 地址变化、网络切换。
- **处理规则**：无需用户操作，自动恢复设备发现与连接监听；恢复不依赖 App 处于前台（现状的 `ensureRunning()` 仅在 `resumed` 触发，见 §3.2，因此需要新的后台触发路径）。
- **可观察结果**：断开并重连 Wi-Fi（或重启路由器）后，另一台设备在本机应用处于后台的情况下即可重新发现本机（AC-08/AC-09）。
- **正常**：重连后自动恢复。
- **失败**：恢复失败 → 记录日志并按 Android 允许的机制重试（退避，不得超过 NFR-001 的强度约束）。
- **判定闭合与平台约束（v0.5 新增，依据复核 R-04）**：判定闭合与 REQ-007 同（观察窗口待定，§16.2 Q-22）。**平台约束补充**：Android 16 起，从常驻前台服务发起的后台作业（`JobScheduler` / WorkManager / `DownloadManager`）须遵守运行时配额（§3.4 P-06）—— 若恢复/重试路径选用其中任一机制，设计阶段须先核对配额。
- **边界**：短时间多次网络抖动（避免恢复风暴，E-11）；从 Wi-Fi 切到移动网络（移动网络下无法被局域网发现，应保持服务运行并在恢复 Wi-Fi 后可达）；多网卡/多接口并存。

### REQ-011 既有功能不回归

- **触发/输入**：本需求的所有改动生效后使用应用（原始需求第 4 条）。
- **处理规则**：不改变既有功能的语义与用户可见行为，包括但不限于：发送/接收全流程、网页分享（web send）、扫描发现、分享入口（share_handler）、接收历史、设置项语义、i18n 语言集与翻译键、FOSS 剥离标记所保护的结构、快捷磁贴行为、传输期前台服务与进度通知（DEC-02 下必须保持原样可用）。
- **可观察结果**：既有的自动化检查（`AGENTS.md` 上半部分列出的 analyze/format/test）、既有测试套件全部通过；手工回归清单全部通过。**这些命令取决于工具链是否就绪**（§14.6、Q-19）。
- **正常**：全部检查通过。
- **失败**：任一检查失败或既有行为改变 → 本需求未完成。
- **边界**：传输进行中开启/关闭后台接收（两个服务独立，需各自正确）；后台接收开启状态下使用网页分享（两者都要监听端口，端口/证书行为需在设计阶段核实）。

---

## 8. 关键页面、状态与用户操作流程

### 8.1 关键页面

| 页面/区域 | 位置 | 本期变化 | 关键状态 |
|---|---|---|---|
| 设置页 | 现有设置 Tab（`app/lib/pages/tabs/settings_tab.dart`） | 新增**两个**开关（「后台接收」「开机自启」）+ 引导状态入口 | 见 §8.3 |
| 引导页/引导区 | 新建（形态待定，Q-13） | 新增 | 全部满足 / 部分满足（逐项显示） / 已跳过 |
| 常驻通知 | 系统通知栏（独立渠道，DEC-02） | 新增 | 后台接收中 / 不可见（通知权限被拒） |
| 传输进度通知 | 系统通知栏（现有渠道） | **行为不变**（REQ-011、DEC-02） | 与常驻通知共存，不互相覆盖 |

### 8.2 用户操作流程（主流程）

1. 用户安装并首次启动（两个开关默认开启，DEC-10、DEC-14）→ 应用请求通知权限（Android 13+）→ 启动常驻服务 → 展示常驻通知。
2. 应用检查引导三项的状态：
   - 全部满足 → 就地提示"后台接收已生效"。
   - 存在未满足项 → 展示引导（可跳过），提示"后台接收可能无法长期生效"。
3. 用户完成引导（或跳过）→ 设置页两个开关显示为开。
4. 用户离开应用 / 息屏 / 重启设备 → 无需其他操作，本机持续可被发现。
5. 用户停止：在设置页关闭「后台接收」，或在常驻通知中点"停止" → 服务与通知消失。
6. 用户只想关掉开机自启而保留后台接收：关闭「开机自启」→ 重启后不再自动恢复，本次运行不受影响。

### 8.3 用户可见状态汇总

| 状态 | 用户可见表现 | 出现条件 |
|---|---|---|
| 后台接收·开·生效中 | 开关为开，常驻通知存在 | 服务运行 **且** 监听可用，且引导无"未满足"项（口径见下方注） |
| 后台接收·开·部分未满足 | 开关为开，常驻通知存在，引导区显示未满足项 | 引导有"未满足"项（"无法检测"不计入） |
| 后台接收·关（含启动失败回滚） | 开关为关，无常驻通知；**提示了失败原因与引导入口** | 用户关闭 / 服务启动被系统拒绝（DEC-03） |
| 后台接收·开·通知不可见 | 开关为开，通知栏无通知 | 通知权限被拒或用户关闭了该渠道 |
| 后台接收·开·无局域网 | 开关为开，常驻通知存在，设置页提示"当前无局域网，暂不可被发现" | 无网络 / 仅移动网络（E-06、REQ-010 边界） |
| 后台接收·开·恢复中 | 开关为开，常驻通知存在；**是否提示"恢复中"待确认** | 网络变化或服务被回收后的恢复期（REQ-007、REQ-010） |
| 后台接收·开·监听不可用 | **呈现方式待确认（§16.2 Q-25）**：不得显示为"生效中" | 服务已启动，但 HTTP 服务端/组播初始化失败，或局域网权限被拒（§10.3） |
| 后台接收·开·被系统用户停止 | **呈现方式待确认（§16.2 Q-21）** | 用户在系统"活动应用"中 Stop（REQ-003） |
| 开机自启·开（依赖后台接收） | 开关为开；若后台接收为关，显示依赖说明 | REQ-006.3 |
| 开机自启·关 | 开关为关 | 用户关闭，重启后不恢复 |

> **"生效中"的判定口径（v0.5 新增，依据复核 R-08）**：上表首行的"生效中"**不得**只以"前台服务在运行"为准 —— 必须同时满足"服务运行 **且** 监听可用（HTTP 服务端与发现机制已就绪）"。
> 原文只覆盖了"前台服务启动被拒"这一种失败（DEC-03），**没有**覆盖：服务已启动但监听初始化失败、局域网权限被拒、无局域网、恢复重试中、被系统用户停止。上表已把这些状态补入，其中呈现方式未定的四处标为"待确认"（Q-21、Q-25），**未定之前不得自行选择一种呈现**，也不得统一显示为"生效中"。

---

## 9. 业务规则

| 编号 | 规则 |
|---|---|
| BR-01 | 用户对**每个开关**的显式选择是最高优先级的意图：用户关闭「后台接收」后，任何自动机制（重启恢复、异常恢复）都不得重新开启；用户关闭「开机自启」后，重启不得自动恢复。 |
| BR-02 | 两个开关的**默认值均为开启**（DEC-10、DEC-14）。这是有意的默认行为改变：首次启动即出现常驻通知并保持局域网在线，属于对用户可见默认行为的变更，因此 REQ-009 的引导必须在首次启动即出现。 |
| BR-03 | 保活手段的合法性优先于保活强度：任何与系统机制正常用法冲突的手段一律不采用（§5.2、NFR-001）。 |
| BR-04 | 系统限制导致的能力上限是**已知限制**而非缺陷，但必须实测记录机型与 ROM（§14.5）。 |
| BR-05 | 开关状态不得呈现假象：以**服务真实运行**为准；启动被拒时回滚开关并说明原因（DEC-03）；「开机自启」在「后台接收」关闭时不产生行为，须在 UI 说明依赖关系。 |
| BR-06 | 关闭必须彻底：不接受"关了开关但通知还在""关了开关但仍在监听"等残留状态（REQ-008）。 |
| BR-07 | 本期不改变任何既有功能的语义与默认值（REQ-011）；常驻能力与传输能力是两条独立链路（DEC-02）。 |
| BR-08 | 不为保活而新增任何外部数据流动（NFR-008）。 |
| BR-09 | 两个开关相互独立，但「开机自启」的效果以「后台接收」开启为前提（REQ-006.3）。 |

---

## 10. 数据与权限

### 10.1 新增数据（本地）

| 数据 | 存储 | 用途 | 敏感性 |
|---|---|---|---|
| 「后台接收」开关状态（布尔，默认开） | `shared_preferences` | REQ-001 | 低 |
| 「开机自启」开关状态（布尔，默认开，DEC-14） | `shared_preferences` | REQ-006 | 低 |
| 引导完成状态（逐项，布尔集合或时间戳） | `shared_preferences` | REQ-009（避免重复打扰；须允许用户被系统设置改回后重新提示，见 REQ-009 边界） | 低 |
| 保活诊断信息（最近一次启动/被拒/恢复尝试的结果） | 现有日志体系 | NFR-007 | 低（不得含文件内容、密钥、指纹） |

**不新增**：不上传、不同步、不写外部存储、不采集设备标识用于保活之外的用途。

**已确认约束 vs 候选方案（v0.5 新增，依据复核 R-13）**：上表"存储"一列里的 `shared_preferences`，以及"引导完成状态"的取值形态（"布尔集合或时间戳"），都是**依据现状（§3.2：已在用、不新增依赖）给出的候选方案**，**不是**已确认的需求。已确认的只有：**不新增依赖、不上传、不写外部存储**（NFR-008、DEC-01）。
具体键名、数据结构与默认值迁移规则由**设计阶段确定**，届时须回到本节核对；`AGENTS.md` §4 的"配置改了同步文档"在此适用。

**授权边界（v0.5 新增，依据复核 R-13）**：新增持久化键是否属于 `AGENTS.md` §8 所列的"持久化格式变更"（该条要求显式授权），**本文件不作单方判断** —— 见 §16.2 **Q-24**。假设 A-14 仍为未确认假设，不因本节而升格。

### 10.2 权限

| 权限 | 现状 | 本期 | 用途 / 依据 |
|---|---|---|---|
| `FOREGROUND_SERVICE` | 由 `flutter_foreground_task` 带来 | 复用 | 前台服务基础（§3.4 P-03） |
| `FOREGROUND_SERVICE_CONNECTED_DEVICE` | **无** | **新增** | `connectedDevice` 类型的必需权限（§3.4 P-03） |
| `CHANGE_WIFI_MULTICAST_STATE`（或以 `CHANGE_NETWORK_STATE` / `CHANGE_WIFI_STATE` 替代） | **无** | **新增**（其一即可） | 满足 `connectedDevice` 前置条件；均为普通权限、无运行时弹窗（§3.4 P-03/P-04）。`CHANGE_WIFI_MULTICAST_STATE` 与本应用已在做的组播发现语义天然契合 |
| `RECEIVE_BOOT_COMPLETED` + 开机广播接收者 | **无** | **新增** | REQ-006（§3.4 P-01） |
| `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` | **无** | **新增** | REQ-009 引导第 1 项；**仅私有侧载**下可用（DEC-06）。若只跳转设置页而不声明该权限，则不需要它 —— 设计阶段二选一 |
| `POST_NOTIFICATIONS` | 由 `flutter_foreground_task` 带来，代码已请求 | 复用 | Android 13+ 通知可见性 |
| `FOREGROUND_SERVICE_DATA_SYNC` | 已声明 | **保留** | 传输期前台服务仍使用 `dataSync`（DEC-02：传输链路不改）；常驻服务不使用它 |
| `INTERNET`、`ACCESS_LOCAL_NETWORK` | 已有（后者已声明并已运行时请求，API 37+） | 复用 | 局域网通信与"可被发现"的前置 |
| 厂商自启动权限 | 无（不可声明，只能跳转系统设置） | **需要跳转能力** | REQ-009 第 3 项 |

### 10.3 权限失败的用户可见后果

| 情形 | 后果 | 要求 |
|---|---|---|
| 常驻服务启动被系统拒绝 | 后台接收不生效 | **回滚开关为关 + 提示原因 + 引导入口**（DEC-03） |
| 通知权限被拒 | 常驻通知不可见，服务仍运行 | 不得误报为未开启（§8.3、NFR-005） |
| 电池优化未关闭 | 后台可能被冻结，可达性下降 | 引导中显示为未满足（REQ-009） |
| 自启动未开启 | 重启后不恢复 | 引导中显示为未满足；REQ-006 失败分支 |
| `ACCESS_LOCAL_NETWORK` 被拒（API 37+） | 局域网通信不可用 | 复用现状的权限请求逻辑；失败时明确提示 |

---

## 11. 异常与边界情况

| 编号 | 情况 | 期望行为 |
|---|---|---|
| E-01 | 常驻服务启动被系统拒绝（前台启动限制、权限缺失） | **回滚开关为关 + 提示原因 + 引导入口**（DEC-03、BR-05） |
| E-02 | 进程被系统内存压力回收 | 由 REQ-007 恢复；不影响用户已保存的数据 |
| E-03 | ROM 强制禁止后台/自启 | 尽力而为；记录机型与 ROM 为已知限制（BR-04）；引导用户手动设置 |
| E-04 | 深度 Doze | 可达性可能被延迟；按 §14 判定口径记录实际耗时；不得为规避而使用滥用手段（NFR-001） |
| E-05 | Wi-Fi 断链 / 路由器重启 / IP 变化 | REQ-010 自动恢复；不得需要用户打开应用 |
| E-06 | 移动网络 / 无网络 | 服务保持运行；恢复局域网后自动可达（REQ-010 边界） |
| E-07 | 用户关闭常驻通知的渠道 | 服务仍运行；状态提示与引导（REQ-003 边界） |
| E-08 | 用户开启系统"超级省电"/"省电模式" | 可能强制停用后台；在引导中说明（文案待定） |
| E-09 | 设备时间被修改 / 时区变化 | 不影响保活判断；不引入依赖本机时钟的保活逻辑 |
| E-10 | 用户在多任务界面划掉应用 | REQ-005；尽力而为 |
| E-11 | 快速连续开关 / 反复重启设备 | 幂等、无恢复风暴、无重复服务实例（REQ-001/REQ-006/REQ-007 边界） |
| E-12 | 传输进行中关闭后台接收 | 两条链路独立（DEC-02）：常驻服务停止，进行中的传输按现状语义继续；不得造成半写文件 |
| E-13 | 磁盘空间不足（接收时） | 属现状行为，本期不改变（REQ-011） |
| E-14 | 应用被卸载后残留计划任务 | 卸载应清理；不得留下系统层面的孤儿任务（NFR-001） |
| E-15 | 多台本 fork 设备同网段同时常驻 | 互不影响，均可被发现（现状的指纹去重机制保持不变） |
| E-16 | 首次启动时用户正在传输 | 默认开启的常驻服务不得干扰传输链路（DEC-02 独立性） |
| E-17 | 设备无 Wi-Fi / 仅以太网（平板、TV） | 行为按现状；常驻不因无 Wi-Fi 而崩溃或反复报错 |

---

## 12. 适用性说明（AI 相关章节）

本期**不涉及任何 AI 功能**，因此"AI 质量评估、成本/延迟边界、失败兜底"三项要求不适用于本文件。若后续引入，需在本文件新增对应章节。

---

## 13. 非功能要求（NFR）

> 仓库所有者已确认**不设量化阈值**（DEC-07）：性能与资源类要求本期只保证**可观测**，不承诺数值。

### 13.1 NFR 分类与判定口径（v0.5 新增，依据复核 R-15）

原文把全部 NFR 并列，但其中一部分**没有通过/失败语义**（不设阈值），因此"全部 NFR 通过"不是有效结论。分类如下：

| 类别 | 条目 | 是否参与质量放行 |
|---|---|---|
| **通过/失败型** | NFR-001、NFR-004、NFR-005、NFR-006、NFR-008、NFR-009、NFR-010 | 是 —— 有明确的检查项与判据 |
| **证据采集型（只观测）** | NFR-002、NFR-003 | **否**（DEC-07：不设阈值）。只要求"能采到数据"，**不得**表述为"通过"或"未通过" |
| **混合** | NFR-007 | 检查项（日志是否写出、是否可导出）通过/失败；耗时类只记录 |

> 由此，完成报告里每个 NFR 的结论必须写成"通过 / 未通过 / 已采集证据（不判定）/ 未验证 + 原因"四者之一。
> 下文每条 NFR 的"可验证方式 / 测量条件"即其**可枚举检查项**；仅含主观形容词（"行为不变""改动集中""能回答失败在哪一步"）的表述已在下文改为可枚举形式。

### NFR-001 保活手段的合法性（禁止滥用系统机制）

- **要求**：实现中不得出现以下任一：1 像素 Activity、无限循环 Alarm、高频（分钟级或更密）WorkManager 轮询、多进程互相守护、伪造前台通知内容的空壳服务。
- **可验证方式**：代码审查 + 静态检索（对新增代码检索上述模式的等价写法），并在设计文档中逐条声明所用机制及其必要性。
- **测量条件**：审查范围为本次改动涉及的全部文件。

### NFR-002 保活期间耗电（只观测）

- **要求**：**不设阈值**（DEC-07）。要求"可观测"：能回答"后台接收开启后，静置 N 小时的耗电是多少"。
- **观测条件**：满电 100%；关闭电池优化；后台接收开启；无任何传输；屏幕关闭；同一路由环境且 Wi-Fi 保持连接；记录开始与结束电量（系统电量统计）与 8 小时 / 24 小时的下降百分比。
- **判定**：本期只记录数据，不作通过/不通过判定；后续若需要阈值，再写入本节。

### NFR-003 常驻期间资源占用（只观测）

- **要求**：**不设阈值**（DEC-07）。要求"可观测"。
- **观测条件**：常驻静置 1 小时后，记录应用进程 PSS 与累计 CPU 时间；与"后台接收关闭"状态下的同一指标对比。
- **判定**：本期只记录数据，不作通过/不通过判定。

### NFR-004 兼容性与配置不变式

- **要求**：
  1. 不改变 `applicationId`、包名、签名配置。
  2. **不改变 minSdk，也不改变任何其余 SDK 级别**（DEC-04）。`minSdkVersion` 保持为 `flutter.minSdkVersion`（现状即未显式声明，本期继续不显式声明）。理由：本需求不需要更高的下限；提高 minSdk 会不可逆地丢弃可安装设备，属于超出需求的改动（`AGENTS.md` §3）。若你日后要求改变 minSdk，需另行授权（`AGENTS.md` §8）。
  3. **验收基线为 Android 16（API 36）**（DEC-04）。在低于 Android 12 的设备上，保活能力按平台可用性自然降级，且不得呈现假象状态（BR-05）：设计阶段需确定"低于某个 API 级别时的呈现方式"（设计输入，非需求变更）。
  4. 前台服务类型与权限声明需与目标 API 级别的平台要求一致（§3.4）。
- **可验证方式**：`build.gradle` 与清单 diff 审查（仅允许出现与保活直接相关的改动）；构建产物可安装且可升级覆盖旧版本。

### NFR-005 通知合规与降级

- **要求**：Android 13+ 通知权限被拒时服务仍可运行；被拒不得导致功能静默失效或状态误报（§8.3）。
- **测量条件**：拒绝通知权限（模拟器可直接 `adb shell pm revoke <pkg> android.permission.POST_NOTIFICATIONS`）后执行首次启动/开启流程，观察设置页状态与可达性。

### NFR-006 平台与功能隔离

- **要求**：仅 Android 行为改变；非 Android 平台构建与行为不变；既有功能语义、默认值与传输期链路不变（REQ-011、BR-07）。
- **可验证方式（v0.5 改为可枚举，依据复核 R-11/R-15）**：①既有自动化检查全通过（`fvm dart format --set-exit-if-changed lib test`、`fvm flutter analyze`、`fvm flutter test`）；②改动面静态检查：新增/修改的文件只落在 Android 宿主层与 `packages/localsend_isolates` 的保活相关文件，非 Android 目标的平台隔离判断（`_isSupported` 一类）未被改动；③非 Android 平台构建检查；④手工回归清单（设计阶段产出）全部通过。
  **当前限制**：③ 在本机**不可执行**（Visual Studio 缺 C++ 工作负载，见 §14.6），因此该项只能写"未验证 + 原因"，**不得**用 ② 的静态检查替代 ③ 的构建检查。

### NFR-007 可观测性与可诊断

- **要求**：保活关键事件（服务启动成功/被拒、服务停止、恢复尝试与结果、引导状态变化、回滚原因）写入现有日志体系，且用户可在应用内导出或查看（沿用现状的诊断入口）。日志不得含文件内容、密钥或证书指纹。
- **测量条件（v0.5 改为可枚举，依据复核 R-15）**：完整执行 §14 各验收项后，能从日志用证据回答以下**每一条**（缺一条即本 NFR 不通过）：
  1. 常驻服务是否启动成功、发生在何时；
  2. 若失败，失败在**启动被拒 / 权限缺失 / 监听初始化 / 网络不可用**中的哪一步；
  3. 恢复机制共尝试了几次、每次的结果；
  4. 引导状态何时由何值变为何值；
  5. 用户触发的停止/关闭动作（开关与通知按钮）是否被记录。

### NFR-008 隐私与数据边界

- **要求**：不新增任何外部上报、第三方 SDK 或云服务；新增权限仅用于本地保活与局域网发现（清单见 §10.2）。
- **可验证方式**：依赖清单 diff + 新增权限清单与用途说明对照。

### NFR-009 文档与本地化同步

- **要求**：行为变更同步更新本文件与相关说明；新增用户可见文案走现有 i18n 机制（slang，`app/assets/i18n/`）并同步 `app/lib/gen`；若触及 `[FOSS_REMOVE]` 保护的文件，保留全部标记。
- **可验证方式**：`AGENTS.md` §4 的同步要求在完成报告中逐项对照（翻译文案的写入范围见 Q-06）。

### NFR-010 可回滚性

- **要求**：改动集中、可独立回滚；与**上游代码基线 `230fb692`** 的 diff 便于逐行对比（`AGENTS.md` §7 的取向）。
  > **基线名不要混用（v0.5 修订，依据复核 R-14）**：`230fb692` = 上游代码基线（v1.18.2）／`fe5e5929` = 协作规则基线／`b8f4900c` = 本文档 v0.4 所在提交。三者用途不同：比对**代码**改动用 `230fb692`，比对**规则**改动用 `fe5e5929`。
- **可验证方式（v0.5 改为可枚举，依据复核 R-15）**：①`git diff --stat 230fb692` 的文件清单全部落在 Android 宿主层与 `packages/localsend_isolates` 的保活相关文件内；②非 Android 平台目录下无改动；③改动可整体回滚而不影响既有功能（由 REQ-011 的回归检查验证）。

---

## 14. 验收标准

### 14.1 验收分层（2026-09-23 调整，DEC-16）

| 层级 | 由谁执行 | 环境 | 何时执行 | 是否阻塞任务 |
|---|---|---|---|---|
| **L1 自动化验收** | **AI，在本机自动完成** | Android Emulator（**Android 16 / API 36**）+ 命令行 + 日志 | **每个开发任务完成前必须执行** | **是** —— L1 未通过即该任务未完成 |
| **L2 真机最终验收** | 仓库所有者人工 | 真实 Android 设备 + 真实局域网 + 第二台设备 | **仅发布前一次性执行** | **否** —— 不作为任何单个开发任务的完成前置 |

原则：

- 凡能通过**自动化测试、模拟器、命令行或日志**验证的内容，**不再要求人工真机验收**（DEC-16）。
- 模拟器不可靠覆盖的能力，明确标记为 **L2 真机最终验收项**（§14.4），但**不得因此阻塞普通开发任务**。
- 依赖真实网络环境的功能（局域网发现、设备互传、后台接收）在**开发阶段**允许通过模拟器、Mock、自动化集成测试或本机多实例验证（DEC-17，§14.3）；真机之间的端到端传输属 L2。
- **缺少真机不构成任何任务的阻塞条件。**

### 14.2 L1 自动化验收项（每个开发任务必须执行）

命令遵循本仓库的 `fvm` 约定（见 `AGENTS.md`）。

| 编号 | 检查 | 命令 | 通过判据 |
|---|---|---|---|
| M-01 | 格式化 | `fvm dart format --set-exit-if-changed lib test`（CI 会先删除 `lib/gen`，生成代码不参与格式检查） | 退出码 0 |
| M-02 | 静态检查 | `fvm flutter analyze` | 无 error |
| M-03 | 单元 / widget 测试 | `fvm flutter test`；改动涉及 UI 状态时可补必要的 widget / integration test；涉及保活逻辑时须补对应单元测试 | 全部通过 |
| M-04 | Android Debug APK 构建 | `fvm flutter build apk --debug` | 构建成功并产出 APK |
| M-05 | 模拟器安装与冒烟 | `adb install -r <apk>` → 启动 → 按该任务验收标准操作关键路径 | 启动无崩溃，关键路径可完成 |
| M-06 | 运行期错误检查 | `adb logcat`（按包名与 `AndroidRuntime` / `FATAL` 过滤）；必要时 `adb shell dumpsys dropbox` | 无未处理异常、无崩溃 |
| M-07 | 变更专项检查 | **任务开始前**在该任务的验收标准里预先声明的专项断言（例：`dumpsys activity services` 中 `foregroundServiceType` 的期望值、`dumpsys power` 中唤醒锁的期望状态） | 任务**预先声明**的每一条断言都通过（v0.5 修订，依据复核 R-11：原判据"与该任务验收标准一致"是循环定义） |

> 完成报告中每个需求 ID 必须附「执行的命令 + 真实输出」（`AGENTS.md` §5/§9）。
> **未实际执行的检查不得声称通过**；无法执行时写"未验证 + 原因"。

#### 14.2.1 L1 的最小覆盖集与追踪矩阵（v0.5 新增，依据复核 R-11）

仅有 M-01…M-07 全绿**不足以**证明需求已被覆盖 —— 这些检查多数与具体需求无关。以下场景**每个都必须有对应的 L1 用例**：

| 必须覆盖的场景 | 对应条目 |
|---|---|
| 四种开关组合（后台接收开/关 × 开机自启开/关）的行为 | REQ-001、REQ-006、BR-09 |
| 首次启动的默认值与引导出现（含"无法检测"态） | REQ-009、BR-02、A-17 |
| 常驻服务启动被拒 → 开关回滚 + 提示原因 | DEC-03、E-01 |
| 服务已启动但监听初始化失败 → 状态呈现 | REQ-002、（Q-25） |
| 关闭常驻通知渠道后，服务与设置页状态不受影响 | REQ-003、NFR-005 |
| 常驻通知与传输通知并存、互不破坏 | DEC-02、S-08、E-16 |
| 关闭「后台接收」后无残留服务/唤醒锁/恢复注册 | REQ-008、AC-12 |
| 用户从系统"活动应用"停止应用之后的行为 | REQ-003、（Q-21） |
| 既有自动化检查全通过 + 改动面静态检查 | REQ-011、NFR-006、NFR-010 |

**追踪矩阵（设计阶段交付物）**：每个 REQ / NFR → 用例 ID → 执行环境（模拟器 / 静态检查）→ 判据。矩阵未产出前，**不得**声称"所有需求已被验收覆盖"；矩阵中无法在 L1 环境执行的项，必须显式标注为"L2 或静态检查"并说明理由（依据复核 R-11）。

### 14.3 模拟器的能力边界与开发期替代验证

**可由模拟器 + 命令行自动验证**（可纳入 L1；各项需在环境就绪后实测确认）：

| 能力 | 自动化手段 |
|---|---|
| 服务与进程状态 | `adb shell dumpsys activity services`、`pidof`、`dumpsys notification` |
| 前台服务与唤醒锁 | `adb shell dumpsys power`（WakeLock）、`dumpsys activity services`（`foregroundServiceType`） |
| 开机自启路径（REQ-006） | `adb shell am broadcast -a android.intent.action.BOOT_COMPLETED <pkg>`；用 `adb shell am compat enable FGS_BOOT_COMPLETED_RESTRICTIONS <pkg>` 模拟 Android 15+ 的启动限制（§3.4 P-01） |
| Doze（AC-07） | `adb shell dumpsys deviceidle force-idle` / `unidle` / `step` |
| 设备重启（AC-10） | `adb reboot` + 等待 `sys.boot_completed` |
| 服务异常退出与恢复（AC-11） | `adb shell am crash <pkg>`、`adb shell kill -9 <pid>` —— **不含 `am force-stop`**（原因见下方"停止语义三分"） |
| 用户在系统"活动应用"中停止应用 | `adb shell cmd activity stop-app <pkg>`（官方文档为"用户停止应用"推荐的等价命令；**本文件未实测**，需在环境就绪后确认） |
| 强制停止（`FLAG_STOPPED`）后的行为 | `adb shell am force-stop <pkg>` —— **单列，不得用于"自动恢复"类验收** |
| 关闭后彻底停止（AC-12） | `dumpsys` 断言无残留服务/唤醒锁 |
| 通知权限（NFR-005） | `adb shell pm revoke <pkg> android.permission.POST_NOTIFICATIONS` |
| 网络栈层面切换（AC-08 的近似） | `adb shell svc wifi disable/enable`、`svc data`（**视镜像而定，需实测确认**） |
| 崩溃与异常 | `logcat`、`dumpsys dropbox` |
| 短时静置（AC-01/AC-02 的近似） | 直接等待 1 小时，或按任务需要选择代表性时长 |

**停止语义三分（v0.5 新增，依据复核 R-06 与官方文档核对）** —— 三种"应用被停"不可混用：

| 情形 | 手段 | 系统行为 | 能否用于"自动恢复"验收 |
|---|---|---|---|
| 进程被异常终止 | `am crash <pkg>`、`kill -9 <pid>` | 进程死亡，应用**不**进入 stopped state | ✅ 可以（AC-11 的主用例） |
| **用户主动停止**（Android 13+ 系统"活动应用 / Task Manager"的 Stop） | `cmd activity stop-app <pkg>` | 终止整个应用、移除前台服务通知、**不发送任何回调**；应用只能在下次启动时通过 `ApplicationExitInfo.REASON_USER_REQUESTED` 得知 | ❌ 不可以 —— 该情形的期望行为**尚未决定**（§16.2 Q-21） |
| 强制停止 | `am force-stop <pkg>` | 应用进入 **stopped state**；Android 15 起系统**取消其所有待处理 intent**，且只有**用户直接或间接与应用交互**才能解除该状态 | ❌ **不可以** —— 必然产生"无法恢复"的假失败 |

> 平台依据（2026-09-23 核对）：① 用户停止前台服务应用的行为与官方测试命令：<https://developer.android.com/develop/background-work/services/fgs/handle-user-stopping>；② stopped state 的解除条件与 intent 取消：<https://developer.android.com/about/versions/15/behavior-changes-all>。
> 本节同时是 REQ-003 删除"用户停止的唯一途径是开关或通知"这一表述的依据，并列出 **REQ-007 的判定闭合**问题（观察窗口待定，Q-22）。

**模拟器不可靠覆盖 → 标记为 L2 真机最终验收项**（§14.4，不阻塞任务）：

| 能力 | 为何不可靠 |
|---|---|
| 真实局域网设备发现（跨主机组播、AP 隔离、VLAN） | 模拟器处于 NAT 虚拟网络，跨主机组播/广播通常不可靠（需实测确认） |
| 后台长期保活（AC-03/04/05 的 8 / 24 / 72 小时） | 模拟器没有真实的内存压力、省电策略与厂商回收行为；且长跑不适合每个任务执行 |
| 不同厂商 Android 行为（MIUI / EMUI / ColorOS 的自启与后台限制） | 仅有 AOSP 镜像 |
| 真实通知 / 权限交互差异 | 系统 UI、通知渠道策略与真机不同 |
| 真实 Wi-Fi / 移动网络切换、路由器重启（AC-09） | 模拟器无真实射频与路由器 |
| 真机文件系统与系统分享行为（SAF、相册保存、share intent 来源） | 模拟器存储与分享来源有限 |
| 电池优化白名单 / "后台运行不受限" / 厂商自启动设置页（REQ-009、AC-03 起效前提） | 这些系统设置页在模拟器上不存在或不同 |

**开发期对网络相关要求的替代验证方式**（DEC-17；用于开发阶段，**不替代** L2）：

1. 同一主机运行**多个模拟器实例**互测；
2. **本机多实例**：模拟器 + 本机其他构建（桌面版 / `cli/`）互为对端；
3. `adb forward` / `adb reverse` 建立端口通道，直接对目标 HTTP 端口发起真实请求，验证服务端与注册流程；
4. Mock / 自动化集成测试覆盖发现、注册与上传的消息流（不依赖真实组播可达性）。

### 14.4 L2 真机最终验收项（发布前一次性人工验收）

环境：仓库所有者本人的 Android 16 真机（机型待补充，Q-11）+ 真实局域网 + 第二台可运行 LocalSend 的设备。
通用前置条件：

1. 两个开关均为**默认开启**状态（若被关闭则先开启）；引导三项**均无"未满足"** —— 允许"无法检测 / 用户自述已设置"（REQ-009、A-17；v0.5 修订，依据复核 R-09）。
2. 本机与探测设备处于**同一 Wi-Fi 子网**，无客户端隔离（AP isolation）、无跨 VLAN。
3. 本机在验收期间不被手动打开应用（除 AC-06 要求划掉任务）。
4. 通信与传输所需的既有权限（含 `ACCESS_LOCAL_NETWORK`、通知权限）均已授予。

> **"可被发现"的判定口径（DEC-05）**：另一台设备在**任何时间**发起扫描都能发现本机。
> **通过判据 = 可被发现**（不要求同时完成一次传输）。传输能力属既有功能（REQ-011 覆盖），故每个观测点**另行记录**"能否完成一次传输"作为观测数据，但**不作为通过门槛**。
> AC-06/07/10/11/12/13 已在 §14.3 列出对应的 L1 自动化近似手段；本表是它们的**最终确认**，也包含模拟器不可覆盖的 AC-01…AC-05、AC-08、AC-09。

> **"任何时间"的可测化缺口（v0.5 新增，依据复核 R-03）**：DEC-05 要求"任何时间发起扫描都能发现本机"，但验收实际只能**离散观测**；AC-07 又允许"记录延迟、不设门槛"，两者在延迟非零时互相矛盾 —— 同一结果可同时被判通过和失败。
> 本文件不单方决定阈值。所需参数见 §16.2 **Q-22**：**扫描频率、每次最大可接受发现时延、允许的失败次数与连续失败判据**。
> 在 Q-22 落定前，所有 AC 的判定写法为"在观测点扫描，记录发现与否与实际时延"，**不得**表述为"已证明全程任意时刻在线"。
> **另注**：AC-07 的"不设门槛"**超出 DEC-07 的原文范围** —— DEC-07 只覆盖耗电/资源阈值与 REQ-007 的恢复时限，并未覆盖 Doze 下的可达性延迟。该延伸需你追认（见 §20）。

| 编号 | 场景 | 验收条件 | 判定 | L1 是否有近似手段 |
|---|---|---|---|---|
| AC-01 | 后台 1 小时 | 应用退到后台放置 1 小时后 | 在任意时刻发起扫描均可发现本机 | 有（短时静置 + `dumpsys`） |
| AC-02 | 息屏 1 小时 | 锁屏息屏 1 小时后 | 在任意时刻发起扫描均可发现本机 | 有（短时静置 + `dumpsys`） |
| AC-03 | 息屏 8 小时 | 锁屏息屏 8 小时后 | 在任意时刻发起扫描均可发现本机 | **无**（长跑，仅真机） |
| AC-04 | 空闲 24 小时 | 无任何交互静置 24 小时后 | 在任意时刻发起扫描均可发现本机 | **无**（长跑，仅真机） |
| AC-05 | 充电连续 72 小时 | 设备保持充电、后台接收开启、期间无任何交互，连续运行 72 小时 | 全程在任意时刻发起扫描均可发现本机；无服务崩溃、无异常重启 | **无**（长跑，仅真机） |
| AC-06 | 划掉最近任务 | 从最近任务列表划掉应用后 | 1 分钟内仍可被发现（按 REQ-005，允许部分 ROM 失败并记入已知限制） | 部分（`am force-stop` 近似，语义不同） |
| AC-07 | Doze 状态 | 设备静置进入 Doze（熄屏、未插电、无移动）后 | 在观测点扫描均可发现本机；**记录实际延迟，门槛待定**（Q-22；v0.5：原"不设门槛"超出 DEC-07 原文范围） | 有（`dumpsys deviceidle force-idle`） |
| AC-08 | Wi-Fi 断开重连 | 断开 Wi-Fi 再重连同一网络，全程不打开应用 | 自动恢复监听，可被发现；无需用户操作（REQ-010） | 部分（`svc wifi` + 端口探测） |
| AC-09 | 路由器重启 | 重启路由器至网络恢复，全程不打开应用 | 自动恢复监听，可被发现；无需用户操作（REQ-010） | **无**（无真实路由器） |
| AC-10 | 设备重启 | 重启设备，全程不打开应用 | **用户首次解锁后**自动恢复，可被发现（REQ-006 第 5 条、A-15） | 有（`am broadcast BOOT_COMPLETED` / `adb reboot`） |
| AC-11 | 服务异常退出 | 人为终止常驻服务（手段限 `am crash` / `kill -9`，**不得**用 `am force-stop`，§14.3） | 自动恢复，可被发现；**记录实际恢复耗时，观察窗口待定**（Q-22；原"不设时限"不可判定） | 有（`am crash` / `kill -9`） |
| AC-12 | 关闭后彻底停止 | 由 REQ-008 推导：关闭「后台接收」后 | 常驻通知消失；本机不再可被发现；无残留保活组件（唤醒锁/定时任务）；重新开启后 AC-01 仍可通过 | 有（`dumpsys` 断言） |
| AC-13 | 「开机自启」独立生效 | 由 REQ-006 推导：保持「后台接收」开启、关闭「开机自启」后重启 | 重启后**不**自动恢复（符合用户意图）；再开启后重启可恢复 | 有（`am broadcast` + `dumpsys`） |

### 14.5 判定与证据要求（所有层级）

- **通过** = 在观测点的**任意时刻发起扫描即可发现本机**（DEC-05；L1 场景按其对应命令的断言）。
- **失败** = 未达到判定；须记录机型/镜像与系统版本、当时的电量与省电状态、是否处于 Doze、日志片段。
- 对 ROM 导致、且已按 REQ-009 完成引导后仍失败的项，记入"已知限制"（BR-04），**不通过也如实记录**，不得以"系统行为"为由判为通过。
- **L1 证据** = 实际命令 + 退出码 / 测试输出 + `logcat` / `dumpsys` 摘录，写进完成报告。
- **L2 证据** = 执行时间、起始状态、观测时刻、结果、失败时的日志与系统设置截图。
- **不得声称未实际执行的测试已经通过**；未执行即写"未验证 + 原因"（`AGENTS.md` §4/§9）。

### 14.6 环境现状与剩余步骤（**v0.5 重测，2026-09-23**）

> 本节原记录（"以下全部缺失"）来自更早一次限定范围检索，**已被本次实测取代**（依据复核 R-12/R-14 的事实部分）。原安装步骤清单已大部分完成，故本节改写为"现状 + 剩余步骤"。

**实测方式**：`command -v <tool>`、`fvm flutter --version`、`fvm flutter doctor`、`ls` 直接检查 SDK 目录、`emulator -list-avds`。

**已就位（本次实测通过）**：

| 项 | 实测证据 |
|---|---|
| Flutter（经 fvm） | `fvm flutter --version` → `Flutter 3.41.9 • channel stable`、`Dart 3.11.5`，与 `.fvmrc` 一致；SDK 位于 `~/fvm/versions/3.41.9` |
| Rust 工具链 | `cargo 1.97.1`、`rustc 1.97.1`（`~/.cargo/bin`） |
| JDK | `JAVA_HOME=C:\Users\Zen\AppData\Local\Programs\Temurin\jdk-17.0.20.1+1`（注意 `java` **不在 `PATH`**） |
| Android SDK | `ANDROID_HOME=…\Android\Sdk`：`platforms/android-36`、`build-tools/36.0.0`、`ndk/28.2.13676358`、`cmake/3.22.1`、`cmdline-tools/latest/bin/sdkmanager.bat`、`platform-tools/adb`、`emulator/emulator.exe` |
| 系统镜像 | `system-images/android-36/google_apis/x86_64` |
| AVD | `emulator -list-avds` → `localsend-api36` |
| 仓库内配置 | `app/android/local.properties` 已指向上述 Flutter 与 Android SDK；该文件**未被版本控制**（符合 `AGENTS.md` §8） |

**仍缺 / 仍有问题（本次实测）**：

| 项 | 实测证据 | 影响 |
|---|---|---|
| Android SDK 许可未接受 | `fvm flutter doctor` → `[!] Android toolchain … ✗ Android license status unknown` | Android 构建可能被拒 |
| 模拟器实例 offline | `fvm flutter doctor` → `! Device emulator-5554 is offline` | M-05 之前不可用，需先排查 |
| Visual Studio 缺 C++ 工作负载 | `fvm flutter doctor` → 缺 `MSVC v142`、`C++ CMake tools for Windows`、`Windows 10 SDK` | **非 Android 桌面构建不可用** ⇒ NFR-006 的"非 Android 平台构建检查"当前**无法执行**，只能写"未验证 + 原因"（复核 R-11/R-15） |
| `flutter_rust_bridge_codegen` | 不在 `PATH`（`~/.cargo/bin` 中无） | 仅当改动 FRB 接口时需要；本期未计划 |
| Chrome | 未安装 | 不影响本期（Web 目标不在范围） |

**剩余步骤**：

| 步骤 | 命令 | 验收方式 |
|---|---|---|
| 1 | 接受 Android SDK 许可：`fvm flutter doctor --android-licenses` | 退出码 0，再跑 `fvm flutter doctor` 不再出现 `[!] Android toolchain` |
| 2 | 排查 `emulator-5554` offline：`adb devices`、`adb kill-server && adb start-server`，必要时重开模拟器 | `adb devices` 显示 `device` |
| 3（按需） | 安装 Visual Studio 的"使用 C++ 的桌面开发"工作负载 | 仅当需要非 Android 构建检查时 |
| 4（按需） | `cargo install flutter_rust_bridge_codegen` | 仅当改动 FRB 接口时 |

**授权边界（复核 R-12 的实质问题）**：步骤 1、2 会改变本机外部状态，执行构建/测试会写入 `target/`、`.dart_tool/` 等被忽略目录 —— 按 `AGENTS.md` §6/§8 需**显式授权**（§16.2 Q-19）。
**必须区分两件事**：工具链就绪与否**不阻塞**需求与架构设计；但**任何实现任务的 L1 验收在未获授权、未实际执行前一律写"未验证 + 原因"，该任务不得判定为完成**（§14.1）。

> **与 `AGENTS.md` §11 的偏差**：§11 的"环境现状"仍记录工具链全部缺失，与本表不符。`AGENTS.md` 属协作规则文档，本次**未修改**，需单独授权（见 §17 D-08）。

**环境搭建参考（在新机器上重建环境时使用；本次实测前已基本完成，与上表"仍缺"项对照使用）**：

| 步骤 | 内容 | 验收方式 |
|---|---|---|
| 1 | 取得 Flutter SDK，版本须与 `.fvmrc`（`3.41.9`）一致。两条路径：① 初始化仓库内的 `support/submodules/flutter` 子模块（与 pin 完全一致）；② 安装 `fvm`（Windows 独立安装器）后在本仓根目录执行 `fvm install` | `fvm flutter --version` 显示 3.41.9 |
| 2 | 安装 JDK（Android 构建所需，建议 Temurin 17 或 Android Studio 自带的 JBR） | `java -version` |
| 3 | 安装 Android SDK 命令行工具（`commandlinetools-win-*.zip` → `%LOCALAPPDATA%\Android\Sdk\cmdline-tools\latest`），设置 `ANDROID_HOME` / `ANDROID_SDK_ROOT`（**本次实测：二者均已设置**，见上表） | `sdkmanager --version` |
| 4 | `sdkmanager --licenses` 接受许可 | 退出码 0 |
| 5 | `sdkmanager "platform-tools" "platforms;android-36" "build-tools;36.0.0" "emulator"` | `adb version`、`emulator -version` |
| 6 | 安装 API 36 系统镜像：`sdkmanager "system-images;android-36;google_apis;x86_64"` | `sdkmanager --list_installed` 含该项 |
| 7 | 创建 AVD：`avdmanager create avd -n localsend-api36 -k "system-images;android-36;google_apis;x86_64"` | `emulator -list-avds` 含 `localsend-api36` |
| 8 | 启用硬件加速（Windows 需 WHPX 或 AEHD；须在 BIOS 中开启 CPU 虚拟化） | 模拟器正常启动 |
| 9 | `fvm flutter doctor -v`，逐项解决 | 无未解决项 |
| 10 | 首次构建与冒烟：`fvm flutter pub get` → `build_runner` / `slang` codegen → `fvm flutter build apk --debug` → 装入模拟器 | §14.2 的 M-01…M-07 全部可执行 |

**已知需在环境就绪后核实的项（v0.5 更新）**：

1. 现状 `compileSdkVersion 36` 而 `targetSdkVersion 37`（§3.1）；构建期是否因此报错或仅告警，需实测（不属本需求改动，仅记录）。
2. §14.3 表中标注"需实测确认"的各项：模拟器组播可达性、`svc wifi` 行为、以及 v0.5 新增的 `cmd activity stop-app` 行为。
3. `fvm flutter test` 与 `fvm flutter build apk --debug` 在本机的实际可用性（**本次未实测**：构建/测试属 `AGENTS.md` §6 需单独授权的操作，且 Android 许可尚未接受）。

---

## 15. 假设清单（剩余，**未确认**）

§16.1 的决策已不再列入本表。以下为随本次更新产生的**剩余假设**。

| 编号 | 假设 | 影响面 | 若不成立的后果 | 状态 |
|---|---|---|---|---|
| ~~A-01~~ | **已于 2026-09-23 确认**：「开机自启」默认**开启** → 升为 DEC-14 | REQ-006、BR-02 | — | 已解决 |
| A-09 | 引导允许用户跳过，不强制 | REQ-009 | 若要求强制完成，需改流程与状态机 | 待确认（Q-16） |
| A-11 | 常驻服务的通知渠道名与传输通知的现用渠道（`localsend_foreground_service`）不冲突，由设计阶段命名 | REQ-003 | 若冲突会互相覆盖（DEC-02 已排除复用，但命名仍需避让） | 设计阶段处理 |
| A-12 | AC-11 的"人为终止常驻服务"通过 `adb` 完成（L1 手段） | §14.3、AC-11 | `adb` 属 §14.6 待安装工具链；安装前 AC-11 的 L1 手段无法执行 | 随 Q-19 |
| A-13 | 接收历史、扫描去重等既有数据不因保活改动而改变 | REQ-011 | 兜底由回归验收覆盖 | 低风险 |
| A-14 | 两个开关的持久化沿用 `shared_preferences` 现有键空间，不新增持久化格式 | §10.1、NFR-004 | 若需要新的持久化格式，属 `AGENTS.md` §8 需授权事项（**是否构成"持久化格式变更"见 Q-24**） | 低风险 |
| A-15 | **开机恢复发生在用户首次解锁之后**，解锁前（`LOCKED_BOOT_COMPLETED`）本期不做（复核 R-16；依据 §18 的 Direct Boot 文档） | REQ-006.5、AC-10 | 若要求解锁前恢复，需改用设备加密存储 + `directBootAware`，属新增需求 | **待确认** |
| A-16 | **覆盖安装（应用升级）后不自动恢复**常驻服务，暂列后续（复核 R-16；现状 `autoRunOnMyPackageReplaced: false`） | REQ-006.6、REQ-007 | 若要求恢复，需新增触发路径与对应用例 | **待确认** |
| A-17 | 引导中"无法检测"项的默认呈现为"**需你自行确认**"，用户可手动标记"我已设置"（界面标注为用户自述） | REQ-009、§8.3 | 若要求省略该项或改为强制引导，需改流程 | **待确认** |
| A-18 | **复核建议的临时取向**（仅为让设计可推进，未确认）：Q-13 引导形态取"设置页内联状态区 + 首次启动提示"；Q-14 本期只有"停止"按钮；Q-17 低版本不承诺保活、只要求不显示"已生效" | REQ-009、REQ-003、NFR-004.3 | 三者任一被否决时，对应设计需返工 | **待确认** |

---

## 16. 决策与开放项

### 16.1 已确认决策（2026-09-23，确认人：仓库所有者）

| 编号 | 问题 | **决策** | 影响的条目 |
|---|---|---|---|
| DEC-01 | 改动范围与依赖授权（原 Q-02） | **授权**修改 `packages/localsend_isolates/**`；**授权**新增依赖 | REQ-002、§3.2、NFR-010 |
| DEC-02 | 常驻通知与传输通知的关系（原 Q-05） | **另建渠道/服务**：常驻服务与传输期服务完全分离，互不影响 | REQ-002.3、REQ-003、S-08、E-12、BR-07、REQ-011 |
| DEC-03 | 服务启动被拒时的开关呈现（原 Q-03） | **回滚为"关闭" + 提示原因**（不允许"开而未生效"的假象） | REQ-001 失败分支、E-01、BR-05、§8.3、§10.3 |
| DEC-04 | minSdk / 目标 Android 版本（原 Q-07） | **由我决定 → 保持现状**：不改变 minSdk 与任何 SDK 级别（继续不显式声明，沿用 `flutter.minSdkVersion`）；**验收基线 = Android 16（API 36）**。理由见 NFR-004.2/3 | NFR-004、§5.2、§14.1 |
| DEC-05 | "可被发现"的判定（原 Q-08） | **第二台设备在任何时间发起扫描都能发现本机**；通过判据仅为"可被发现"，不要求完成传输（传输能力作为观测记录） | §14.4、§14.5、REQ-004/005 判定 |
| DEC-06 | 发行渠道（原 Q-10） | **仅私有侧载** → 可直接使用 `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`；无需 Play Console 类型申报 | REQ-009、§10.2、NFR-004.4 |
| DEC-07 | 耗电/资源阈值与恢复时限（原 Q-09/Q-04） | **不设**：耗电与资源本期只观测不设阈值；REQ-007 的恢复**不设时限**，只记录实际耗时 | NFR-002、NFR-003、REQ-007、AC-07、AC-11 |
| DEC-08 | 验收设备（**已被 DEC-16 取代**） | 原为"仓库所有者本人的手机即日常验收设备" | §14.1 → 现降级为 L2 真机最终验收用机 |
| DEC-09 | 需求 ID 格式（原 Q-12） | **修订 `AGENTS.md` §5**：改为 `REQ-<三位序号>` / `NFR-<三位序号>`。**已执行** | §17 D-01、`AGENTS.md` §5 |
| DEC-10 | 开关默认值与独立性（原 Q-01） | 「后台接收」**默认开启**；「开机自启」是**独立开关**（默认值见 DEC-14） | REQ-001、REQ-006、BR-02、BR-09、§8、S-01 |
| DEC-11 | 目标平台类型选择（实现要求 3 的落实） | 常驻服务按 **`connectedDevice`** 设计，不使用 `dataSync` 作为全天候保活（平台核对见 §3.4，结论：该选择同时解开 REQ-002 与 REQ-006 的阻塞） | REQ-002.2、§10.2、§3.4 |
| DEC-12 | 常驻通知是否允许长期显示 | **允许**（实现要求 2） | REQ-003 |
| DEC-13 | 滥用系统的方案 | **不采用** 1 像素 Activity / 无限 Alarm / 高频 WorkManager / 多进程守护（实现要求 9） | §5.2、NFR-001 |
| DEC-14 | 「开机自启」开关的默认值（原 Q-15，2026-09-23 确认） | **默认开启**（与「后台接收」一致） | REQ-006.1、BR-02、§8.2、§10.1 |
| DEC-15 | `AGENTS.md` §1 / §2 的路径引用（原 Q-18，2026-09-23 确认） | **同步修订**为 `docs/requirements.md`。**已执行** | §17 D-02、`AGENTS.md` §1、§2 |
| DEC-16 | 验收策略（2026-09-23 调整） | **分层**：L1 自动化验收（AI 在本机用 Android Emulator + 命令行完成，**每个任务必须执行**，是任务完成判据）；L2 真机最终验收（**仅发布前一次性人工执行**，不作为任务前置）。凡可自动化验证的，不再要求人工真机验收。**缺少真机不构成阻塞** | §14.1、§14.2、§14.4、§16.2 Q-11、§15 A-12、§17 D-04、§1.2；取代 DEC-08 |
| DEC-17 | 网络类能力的开发期验证方式 | 局域网发现、设备互传、后台接收等依赖真实网络环境的能力，**开发阶段**允许用模拟器 / Mock / 自动化集成测试 / 本机多实例验证；真机之间端到端传输属 L2 | §14.3、REQ-010 |

> **对 DEC-07 的范围说明（v0.5 新增，依据复核 R-03/R-04）**：DEC-07 的"**不设时限**"被本文件读作"**不设性能阈值**"，而**不是**"连判定窗口也不设" —— 否则 AC-07/AC-11 将永远无法判失败。判定所需的最小观察窗口见 Q-22。若你的本意确实是"连窗口也不设"，请指出，那会同时使这两条 AC 不可判定。

### 16.2 开放项

**两类（v0.5 区分，依据复核 R-10/R-12）**：标「**设计前必须**」者必须先落定 —— 否则设计会把未确认的假设固化成需求；其余不阻塞架构设计，可随设计一并确定。
**本节的任何一条都不是已确认需求**：在落定前一律按 `AGENTS.md` §10 记录、不实现。

| 编号 | 问题 | 备选 / 备注 |
|---|---|---|
| ~~Q-15~~ | 已决定：「开机自启」默认**开启** → 见 DEC-14 | 已解决（2026-09-23） |
| Q-11 | L2 真机最终验收用机的具体机型与系统版本（**不阻塞任何任务**）。 | 需你提供 |
| Q-13 | 引导页的形态：独立页面 / 设置页内联区 / 首次启动时的对话框。 | 影响 §8.1；建议内联区 + 首次对话框 |
| Q-14 | 常驻通知的文案与是否需要更多操作按钮（如"暂停接收"）。 | 影响 REQ-003 |
| Q-06 | 新增用户可见文案是否允许写入 `app/assets/i18n/*.json`（会与上游 Weblate 流程产生差异）。 | 影响 NFR-009 |
| Q-16 | 引导是否允许跳过（A-09）。**「设计前必须」** | ①允许跳过（当前假设，A-09）；②强制完成。**直接改变业务流程与状态机，设计前必须落定**（原文把它与其它开放项并列为"不阻塞设计"，与它自身的性质不符） |
| Q-17 | 低于 Android 12 的设备上，保活能力不可用时如何呈现（NFR-004.3 的设计输入）。 | ①开关置灰并说明；②允许开启但标注"本设备不受支持" |
| ~~Q-18~~ | 已决定：同步修订 → 见 DEC-15 | 已解决（2026-09-23） |
| Q-19 | **是否授权执行会改变本机外部状态 / 写入被忽略目录的操作**：①接受 Android SDK 许可（`fvm flutter doctor --android-licenses`）；②排查模拟器 offline；③执行 `pub get` / codegen / `analyze` / `test` / 构建（写入 `.dart_tool/`、`target/`）。**v0.5 重述**：工具链已就位（§14.6 实测），原"授权安装工具链"的前提不再成立。 | ①**授权**（建议 —— L1 是任务完成判据，§14.1）；②暂不授权，则 L1 各项一律写"未验证 + 原因"、任务**不得判定为完成**。**此条不阻塞需求与架构设计**（复核 R-12 的实质问题就出在这里：原 D-04 的"不视为任务阻塞"被误读成"不阻塞实现验收"） |
| Q-20 | 关闭「后台接收」时，**进程级单实例**的 HTTP 服务端 / 发现机制如何处置（REQ-008 与 E-12 冲突，复核 R-02）。**「设计前必须」** | ①立即停止（进行中的传输会中断，须同时改写 REQ-008 边界与 E-12）；②等当前传输结束后再停（需定义"结束"与超时）；③有传输时不允许关闭（开关延迟生效） |
| Q-21 | 用户在 Android 13+ 系统"活动应用"中 Stop 之后，**是否自动恢复常驻**（复核 R-06）。**「设计前必须」** | ①**尊重用户停止**：下次打开应用前不恢复（与 BR-01 口径一致；官方建议用 `ApplicationExitInfo.REASON_USER_REQUESTED` 识别）；②自动恢复。若选①，§8.3 与 AC-11 需相应调整 |
| Q-22 | "任何时间可发现"与"恢复"的**可测边界**（复核 R-03/R-04）：扫描频率、每次最大可接受发现时延、允许失败次数与连续失败判据、恢复观察窗口。**「设计前必须」** | 需你给出数值或口径（也可延用"零时延、零失败"，但须说明其测试方法：观测点数量与总时长）。未落定前 AC-01…AC-11 只能记录数据、不能判通过 |
| Q-23 | P1 失败是否阻塞**本期完成**；BR-04"已知限制"是否阻塞**发布放行**（复核 R-05；见 §6.1） | ①P1 失败不阻塞本期、已知限制不阻塞发布（`AGENTS.md` §9 的"未完成"只按证据判定）；②P1 失败即本期未完成；③其他组合 |
| Q-24 | 新增持久化键（两个开关 + 引导状态）是否构成 `AGENTS.md` §8 的"持久化格式变更"、是否授权（复核 R-13） | ①授权新增（不新增依赖，仅新增键）；②留待设计方案批准；③不落盘（会失去"重启后保持"，与 REQ-001 冲突） |
| Q-25 | 常驻服务**已启动但监听不可用**（HTTP 服务端/组播初始化失败、局域网权限被拒）时的开关呈现（复核 R-08） | ①置灰 + 说明原因；②保持"开"但显示"异常/不可发现"；③回滚为关（同 DEC-03 口径）。**不得**统一显示为"生效中" |
| Q-26 | 老用户**覆盖升级**后缺少两个新键时的默认值（复核 R-07） —— 原文只规定了首次安装/首次启动的默认值 | ①缺失键按"开启"（与 DEC-10/DEC-14 一致，升级后即出现常驻与通知）；②缺失键按"关闭"，等用户主动开启（保守迁移，但与"默认开启"不一致）；③按安装时间或版本区间区分 |
| Q-27 | 引导第 3 项（厂商自启动）不可检测时的默认呈现（复核 R-09；A-17） | ①"无法检测，需你自行确认" + 允许用户自述已设置；②省略该项（只保留前两项）；③保留但标注"本设备无法确认" |
| Q-28 | 原始 10 项验收标准与 AC-01…AC-11 的**逐项对应关系**（复核 R-01；原文未入库，见 §2.3） | 请补录原始 10 项原文并逐项确认；若某项被拆成两条 AC 或被合并，请在 §2.3 注明 |

> Q-13 / Q-14 / Q-17 的**临时取向**（复核建议、未确认）见 §15 的 A-18：仅为让设计可推进，任何一条被否决时对应设计需返工。
> Q-20…Q-28 均由本次复核新增，编号续排以保持既有编号稳定。

---

## 17. 与 `AGENTS.md` 的偏差与冲突记录

> 依据 `AGENTS.md` §10：指出冲突与影响，不静默修改。

| 编号 | 冲突/偏差 | 状态 |
|---|---|---|
| D-01 | §5 规定需求 ID 格式为 `REQ-<域>-<三位序号>`，与本次使用的 `REQ-001…` 冲突。 | **已解决（DEC-09）**：已按授权修订 `AGENTS.md` §5 为 `REQ-<三位序号>` / `NFR-<三位序号>`，并把该节内的需求正文路径引用由 `requirement.md` 更新为 `docs/requirements.md`。 |
| D-02 | §1 的文档入口表曾写需求文档为 `requirement.md`（目录未指定），实际为 `docs/requirements.md`；§2 第 3 条也引用了 `requirement.md`。 | **已解决（DEC-15）**：已按授权把 `AGENTS.md` §1 入口表与 §2 第 3 条的路径引用更新为 `docs/requirements.md`。 |
| D-03 | §3 规定默认只改 `app/`，改 `packages/` 需显式授权。 | **已解决（DEC-01）**：已获授权修改 `packages/localsend_isolates/**`。仍然**不**授权改 `packages/core`、`server/`、`cli/`（§5.2）。 |
| D-04 | §4 规定构建/测试命令在"工程初始化并实测后回填"，当前不可用；而本文件 §14 要求验收。 | **已解决（DEC-16）**：验收分层后，L1 自动化验收是任务完成判据、L2 真机验收仅发布前；工具链缺口改为 §14.6 的安装清单 + Q-19 的授权，**不再视为任务阻塞**。`AGENTS.md` §4 的命令回填条件仍待工具链安装并实测后满足。 |
| D-05 | 本文件新增了原想法之外的条目：REQ-011（不回归，源自原始想法 4，属落地而非扩充）、AC-12/AC-13（由 REQ-008/REQ-006 推导，用于可验证性）、BR-01…BR-09（对已有要求的归纳）、E-01…E-17（边界情况展开）、§3.4 平台核对（事实而非需求）。 | 无功能扩充。如认为其中任一超出你的意图，请指出，我移除。 |
| D-06 | 本文件与 `AGENTS.md` 的**默认行为变更**值得显式记录：BR-02 使应用首次启动即常驻并有通知，这是对既有默认行为的改变，与"不改变软件其他功能"（原始需求 4）在**默认值层面**存在张力 —— 该张力由 DEC-10 明确选择"默认开启"解决，且 REQ-011 保证功能语义不变。 | 已记录，无需处理；仅提醒后续复核者。 |
| D-07 | `README.md`（行 315–322）仍写"目标用户 / 要解决的问题 **待确认**"，并称"本 fork 未对这两份文档做改动、其中的命令与约束继续有效"；前者与现状（需求文档已确认、`AGENTS.md` 已含本 fork 规则）冲突。另：README 把 v1.18.2 的 `230fb692` 记为 fork 基线（**正确**，但本文件 v0.4 曾把代码基线写作 `fe5e5929`）。 | **未解决（复核 R-14）**。README **不在本次修订范围**，**未修改**；请授权后再改。本文件已把三种基线分开标注（头部表、NFR-010）。 |
| D-08 | `AGENTS.md` §11 的"环境现状"仍记录工具链全部缺失（`fvm`/`flutter`/`dart`/`cargo`/`rustc`/JDK/Android SDK），与本次实测不符（§14.6）。 | **本文件已修正**（§14.6 重测并注明偏差）；`AGENTS.md` 属协作规则文档，**未修改**，需单独授权。 |

---

## 18. 参考资料

平台约束（§3.4）与平台行为（REQ-003、REQ-006、§14.3）的来源，核对日期 2026-09-23：

1. 前台服务的变更（含 Android 15+ 的 `BOOT_COMPLETED` 启动限制、`dataSync` / `mediaProcessing` 运行时长上限、**Android 16 起由前台服务发起的后台作业配额** → §3.4 P-06）：<https://developer.android.com/develop/background-work/services/fgs/changes>
2. 前台服务类型（含 `connectedDevice` 的权限与前置条件、各类型的超时 → §3.4 P-01…P-05）：<https://developer.android.com/develop/background-work/services/fgs/service-types>
3. Android 15 前台服务类型的变更（`dataSync` / `mediaProcessing` 的超时）：<https://developer.android.com/about/versions/15/changes/foreground-service-types>
4. **用户停止前台服务应用**（Android 13+ 的 Task Manager / "活动应用" Stop：终止整个应用、移除通知、**无回调**；建议检查 `ApplicationExitInfo.REASON_USER_REQUESTED`；官方测试命令 `cmd activity stop-app`）→ REQ-003、§14.3、Q-21：<https://developer.android.com/develop/background-work/services/fgs/handle-user-stopping>
5. Android 15 行为变更（**stopped state 只能由用户直接或间接操作解除**；进入该状态时取消全部待处理 intent）→ §14.3：<https://developer.android.com/about/versions/15/behavior-changes-all>
6. Direct Boot（`ACTION_BOOT_COMPLETED` 对**非** direct-boot-aware 应用在**用户解锁后**投递；`LOCKED_BOOT_COMPLETED` 与设备加密存储）→ REQ-006.5、A-15：<https://developer.android.com/privacy-and-security/direct-boot>

> 以上为文档核对结论；**§3.4 的每一条都需在目标真机（Android 16）上复核**，复核结果回填本节。
> 第 4、5 条由 v0.5 的复核响应（R-06、R-16）新增；第 4 条中的测试命令**尚未在本机实测**（§14.6）。

---

## 19. 附：术语

| 术语 | 含义 |
|---|---|
| 后台接收 | 本需求引入的用户可选能力：应用在后台长期保持发现与监听（REQ-001…REQ-003） |
| 开机自启 | 独立的用户开关：控制设备重启后是否自动恢复后台接收（REQ-006） |
| 常驻服务 | 承载后台接收的长期前台服务，类型为 `connectedDevice`（REQ-002） |
| 传输期服务 | 现状已有的 `dataSync` 前台服务，仅在传输进行中使用（§3.2，DEC-02 下保持不变） |
| 可被发现 | 同网段设备在任意时刻发起扫描都能发现本机（判定见 §14.4、DEC-05） |
| 引导 | REQ-009 的一次性设置引导（电池优化、后台不受限、国产 ROM 自启） |
| 已知限制 | 由系统/ROM 行为导致、已实测记录的能力上限（BR-04） |

---

## 20. 独立复核意见的处理记录（v0.5，2026-09-23）

> 来源：一次独立只读复核（结论"不通过"，问题编号 R-01…R-16）。
> 处理原则：**能由事实判定的一律修正；需要业务取舍的一律不替你决定**，改为写入 §16.2 并标注「设计前必须」。
> 涉及外部平台行为的，均以官方文档核对（§18）；涉及本机事实的，均以本次实测为准（§14.6）。

### 20.1 逐条处理（问题 ID → 修改位置 → 结论）

| ID | 修改位置 | 处理结论 |
|---|---|---|
| R-01 | §2（拆为 2.1/2.2/2.3 + 可审计性说明）；§16.2 Q-28 | **成立**。全仓检索确认原始 10 条实现要求与 10 项验收标准的原文**不在仓库内**，故删除"确保无遗漏、无扩写"的断言，改为可审计性说明 + 占位表待你补录；"10 项 → 11 条 AC"的对应关系列入 Q-28。**映射本身未改动**（不凭记忆编造原文）。 |
| R-02 | REQ-002 第 3/4 条改写、新增资源归属表与冲突说明；§16.2 Q-20 | **成立**。已消除"完全分离"与"共用同一常驻主体"的歧义（= 传输期间常驻服务不中断，而非把传输期服务并入）；新增资源级归属表。**REQ-008 与 E-12 在"HTTP 服务端为进程级单实例"前提下确实冲突**，列入 Q-20（设计前必须），未替你选择。 |
| R-03 | §14.4 判定口径新增"可测化缺口"；AC-07 行；§16.2 Q-22 | **成立**。"离散观测 vs 任何时间"的矛盾已显式写出；另发现并记录：AC-07 的"不设门槛"**超出 DEC-07 原文范围**（DEC-07 只覆盖耗电/资源阈值与 REQ-007 恢复时限）。阈值参数须你给出（Q-22）。 |
| R-04 | REQ-007、REQ-010 新增"判定闭合"；AC-11 行；Q-22 | **成立**。原文"不设时限"使验收不可终止；已改为"不设阈值、但必须设观察窗口"，窗口口径待你给（Q-22）。 |
| R-05 | §6.1 新增"三套门槛"；§16.2 Q-23 | **成立**。任务完成 / 本期完成 / 发布放行 已显式分开；P1 失败与"已知限制"各自的阻塞性待你定（Q-23）。 |
| R-06 | REQ-003（删除"唯一途径"、新增三条途径与可划掉性）；§8.3；§14.3（停止语义三分 + 新命令）；§14.4 AC-11；§18 第 4/5 条 | **成立，已按官方文档核对修正**。① Android 13+ 系统"活动应用"Stop 会终止整个应用、移除通知且**无回调**，应用只能靠 `ApplicationExitInfo.REASON_USER_REQUESTED` 事后识别，官方测试命令为 `cmd activity stop-app`；② `am force-stop` 使应用进入 stopped state，Android 15 起**仅用户操作可解除**且进入时取消全部待处理 intent ⇒ **不得**用于恢复类验收；③ `cmd activity stop-app` **本机未实测**，已按 `AGENTS.md` §4 标注"需实测确认"。**"用户停止后是否自动恢复"需你定（Q-21）**。 |
| R-07 | §16.2 Q-26 | **成立**。原文确实只规定"首次安装/首次启动"的默认值。首次安装 / 覆盖升级 / 清除数据 三种情形已列出选项，取舍需你定（Q-26）—— 未替你决定。 |
| R-08 | §8.3（补 4 行状态 + "生效中"口径）；§16.2 Q-25 | **成立**。已补入"无局域网 / 恢复中 / 监听不可用 / 被系统用户停止"四种状态，并明确"生效中"= 服务运行 **且** 监听可用；四处呈现方式未定者列入 Q-25，未统一写成"生效中"。 |
| R-09 | REQ-009 新增"可检测性边界"与三态取值；§10.1；§15 A-17；§16.2 Q-27；§14.4 前置条件 1 | **成立**。三项状态改为**已满足 / 未满足 / 无法检测**；"跳转 ≠ 已获准"、"系统实时状态与用户已读/已跳过分离"已写入。**这改变了 REQ-009 的可观察结果与 L2 前置条件表述 → 需你追认**（见 20.3）。 |
| R-10 | 文档头部状态；§16.2 标题与 Q-16/Q-14；§5.2 新增一行；§15 A-18 | **成立**。文档状态改为"需求正文已确认，设计前必须落定项未定"；**Q-16 标为「设计前必须」**；"暂停接收"按 `AGENTS.md` §10（未确认的新需求不实现）明确不进入本期；Q-13/Q-14/Q-17 的临时取向记为 A-18。 |
| R-11 | §14.2 M-07；新增 §14.2.1 最小覆盖集与追踪矩阵；NFR-006；NFR-010 | **成立**。M-07 的循环判据改为"任务**预先声明**的断言全部通过"；最小覆盖集（9 类场景）与"追踪矩阵"列为设计要求；非 Android 构建检查因 VS 组件缺失**当前不可执行**，已明确只能写"未验证 + 原因"。 |
| R-12 | §14.6 重测重写；§16.2 Q-19 重述；§17 D-08 | **成立，且其事实前提已变化**。本次实测：工具链**已就位**（`fvm` + Flutter 3.41.9 / `cargo`+`rustc` 1.97.1 / JDK 17 / Android SDK 36 + AVD `localsend-api36`），原"全部缺失"记录作废；仍缺：SDK 许可未接受、模拟器实例 offline、VS C++ 工作负载、`flutter_rust_bridge_codegen`。Q-19 已从"授权安装"改为"**授权执行构建/测试与许可接受**"，并明确它**不阻塞设计、但阻塞实现任务的验收完成**。 |
| R-13 | §10.1（候选 vs 已确认）；§15 A-14；§16.2 Q-24 | **成立**。`shared_preferences` 与引导状态的取值形态已标为**候选方案**（由设计确定），已确认的只是"不新增依赖、不上传、不写外部存储"；新增持久化键是否属 §8 的"持久化格式变更"列为 Q-24，**未单方判断**。 |
| R-14 | 文档头部（三种基线）；NFR-010；§17 D-07/D-08 | **成立**。已区分上游代码基线 `230fb692` / 协作规则基线 `fe5e5929` / 本文档基线 `b8f4900c`；README 的偏差记入 D-07 但**未修改**（不在本次修订范围），需授权。 |
| R-15 | §13.1 新增分类；NFR-007 测量条件；NFR-006 / NFR-010 可验证方式 | **成立**。NFR-002/003 明确为**证据采集型、不参与质量放行**；NFR-007 改为 5 条可枚举问题；NFR-006/NFR-010 由主观表述改为可枚举检查项。 |
| R-16 | REQ-006 新增第 5/6 条；AC-10；§15 A-15/A-16；§18 第 6 条 | **成立，已按官方 Direct Boot 文档核对**：**非** direct-boot-aware 应用的 `BOOT_COMPLETED` 在**用户解锁后**才投递，且 `shared_preferences` 位于凭据加密存储 ⇒ 本期只承诺"**首次解锁后**恢复"（A-15）；覆盖安装后是否恢复列为 A-16。**AC-10 判定口径据此改变 → 需你追认**（见 20.3）。 |

### 20.2 未采纳或未完全采纳的意见及依据

| 意见 | 处理 | 依据 |
|---|---|---|
| R-01 称可能"少一项、重复一项或夹带推导项" | **部分不采纳**：AC-12/AC-13 在 §14.4 已明确标注为"由 REQ-008/REQ-006 推导"，故"夹带推导项"不成立；真实问题只有"原始 10 项与 AC-01…AC-11 的逐项对应无载体"。 | §14.4 原文；§2.3 补充说明 |
| R-06 建议用 `cmd activity stop-app` 作为用户停止的测试手段 | **采纳为候选，但不标为"可用"**：本机未执行过该命令，按 `AGENTS.md` §4 只能写"需实测确认"。 | `AGENTS.md` §4；§14.3、§18 第 4 条 |
| R-11 要求补"非 Android 构建命令" | **部分不采纳（无法补）**：本机缺 Visual Studio 的 C++ 工作负载，非 Android 桌面构建**不可用**；补一条跑不通的命令违反 `AGENTS.md` §4。改为标注"未验证 + 原因"，并保留可执行的静态隔离检查。 | §14.6 实测输出；NFR-006 |
| R-12 称 D-04 与 Q-19 自相矛盾 | **采纳其区分**（不阻塞设计 / 阻塞实现验收），但指出 D-04 的事实前提已因环境变化而部分消失（工具链已就位）。 | §14.6 实测 |
| R-14 要求修 README | **记录但未执行**：本次只修改目标文档；README 需单独授权。 | §17 D-07；`AGENTS.md` §10 |

### 20.3 影响已确认需求或关键设计的改动（**需你追认**）

以下 6 处改变了此前"已确认"的表述。每处都集中在对应小节，若你不认可，回退成本仅为还原该段：

| # | 改动 | 位置 | 原表述 → 现表述 |
|---|---|---|---|
| 1 | 开机恢复时刻 | REQ-006.5、AC-10、A-15 | "系统启动完成后" → "**用户首次解锁后**"（平台事实） |
| 2 | 引导状态取值 | REQ-009、§14.4 前置条件 1、A-17 | "已满足/未满足" → "**已满足/未满足/无法检测**"；L2 前置由"三项均显示已满足" → "三项均无'未满足'" |
| 3 | Doze 延迟门槛 | AC-07、§14.4 判定口径 | "记录延迟、不设门槛（DEC-07）" → "**门槛待定（Q-22）**"，并指出原表述**超出 DEC-07 原文范围** |
| 4 | 恢复时限 | REQ-007、REQ-010、AC-11 | "不设时限" → "**不设阈值，但必须设观察窗口**"（否则验收不可终止） |
| 5 | 文档状态 | 头部表 | "已确认" → "**需求正文已确认；设计前必须落定项未定**" |
| 6 | REQ-002 资源归属 | REQ-002 资源表 | DEC-02 的"分离"具体化为资源级归属；其中"HTTP 服务端 + 发现机制"一行标为**冲突点**（Q-20），未决定 |

### 20.4 仍需你决定的问题（按优先级）

**A. 设计前必须落定（否则设计会把未确认假设固化成需求）**

1. **Q-16** 引导是否允许跳过（直接改流程）。
2. **Q-20** 关闭「后台接收」时，进程级单实例的 HTTP 服务端/监听如何处置（REQ-008 × E-12 冲突）。
3. **Q-21** 用户从系统"活动应用"停止应用后，是否自动恢复。
4. **Q-22** "任何时间可发现"与恢复的可测边界（扫描频率、最大发现时延、允许失败次数、恢复观察窗口）。
5. **Q-23** P1 失败是否阻塞本期完成；"已知限制"是否阻塞发布放行。
6. **Q-25** "服务已启动但监听不可用"时的开关呈现。
7. **Q-26** 覆盖升级后缺少两个新键时的默认值（以及清除数据后的初始化规则）。
8. **Q-28** 原始 10 项实现要求与 10 项验收标准的**原文补录**及逐项对应（恢复需求可审计性）。
9. **Q-27** 引导第 3 项不可检测时的默认呈现。
10. **Q-24** 新增持久化键的授权。

**B. 进入实现阶段前必须解决**

11. **Q-19** 授权接受 Android SDK 许可、排查离线模拟器、执行构建/测试（不阻塞设计，但未授权前任何实现任务都不能判定完成）。

**C. 不阻塞**

12. Q-11（L2 机型）、Q-13 / Q-14 / Q-17（可用 A-18 的临时取向推进）、Q-06（写翻译文件前授权）。

### 20.5 本次修订未做的事（明确边界）

- **未修改任何代码**，未执行构建、测试或模拟器操作（`AGENTS.md` §6 需单独授权）。
- **未改动 `AGENTS.md`、`README.md`**（分别记入 D-08、D-07，需授权）。
- **未替你做任何业务取舍**：§16.2 的开放项只列选项，不预设结论；§15 的 A-15…A-18 明确标为未确认。
- **未补录原始条目原文**（原文不在仓库内，不能凭记忆编造，见 §2.3）。
